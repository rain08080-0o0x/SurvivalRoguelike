# 引き継ぎメモ

## 基本情報
- プロジェクト: 奈落塔 / NarakuProto
- リポジトリパス: `C:\HAL\個人制作\NarakuProto`
- ソリューション: `C:\HAL\個人制作\NarakuProto\DX22_Project.sln`
- ブランチ: `feature/piece-stage-system`
- リモート: `origin https://github.com/rain08080-0o0x/SurvivalRoguelike.git`
- 現在日付: `2026-06-30`

## Git状態
- 直近の実装コミット: `030136b Use native menu for piece editor`
- その前: `2271d0b Add Naraku piece stage editor`
- このメモ作成前時点では `origin/feature/piece-stage-system` より1コミット先行。引き継ぎメモ追加コミット後はさらに先行数が増える。
- 前回pushは環境ポリシーにより拒否された。ユーザーは今回コミットプッシュを依頼しているが、再試行時も環境ポリシーで止まる可能性がある。
- このメモ自身は `030136b` の後続コミットに含まれる想定。

## ビルド/検証
- Debug x64 ビルド成功済み。
- 実行コマンド: `& 'C:\Program Files\Microsoft Visual Studio\2022\Enterprise\MSBuild\Current\Bin\amd64\MSBuild.exe' 'C:\HAL\個人制作\NarakuProto\DX22_Project.sln' /p:Configuration=Debug /p:Platform=x64 /v:minimal`
- `git diff --check` 通過済み。

## 直近の実装要約
- `SceneNarakuPieceEditor` の ImGui メインメニューバーを撤去した。
- Win32 ネイティブメニュー方式に変更した。
- `Startup.cpp` に小ステージエディタ用メニュー生成・シーン別メニュー切替・`WM_COMMAND` 配送を追加した。
- `File -> Save` は `m_requestOpenSavePiecePopup` を使い、次描画で保存ポップアップを開く。
- `File -> Load` は `OpenLoadPieceDialog` を呼ぶ。
- `View` メニューから各ウィンドウ表示 / 非表示を切り替える。
- メニューバーと ImGui ウィンドウ被りは構造的に解消した。

## 主要ファイルと役割
- `DX22_Project/SceneNarakuPieceEditor.h`
- `DX22_Project/SceneNarakuPieceEditor.cpp`
- `DX22_Project/Startup.cpp`
- `DX22_Project/NarakuPieceData.h` / `DX22_Project/NarakuPieceData.cpp`
- `SceneNarakuEditor.*` は元の地形エディタで参考実装。
- `開発記録帳.md` / `タスクリスト.md` / `地形エディタ機能紹介と使い方.md` / `ドキュメント更新ルール.md`

## 小ステージエディタの現状機能
- 小ステージJSON Save / Load
- Drafts / Completed 保存先
- 16x16地形編集
- 頂点 / セル選択、複数選択、高さ編集、セル属性、削除 / 復元
- 採掘ポイント / ロープ / 開始帰還候補の配置編集
- 小ステージHierarchy、`piece_hierarchy.cfg`
- メインウィンドウタイトルへ編集中ファイル名表示

## 未確認/次に見るべきこと
- 実機で `File -> Save / Load` が動くか
- `View` の表示切替が全ウィンドウで動くか
- `SceneNarakuEditor` と `SceneNarakuPieceEditor` を切り替えたときネイティブメニューが競合しないか
- 小ステージHierarchyの実操作確認
- `piece_hierarchy.cfg` のGit管理方針
- 小ステージ生成 / 連結ロジック設計と実装
- Completedフォルダから生成候補収集
- `stageCategory` / `edgeCategory` / `height` 傾向による接続判定
- 3x3 / 4x4敷き詰め生成
- 敵スポーン条件の小ステージ対応
- 配置オブジェクトエディタ / モデル配置Scene

## 作業ルール/注意点
- ユーザーのAGENTS規約: 実装 / 修正はサブエージェントに任せ、メインは調査 / 設計 / レビュー。
- コメントは日本語Doxygen形式。
- 変更時は関連Markdownも更新する。
- 既存変更を戻さない。
- formatter / linter設定はプロジェクト本体では見当たらず、ビルドと `git diff --check` で確認している。

## 推奨再開手順
1. `git status --short --branch`
2. `git log --oneline -5`
3. Debug x64 build
4. 実機確認
5. 必要ならpush再試行
