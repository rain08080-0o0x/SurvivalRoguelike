# Editorとゲームの分離・未確定事項

## 1. この文書の目的

この文書は、「Editorとゲームの分離」を実装する前に決定が必要な事項と、その回答履歴をまとめた確認票である。

- 確定済み仕様は判断の前提として記載する。
- ユーザーが「推奨で」と回答した項目では、その項目内の推奨案を確定仕様として扱う。
- 未回答項目の「推奨案」は未確定であり、採用された仕様ではない。
- 各項目は`ED-01`のようなID単位で回答できる。
- この文書の項目が確定するまでは、プロジェクト分離の実装へ進まない。

## 2. 確定済みの前提

- ゲームとEditorを別プロジェクト、別実行ファイルにする。
- ゲーム側の名称は「奈落塔」とする。
- 現在の既存プロジェクトは削除せず残す。
- ゲームのインゲーム部分は`SceneNarakuProto`を基準にする。
- `SceneNarakuEditor`と`SceneNarakuPieceEditor`はEditor側へ移す。
- 城、ボス、エフェクトなどの既存Editorをどの分離先へ置くかは再確認する。
- 自宅、商店、武具屋、レストラン、リザルトは`SceneNarakuProto`から分離する。
- 正式なTitleとResultの内容は今後作成する。
- Credit Sceneは作成せず、クレジット表示も行わない。
- ゲームのDebugビルドではデバッグウィンドウを利用可能にする。
- ゲームのReleaseビルドではデバッグウィンドウを無効化する。
- 参照ファイルは相対パスで扱う。
- Assetsは出力先ごとにコピーする。
- ゲームセーブはゲーム側基準の`Assets/Save`へ保存する。
- Editorの設定と生成物はEditor側へ保存する。
- Editorからゲームへマップを反映する場合は、ゲーム側の`Assets/Maps`を上書きする。
- Editorの生成プレビューでは15段階すべてを生成できるようにする。
- プレビューではシード指定、固定、再生成を可能にする。
- プレビューはゲーム本番データを書き換えない。
- 俯瞰表示と歩行表示はMenuBarの`View`から切り替える。
- 歩行プレビューでは敵を生成するが静止させ、採掘、能力値消費、死亡を無効化する。
- 歩行プレビューでも層間移動は可能にする。

## 3. ED-01～ED-37 回答反映状況

|ID|状態|反映結果|
|---|---|---|
|ED-01|確定|Editorプロジェクト名とexe名は`NarakuEditor`とし、従来どおりプロジェクト名からファイル名を出す|
|ED-02|確定|3プロジェクトへ完全分離し、NarakuEditorには奈落ステージEditor、奈落塔にはゲーム、既存プロジェクトには残存機能を置く|
|ED-03|確定|共通親出力の下を`Game`、`Editor`、既存・残存プロジェクト用に分離する|
|ED-04|確定|残存部分は独立プロジェクトとして独自の出力先を持つ|
|ED-05|確定|Debug x64とRelease x64だけを対象とし、Win32は廃止する|
|ED-06|確定|各プロジェクトで必要な基盤コードをすべて含め、モデル読込関連も含める。不要な機能は含めない|
|ED-07|確定|ソースは明示的な同期スクリプト、配布用Assetsはビルド後コピーとする|
|ED-08|確定|生成処理と生成用データだけは共通ファイルを両プロジェクトからコンパイルする|
|ED-09|確定|コードはゲーム側を正本として扱う|
|ED-10|確定|exe直接起動時はexeディレクトリ、Visual Studio実行時は`$(ProjectDir)`をカレントディレクトリにする|
|ED-11|確定|各プロジェクトのAssetsを基準とし、Editorで発行したMapをゲーム側へ差し替える|
|ED-12|確定|Save、ログ、一時ファイルはコピーから除外し、既定設定とユーザー設定を分離して既存ユーザー設定を上書きしない|
|ED-13|確定|Editorの発行操作でMapsと参照Modelを検証後に同期し、削除も反映する。5世代バックアップとMapバージョン更新を行う|
|ED-14|確定|Editor固有ファイルとログは既存と同じ相対パスを維持する|
|ED-15|確定|正式Title作成までは現在どおり`SceneNarakuProto`から起動する|
|ED-16|確定|1つの街Scene内で自宅・店舗を施設UIまたは施設モードとして扱う|
|ED-17|確定|結果計算は奈落側に残し、Result表示処理だけを別ファイルへ分け、将来Scene化できる境界にする|
|ED-18|確定|一時進行を保持する専用Singletonを用意し、セーブは永続化だけに使う。クラス名は実装時に決める|
|ED-19|確定|正規遷移専用ルートを設け、結果確定・Singleton移送・保存成功後に解放する。保存失敗時は遷移を中止し、アプリ終了と非正規離脱は探窟放棄扱い|
|ED-20|確定|Credit Sceneを作らず、クレジットも表示しない|
|ED-21|確定|ゲームDebugはゲーム調整だけ、ゲームReleaseはデバッグ機能なし、EditorはEditor機能を保持する|
|ED-22|確定|ゲームReleaseではプリプロセッサ条件によりデバッグコードをコンパイルから除外する|
|ED-23|確定|ゲームとEditorで別エントリポイントを持ち、Editorメニューコードをゲームへ含めない|
|ED-24|確定|15段階を常に生成し、選択値は表示対象・歩行開始先・段階別エリア数の指定に使う|
|ED-25|確定|各段階に`Random / 2 / 3 / 4`を指定できる|
|ED-26|確定|64bitシードを使い、固定中は同一結果、解除中は再生成ごとに新規シードとする。Editor終了後は保持しない|
|ED-27|確定|Preview生成は完全にメモリ内で行い、本番ファイルへ書き込まない|
|ED-28|確定|未保存内容がある場合は、現在内容でPreview、保存してPreview、キャンセルを選べる|
|ED-29|確定|敵AIを完全停止し、表示と確認用当たり判定だけを残す|
|ED-30|確定|落下・水没時は最後の安全地点へ戻し、MenuBarから開始地点へも戻せる|
|ED-31|確定|通常開始地点と選択エリア開始の両方を用意する|
|ED-32|確定|選択エリア中心の俯瞰表示とし、層間移動後は接続先へ自動フォーカスする|
|ED-33|確定|`View`にPreview、俯瞰、歩行、生成設定、開始地点へ戻る項目を設ける|
|ED-34|確定|再生成まで15段階をメモリ保持し、往復しても生成結果を変えない|
|ED-35|確定|生成失敗時は別シードへ自動変更せず、段階・エリア・理由・シードを表示する|
|ED-36|確定|第VII部の推奨完了条件を採用する|
|ED-37|確定|3プロジェクトのDebug x64とRelease x64を検証し、Win32は対象外とする|
|ED-38|確定|NarakuEditorには奈落ステージEditorと生成Previewだけを置き、城・ボス・エフェクト・旧機能は残存プロジェクトへ置く|
|ED-39|確定|残存プロジェクトとexeは`DX22_Project`を維持し、旧機能ランチャーから起動する。ほか2プロジェクトとは遷移させない|
|ED-40|確定|EditorのMenuBarに「ゲームへ発行」を追加し、Mapsと参照Modelを一時準備・検証してからゲーム側へ置換する|
|ED-41|確定|ゲーム側`Assets/Backup/MapPublish/yyyyMMdd_HHmmss/`へMapsと対象Modelを保存し、最新5世代を保持してEditorから復元可能にする|
|ED-42|確定|発行時にMapバージョンを更新し、次回ゲーム起動時に週生成マップ、探索情報、クエスト配置をリセットする。所持金・Lv・装備など恒久進行は維持する|
|ED-43|確定|既定設定は`Assets/Config/Defaults`からコピーし、利用者設定は`Assets/Config/User`へ分けて上書きしない|
|ED-44|確定|新ゲーム初回起動時だけ旧候補パスからセーブを新保存先へコピーし、移行元は削除しない|
|ED-45|確定|現在リポジトリ内のモデル読込構成を維持し、VC143用AssimpのDebug／Release DLLだけを各出力先へコピーする。外部取得は行わない|
|ED-46|確定|同じSolutionへ3プロジェクトを置き、既定スタートアップを「奈落塔」とする|
|ED-47|確定|同一シードからエリア数、接続ID、Piece配置IDがゲームとEditorで一致する自動テストを追加する|
|ED-48|確定|正規性検証、結果確定、Singleton移送、永続保存、保存成功確認、解放、次Scene生成の順とし、保存失敗時は遷移を中止する|
|ED-49|確定|ゲーム実行中は発行を禁止し、発行ロック取得失敗時は上書きせず「実行中なので保存できません」と表示する|

過去の各ED節は、判断理由と選択肢の履歴として残す。ED-01～ED-49はすべて確定済みである。

---

# 第I部 プロジェクトと出力構成

## ED-01 Editor側の名称

### 現状

ゲーム側の名称は「奈落塔」で確定しているが、Editor側のプロジェクト名とexe名が未指定。

### 決める内容

- Visual Studio上のEditorプロジェクト名
- Editorのexeファイル名
- 日本語名をそのままプロジェクト名・exe名へ使うか

### 選択例

1. プロジェクト名・exe名とも`NarakuEditor`
2. プロジェクト名は`NarakuEditor`、exe名は`奈落塔Editor`
3. プロジェクト名・exe名とも`奈落塔Editor`

### 推奨案

プロジェクト名を`NarakuEditor`、exe名を`奈落塔Editor`とする。ビルド設定内ではASCII名を使い、利用者が見る実行ファイルだけ日本語化できる。

## ED-02 最終的なプロジェクト数

### 一致しない可能性がある点

「ゲームとEditorを別プロジェクトにする」と「現在の既存プロジェクトを残す」を両立すると、次の3プロジェクト構成になる。

1. 現在の`DX22_Project`
2. 新しいゲーム用プロジェクト
3. 新しいEditor用プロジェクト

既存プロジェクトをEditor用として転用する場合は2プロジェクトで済むため、どちらか決定が必要。

### 選択肢

1. 既存プロジェクトを保守用として残し、新規にゲームとEditorを追加する（合計3）
2. 既存プロジェクトをEditor用へ変更し、ゲームだけ新規追加する（合計2）
3. 既存プロジェクトをゲーム用へ変更し、Editorだけ新規追加する（合計2）

### 推奨案

既存プロジェクトをEditor用へ変更し、ゲーム用の「奈落塔」プロジェクトを追加する。現在のプロジェクトにEditor機能が多く、移動量を抑えられる。

## ED-03 「出力ディレクトリは共通」の意味

### 一致しない点

次の指定が同時に存在する。

- 出力ディレクトリは共通
- 実行時に出力されるファイルを分ける
- Assetsは出力先ごとにコピーする
- ゲームとEditorで別実行ファイルにする

ゲームとEditorを同一ディレクトリへ出すと、相対パス`Assets`も共有され、「出力先ごとにコピー」「生成物を分ける」と一致しにくい。

### 選択肢

1. 同じ出力ディレクトリへ両exeを置き、Assetsも共有する
2. 共通の親ディレクトリ内に`Game`と`Editor`を分ける
3. Visual Studio既定どおり、プロジェクトごと・構成ごとに別出力する

### 推奨案

共通の親ディレクトリ内で分離する。

```text
Output/
  Debug/
    Game/
      奈落塔.exe
      Assets/
    Editor/
      奈落塔Editor.exe
      Assets/
  Release/
    Game/
    Editor/
```

## ED-04 既存プロジェクトの出力先

### 不足している情報

既存`DX22_Project`を残す場合、そのexeを新しいゲーム／Editorと同じ出力ルートへ置くか、従来の場所へ残すか未指定。

### 決める内容

- 既存プロジェクトを今後もビルド対象にするか
- 既存exeの出力先
- Solution Buildで既存プロジェクトも毎回ビルドするか

## ED-05 Debug・Release・Win32・x64の対象

### 不足している情報

現在はDebug／ReleaseとWin32／x64の構成がある。新プロジェクトで全構成を維持するか未指定。

### 選択肢

1. Debug x64とRelease x64だけ作る
2. 現在と同じ4構成を維持する

### 推奨案

現在主に検証しているDebug x64と、配布用Release x64だけを対象にする。Win32が必要なら、その理由を確定してから追加する。

---

# 第II部 ソースコードの共有と同期

## ED-06 共通コードをコピーする範囲

### 現状

別プロジェクトの場合は、必要なファイルを各プロジェクトディレクトリへコピーしてコンパイルする方針が示されている。

### 不足している情報

次のどこまでを複製するか未指定。

- DirectX、入力、音声、モデル、描画などの基盤
- `Scene`と`SceneManager`
- `NarakuMapData`
- `NarakuStageGenerator`
- プレイヤー、敵、衝突
- ImGui
- 外部ライブラリのヘッダーとlib

### 決める内容

- ゲーム用とEditor用に物理コピーするファイル一覧
- コピーせず共通位置から参照してよい外部ライブラリ
- 新規コード追加時に両方へコピーする規則

## ED-07 コピー方法

### 不足している情報

コードとAssetsを、いつ、どの方法でコピーするか未指定。

### 選択肢

1. 常に手作業でコピーする
2. 専用同期スクリプトを明示的に実行する
3. ビルド前・ビルド後イベントで自動コピーする

### 推奨案

ソースは明示的な同期スクリプト、配布用Assetsはビルド後コピーとする。ビルドのたびにソースを上書きすると、片側の編集中変更を失う危険がある。

## ED-08 生成処理の同一性

### 一致しない点

次の2つが同時に指定されている。

- Editorプレビューはゲームと完全に同じ抽選データ・生成処理を使う
- 別プロジェクトでは生成処理をコピーし、方式変更時はゲーム側も手直しする

物理コピーしたコードは更新漏れによって差が生じるため、「常に完全に同じ」を自動的には保証できない。

### 決める内容

1. Editor側を正としてゲームへ同期する
2. ゲーム側を正としてEditorへ同期する
3. 共通生成コードだけはコピーせず、両プロジェクトで同じファイルをコンパイルする
4. コピー方針を維持し、バージョン番号またはハッシュで不一致を検出する

### 推奨案

生成処理と生成用データだけは共通ファイルを両プロジェクトからコンパイルする。それ以外を分離しても、生成結果の一致と軽量化は両立できる。

## ED-09 同期元と競合時の扱い

### 不足している情報

ゲーム側とEditor側の両方で同じコピー済みファイルを変更した場合、どちらを採用するか未指定。

### 決める内容

- ファイル種別ごとの正本
- 双方で変更されていた場合に同期を停止するか
- 差分確認なしで上書きしてよいか

---

# 第III部 Assetsと相対パス

## ED-10 「相対パス」と「exe位置基準」

### 一致しない点

単純な相対パスは、exeの保存場所ではなくプロセスのカレントディレクトリを基準にする。ショートカット、Visual Studio、コマンドラインなど起動方法によって基準が変わる。

そのため、次の2要件は追加ルールなしでは同時に保証できない。

- 参照文字列は`Assets/...`の相対パスにする
- exeを直接起動した時は必ずexe横のAssetsを使う

### 選択肢

1. 起動時にカレントディレクトリをexeのディレクトリへ変更する
2. exeディレクトリを取得し、内部で絶対パスへ解決する
3. 起動側が正しいカレントディレクトリを設定する前提にする

### 推奨案

exe直接起動時はexeディレクトリへカレントディレクトリを設定し、Visual Studioでは`$(ProjectDir)`をデバッグ作業ディレクトリに設定する。コード上の参照は`Assets/...`のまま維持できる。

## ED-11 Visual Studio実行とexe直接起動のAssets同期

### 現状

- Visual Studio実行時：プロジェクトディレクトリのAssetsを使う
- exe直接起動時：exeディレクトリのAssetsを使う

### 不足している情報

2か所のAssetsをいつ同期するか、どちらを正本とするか未指定。

### 決める内容

- プロジェクト側Assetsを正本にするか
- 出力側Assetsの手編集を許可するか
- Visual Studioビルド時に自動コピーするか
- 古い不要ファイルを出力側から削除するか

### 推奨案

各プロジェクトのAssetsを正本とし、出力側Assetsはビルド成果物としてコピーする。出力側を直接編集しない。

## ED-12 コピー時に除外するデータ

### 問題

Assets全体を上書きすると、ゲームセーブやEditor固有設定を消す可能性がある。

### 最低限決める対象

- `Assets/Save`をコピー対象から除外するか
- Editor設定ファイルを除外するか
- 生成ログを除外するか
- 一時ファイルを除外するか
- ゲーム側だけにあるマップを保持するか

### 推奨案

`Save`、ログ、一時ファイル、ユーザー設定を除外し、配布用静的Assetsだけをコピーする。

## ED-13 Editorからゲームへのマップ反映

### 不足している情報

「ゲーム側の`Assets/Maps`を上書きする」際の範囲と削除規則が未指定。

### 決める内容

- 選択中マップだけを反映するか
- `Assets/Maps`全体を反映するか
- 小ステージ素材も同時に反映するか
- Editor側で削除したファイルをゲーム側でも削除するか
- ゲームセーブ中の週固定マップとの互換性
- 反映前のバックアップを作るか

## ED-14 Editor固有出力の保存場所

### 不足している情報

Editorの設定・生成物をEditor側へ分ける方針は確定しているが、具体的な相対パスが未指定。

### 対象

- Editor UI設定
- 小ステージファイル
- 完成マップ
- 生成ログ
- 検証ログ
- 一時プレビュー
- 環境モデル登録簿

---

# 第IV部 ゲーム側のシーン分割

## ED-15 起動シーン

### 問題

正式Titleは今後作成するとされているが、ゲームexeの現在の起動先が未指定。

### 選択肢

1. 暫定Titleを作り、そこから自宅へ遷移する
2. Title完成までは自宅から直接開始する
3. 現在の旧`SceneTitle`を暫定利用する

### 推奨案

Title完成までは自宅Sceneから直接開始する。空の遷移画面だけを増やさずに検証できる。

## ED-16 自宅と店舗の分割単位

### 不足している情報

「自宅、商店、武具屋、レストランを別シーンへ分割」が次のどちらを意味するか未確定。

1. 自宅、商店、武具屋、レストランをそれぞれ独立Sceneにする
2. 1つの街Scene内で施設UI・内部モードを分ける

歩ける地上を今後実装するため、施設をSceneにするかUIにするかは地上設計にも影響する。

### 推奨案

歩ける地上を1つの街Sceneとし、施設は街Sceneから開くUIまたは施設モードにする。インゲームだけを奈落Sceneとして分離する。

## ED-17 Result Sceneへ移す範囲

### 不足している情報

現在の`SceneNarakuProto`には次が含まれる。

- 帰還確認
- 探窟放棄確認
- 帰還報酬計算
- 帰還リザルト
- 死亡処理
- 死亡リザルト
- レベル低下・保護消費
- 自宅への状態移送と保存

どの時点からResult Sceneへ責務を渡すか未指定。

### 決める内容

- 帰還・死亡の計算を奈落Scene内で確定してからResultへ渡すか
- Result Scene側で計算するか
- 放棄結果もResult Sceneを使うか
- Result画面を閉じた後の遷移先

### 推奨案

奈落Sceneで結果データと進行更新を確定し、Result Sceneは表示だけを担当する。画面遷移による二重計算を防げる。

## ED-18 シーン間の進行状態の所有者

### 問題

現在は`SceneNarakuProto`が所持金、レベル、装備、自宅在庫、持ち込み設定、探窟結果などを直接所有している。Sceneを破棄すると状態も失われるため、分離後の所有先が必要。

### 選択肢

1. ゲーム全体で生存する進行管理クラスを作る
2. Scene遷移ごとにセーブへ書き、次Sceneで読み直す
3. SceneManagerが全進行状態を所有する

### 推奨案

ゲーム全体で生存する進行管理クラスを1つ設け、セーブは永続化手段として使う。Scene遷移だけで毎回ファイルを往復しない。

## ED-19 Scene遷移中の探窟状態

### 問題

現在の`SceneNarakuProto`は終了時に、探窟中なら放棄ペナルティを適用する。Scene分割後に通常の画面遷移で破棄すると、意図せず放棄扱いになる可能性がある。

### 決める内容

- 奈落SceneからResult Sceneへ移る時の終了理由
- アプリ終了と通常Scene遷移の区別
- ロード失敗時の復帰先
- Scene切替中に自動保存するタイミング

## ED-20 Credit Scene

### 不足している情報

- 空Credit Sceneに戻るボタンを置くか
- 起動・遷移確認用の仮表示を置くか
- Release版へ含めるか
- 正式Titleがない期間に到達経路を設けるか

### 推奨案

黒背景と「Credit 未実装」、自宅へ戻るボタンだけを持つ仮Sceneにする。

---

# 第V部 DebugとEditor専用機能

## ED-21 ゲームDebug版へ残す機能

### 一致しない点

次の記述がある。

- Editor用ネイティブメニューとパフォーマンス計測をゲーム側から完全に外す
- Debugビルドでは出るようにする

「Debugで出す」の対象がゲーム調整ウィンドウだけか、Editor用機能も含むか確定していない。

### 対象ごとに決定が必要

|機能|ゲームDebug|ゲームRelease|Editor Debug|Editor Release|
|---|---|---|---|---|
|所持金・レベル調整|未確定|非表示|未確定|未確定|
|ゲームプレイ調整|未確定|非表示|未確定|未確定|
|Editorネイティブメニュー|未確定|除外|表示予定|未確定|
|Editorパフォーマンス計測|未確定|除外|表示予定|未確定|
|Editor Scene|未確定|除外|表示予定|未確定|

### 推奨案

- ゲームDebug：ゲーム調整ウィンドウだけ含める
- ゲームRelease：すべて除外する
- Editor Debug／Release：EditorメニューとEditor Sceneを含める
- 詳細パフォーマンス計測はEditor Debugだけ表示する

## ED-22 Releaseでの「非表示」と「コンパイル除外」

### 不足している情報

Release版でデバッグ機能を単に非表示にするか、ソース・文字列・処理ごとコンパイルから除外するか未指定。

### 推奨案

ゲームReleaseではプリプロセッサ条件によりコンパイル対象から除外する。軽量化という目的に合致する。

## ED-23 起動コードとネイティブメニュー

### 現状

現在の`Startup.cpp`はゲームウィンドウ作成とEditor用ネイティブメニュー作成を同時に行う。

### 決める内容

- ゲーム用とEditor用で`WinMain`を別ファイルにするか
- 共通起動コードをコピーして条件分岐するか
- Editorメニュー作成処理をEditor専用ファイルへ移すか

### 推奨案

ゲーム用とEditor用に別のエントリポイントを持たせ、Editorメニューのコードをゲームプロジェクトへ含めない。

---

# 第VI部 Editor生成プレビュー

## ED-24 「15段階生成」と選択項目の関係

### 一致しない可能性がある点

次が同時に指定されている。

- 15段階すべてを生成する
- 深度、上中下、エリア数を選択できる

「選択」が生成条件か、生成済み結果の表示・移動先か不明。

### 選択肢

1. 常に15段階生成し、深度・上中下・エリアは表示対象として選ぶ
2. 選択した段階だけ生成する
3. 15段階生成するが、各段階のエリア数を手動指定できる

### 推奨案

15段階を常に生成し、深度・上中下・エリア番号は表示対象と歩行開始先の選択に使う。エリア数は段階ごとに2～4の範囲で上書き可能にする。

## ED-25 エリア数指定

### 不足している情報

- 1段階ごとに個別指定するか
- 全15段階へ同じ数を適用するか
- `Random`を選べるようにするか
- 現行制限2～4以外も検証できるようにするか

### 推奨案

各段階に`Random / 2 / 3 / 4`を指定できるようにする。

## ED-26 シードの固定

### 不足している情報

「指定」「固定」「再生成」の正確な挙動が未指定。

### 決める内容

- 固定中に再生成を押すと同じ結果にするか
- 固定解除時は毎回新しいシードを生成するか
- Editor再起動後も固定シードを記憶するか
- シード入力の型と範囲
- 使用したシードを画面へ表示・コピーできるか

### 推奨案

- 64bit整数シード
- 固定中は再生成しても同じ結果
- 固定解除中は再生成ごとに新しいシード
- Editor終了時には保持しない
- 現在シードを表示してコピー可能にする

## ED-27 ゲーム本番データを書き換えない方法

### 問題

現在の生成処理は生成マップやログをファイルへ書く経路を持つ。Preview保存不要という要件に対し、どこまでファイル出力を禁止するか未指定。

### 選択肢

1. プレビューは完全にメモリ上だけで生成する
2. Editor専用Tempへ書き、終了時に削除する
3. Editor専用Previewディレクトリへ書くが手動保存機能は付けない

### 推奨案

完全なメモリ生成とする。ゲーム本番データを変更しないことを構造的に保証できる。

## ED-28 未保存の小ステージ編集内容

### 不足している情報

小ステージ編集中にPreviewを実行した場合、どのデータを生成へ使うか未指定。

### 選択肢

1. 保存済みファイルだけを使う
2. 現在開いている小ステージだけ未保存内容を使う
3. Preview前に保存を要求する

### 推奨案

Preview前に未保存変更があることを通知し、「現在内容でPreview」「保存してPreview」「キャンセル」を選べるようにする。

## ED-29 敵の停止範囲

### 不足している情報

「敵は生成するが動かさない」「プレイヤーは死なない」だけでは、次が確定しない。

- 索敵するか
- 向きを変えるか
- 攻撃演出を行うか
- 接触判定を持つか
- プレイヤーの攻撃を当てられるか
- HPを減らせるか

### 選択肢

1. 敵を完全な静止モデルとして表示する
2. 移動だけ止め、索敵と攻撃演出は行う
3. AIを通常動作させ、位置だけ固定する

### 推奨案

敵を完全停止し、当たり判定だけ表示確認用に残す。戦闘確認は通常ゲーム側で行う。

## ED-30 歩行プレビューの落下・水没復帰

### 問題

死亡を無効化するため、通常なら死亡する落下、場外、水没時の復帰方法が必要。

### 決める内容

- 最後に立っていた安全地点へ戻す
- 現在エリアの入口へ戻す
- 最初の開始地点へ戻す
- 手動リセット操作を用意する

### 推奨案

最後に立っていた安全地点へ自動復帰し、MenuBarにも「開始地点へ戻る」を用意する。

## ED-31 任意段階からの歩行確認

### 一致しない可能性がある点

歩行開始位置は「通常ゲームの開始地点と同じ」と確定している。一方で深度・上中下・エリアを選択できるため、深層を確認するたびに第一層から移動するのか不明。

### 選択肢

1. 常に第一層上層の開始地点から歩く
2. 選択中エリアの入口から歩行開始できる
3. 通常開始と選択エリア開始の両方を用意する

### 推奨案

両方を用意する。通常経路の接続確認と、個別エリアの短時間確認を両立できる。

## ED-32 俯瞰視点の初期位置と操作

### 不足している情報

- 現在エリアだけを見るか、15段階全体を見るか
- カメラ移動、回転、拡大縮小の操作
- 歩行から俯瞰へ戻った時の注視位置
- 層間移動後に自動で対象エリアへフォーカスするか

### 推奨案

現在選択エリアを中心に表示し、層間移動後は接続先エリアへ自動フォーカスする。

## ED-33 MenuBar項目と生成設定UI

### 不足している情報

`View`から切り替えることは確定しているが、項目名と生成設定の配置が未指定。

### 推奨案

```text
View
  Generated Stage Preview
  Overview Mode
  Walk Mode
  Generation Settings
  Return to Start
```

生成設定は独立した`Generation Settings`ウィンドウに置く。

## ED-34 プレビュー中の層間移動と状態保持

### 不足している情報

- 層間移動後に戻った時も同じ生成結果を保持するか
- 敵位置は常に初期位置か
- 開いた扉などの状態を保持するか
- Previewを再生成するまで15段階すべてを保持するか

### 推奨案

再生成するまで全15段階をメモリ上に保持し、往復しても生成結果を変えない。

## ED-35 プレビューの生成失敗

### 不足している情報

- 15段階の一部だけ失敗した場合に全体を破棄するか
- 自動再試行回数
- 失敗したシードを表示するか
- 失敗箇所へ移動できないようにするか

### 推奨案

自動再試行で別シードへ変えず、同じシードの失敗として停止する。段階、エリア、理由、シードを表示し、設定修正後にユーザーが再生成する。

---

# 第VII部 完了条件と検証

## ED-36 「分離完了」の具体的な判定

数値的な軽量化目標を設けないことは確定しているが、「分離がすべて遂行された」の検証項目を定義する必要がある。

### 推奨する完了条件

- ゲーム用とEditor用の両プロジェクトを個別にビルドできる。
- 両exeを個別に起動できる。
- ゲームReleaseへEditor Scene、Editorメニュー、Editor計測コードが含まれない。
- Editor exeから全Editorを起動できる。
- Editorで15段階Previewを生成できる。
- 俯瞰と歩行を切り替えられる。
- 歩行Previewで能力値消費、採掘、死亡が発生しない。
- Previewが本番マップ、ゲームセーブ、Editor原本を書き換えない。
- ゲームとEditorが同じシード・同じ入力データから同じ構造を生成する。
- Visual Studio起動とexe直接起動の両方で、意図したAssetsを読み込む。
- ゲームセーブがゲーム側`Assets/Save`だけへ作成される。
- Editor生成物がゲーム側へ自動混入しない。

## ED-37 分離後に必要なビルド検証

### 決める内容

- Debug x64だけ確認するか
- Release x64も必須にするか
- 既存プロジェクトもビルド成功を維持するか
- Win32を維持する場合はWin32も確認するか
- Assetsコピー結果を自動検査するか

### 推奨案

新ゲーム、新Editor、既存プロジェクトのDebug x64とRelease x64を必須とする。Win32を廃止する場合は対象外とする。

---

# 第VIII部 回答後に追加で判明した未確定事項

## ED-38 3プロジェクトの機能分担

### 一致しない点

最新回答では次の3プロジェクトへ完全分離すると指定されている。

1. ステージEditorだけを入れたプロジェクト
2. ゲーム用プロジェクト
3. 分離後に残った部分のプロジェクト

一方、以前は城、ボス、エフェクトなどの既存Editorも「新Editorへ残す」と指定されている。「ステージEditorだけ」と両立しないため、配置先を確定する必要がある。

### 確認する配置表

|機能|NarakuEditor|奈落塔|残存プロジェクト|
|---|---|---|---|
|`SceneNarakuEditor`|配置予定|なし|なし|
|`SceneNarakuPieceEditor`|配置予定|なし|なし|
|15段階生成Preview|配置予定|通常生成のみ|なし|
|城Editor|要確認|なし|要確認|
|ボスEditor|要確認|なし|要確認|
|エフェクトEditor|要確認|なし|要確認|
|旧ゲームScene|なし|要確認|要確認|
|旧デバッグScene|なし|なし|要確認|

### 推奨案

`NarakuEditor`には奈落のステージEditorと生成Previewだけを置き、城・ボス・エフェクト・旧ゲームSceneは残存プロジェクトへ置く。

## ED-39 残存プロジェクトの名称と起動先

### 不足している情報

- 残存プロジェクトの名前を`DX22_Project`のままにするか
- 残存exeのファイル名
- 起動時に表示するScene
- 今後も機能開発するか、参照・保守用として残すだけか
- ゲームやNarakuEditorから起動・遷移できるようにするか

### 推奨案

名称とexe名は`DX22_Project`のまま維持し、現在の旧機能を確認できるランチャーSceneから起動する。奈落塔とNarakuEditorからは遷移させない。

## ED-40 Map発行操作と同期単位

### 不足している情報

Editorからゲームへ`Assets/Maps`を反映する具体的な操作が未指定。

### 決める内容

- MenuBarに「ゲームへ発行」を追加するか
- 手動ファイルコピーだけにするか
- 発行先ゲームプロジェクトを固定するか選択可能にするか
- `Maps`全体を毎回同期するか、変更ファイルだけ同期するか
- Model依存をEditorが自動検出するか、利用者が手動追加するか
- 発行中に失敗した場合、更新前へ自動復旧するか

### 推奨案

MenuBarに「ゲームへ発行」を追加し、Maps全体と参照Modelを一時ディレクトリへ準備して検証後に置換する。途中失敗時はゲーム側を更新しない。

## ED-41 Map発行バックアップ

### 不足している情報

- バックアップの保存場所
- フォルダ名・時刻形式
- 保持世代数
- 自動削除の有無
- MapだけかModelも含めるか
- Editorから復元操作を用意するか

### 推奨案

ゲーム側`Assets/Backup/MapPublish/yyyyMMdd_HHmmss/`へ、置換対象のMapsとModelを保存する。最新5世代を保持し、Editorの発行画面から選択復元できるようにする。

## ED-42 Map更新時のゲーム進捗リセット

### 不足している情報

「更新した段階でゲームの進捗をリセットする」の範囲と実行条件が未指定。

### 決める内容

- Editorからゲームへ発行した時点でリセットするか
- 次回ゲーム起動時にMapバージョン不一致を検出してリセットするか
- 削除する対象がゲームセーブ全体か、週マップ・探索情報だけか
- 所持金、レベル、装備、遺物、クエストも失うか
- リセット前にセーブをバックアップするか
- ゲーム起動中に発行できないようにするか

### 推奨案

Map発行時にMapバージョンを更新し、ゲーム起動時に不一致を検出する。週生成マップと探索・クエスト配置だけを再生成し、恒久進行は維持する。全進捗削除を意図する場合は明示確認が必要。

## ED-43 コピー対象に含めるユーザー設定

### 問題

ED-12ではユーザー設定をコピー対象から除外しないと確定した。しかし、Editor設定をゲーム側へコピーすると不要な設定が混入し、既存ユーザー設定を上書きする可能性がある。

### 決める内容

- 「ユーザー設定」がどのファイルを指すか
- ゲーム設定とEditor設定を別ファイルにするか
- プロジェクト側から出力側への一方向コピーか
- 既存出力側の設定を毎回上書きするか
- 初回だけ既定設定をコピーするか

### 推奨案

既定設定だけを`Assets/Config/Defaults`からコピーし、実行後に生成される利用者設定は`Assets/Config/User`へ分けて上書き対象外にする。

## ED-44 既存セーブデータの移行

### 不足している情報

ゲームexeとAssetsの場所が変わるため、現在の`Assets/Save/naraku_proto_save.dat`を新しいゲーム側へ移すか未指定。

### 選択肢

1. 初回起動時に旧保存場所から自動移行する
2. 実装時に一度だけ手動コピーする
3. 分離後は新規ゲームとして扱う

### 推奨案

分離後の初回起動時だけ旧候補パスを確認し、見つかった場合は新保存先へコピーする。移行元は削除しない。

## ED-45 Runtime DLLと外部ライブラリ

### 確定仕様

- 外部サイトや別Gitから、新しいモデル読込依存を追加しない。
- 現在リポジトリに含まれるAssimpのヘッダー、lib、DLLを使用する。
- Visual Studio 2022／VC143用のファイルを使用する。
- Debug x64では`assimp-vc143-mtd.lib`と`assimp-vc143-mtd.dll`を使用する。
- Release x64では`assimp-vc143-mt.lib`と`assimp-vc143-mt.dll`を使用する。
- ヘッダーとlibは現在のリポジトリ内配置から参照する。
- 実行に必要なDLLだけを各exeの出力先へビルド後コピーする。
- VC141、VC142用DLLを新しい出力先へコピーしない。

現在の`Model.cpp`はVC143用Assimpを構成別にリンクしており、この方針でゲームとEditorのモデル読込に必要な機能を満たせる。

## ED-46 Solution構成と起動プロジェクト

### 不足している情報

- 3プロジェクトを同じ`.sln`へ入れるか
- 既定のスタートアッププロジェクト
- 複数スタートアップを設定するか
- Project Dependencyとビルド順
- `Build Solution`で3つすべてをビルドするか

### 推奨案

同じSolutionへ3プロジェクトを入れ、通常のスタートアップを「奈落塔」にする。共有生成コードに独立プロジェクトを作らない場合、3プロジェクト間に直接ビルド依存は設けない。

## ED-47 生成一致の自動検証

### 問題

ゲームとEditorが同じ生成処理をコンパイルしても、入力Assetsや設定が異なれば生成結果は一致しない。

### 決める内容

- 同一シード・同一入力から接続構造を比較するテストを作るか
- Map・Pieceデータのバージョン番号を持つか
- ゲームとEditorの生成設定不一致を起動時に警告するか

### 推奨案

同一シードから15段階のエリア数、接続ID、Piece配置IDを出力し、ゲームとEditorで一致する自動テストを追加する。

## ED-48 Scene切替保存の厳密な順序

### 不足している情報

ED-19では「最初に保存」「状態解放前に対象すべてを保存」と指定されているが、次の順序で確定してよいか確認が必要。

```text
1. 遷移要求を正規ルートか検証
2. 帰還・死亡・放棄などのゲーム結果を確定
3. Singletonへ一時状態を移送
4. 永続化対象をセーブ
5. 保存成功を確認
6. 奈落Sceneのリソースを解放
7. 次Sceneを生成
```

保存失敗時はScene遷移を中止し、現在Sceneと状態を維持する。

## ED-49 発行中・実行中のファイル競合

### 不足している情報

- ゲーム実行中にEditorからMapを発行できるか
- Editorを複数起動できるか
- 発行先ファイルが使用中の場合の扱い
- 同時発行を禁止するロックが必要か

### 推奨案

ゲーム実行中は発行を禁止し、Editor側で発行ロックを取得できない場合は何も上書きせず`実行中なので保存できません`と表示する。

---

# 第IX部 確定状況

## 1. 未確定事項

ED-01～ED-49について、ユーザー判断が必要な未確定事項は残っていない。

クラス名、同期スクリプト名、内部関数名、Visual Studio上のフィルター構成など、ゲームの外部動作へ影響しない名称は、実装時に既存規約へ合わせて決定する。

## 2. 実装前提となる最終構成

```text
DX22_Project.sln
  奈落塔                 ゲーム本体
  NarakuEditor           奈落ステージEditorと15段階生成Preview
  DX22_Project           城・ボス・エフェクト・旧機能

Output/
  Debug/
    Game/
    Editor/
    Legacy/
  Release/
    Game/
    Editor/
    Legacy/
```

- 対象プラットフォームはx64だけとする。
- 3プロジェクトすべてでDebugとReleaseを用意する。
- Solutionの既定スタートアップは「奈落塔」とする。
- ゲームReleaseにはEditor機能とデバッグ機能を含めない。
- EditorのMap発行は検証、バックアップ、置換の順で行う。
- ゲーム実行中はMap発行を行わない。
- Scene切替保存に失敗した場合は遷移しない。

## 3. ED-45確認結果

現在のモデル読込形態で不足はない。

- 既存の`Model.cpp`がAssimpを使用している。
- VC143用のDebug／Release libとDLLがリポジトリ内に存在する。
- 新しいゲームとEditorの出力先へ、構成に合ったDLLをコピーすればよい。
- 外部サイト、別Git、新しいパッケージ管理機構は不要である。

## 4. 実装時の検証対象

- 3プロジェクトのDebug x64ビルド
- 3プロジェクトのRelease x64ビルド
- 各exeの単独起動
- 各exeが意図した相対Assetsを参照すること
- ゲーム側だけがゲームセーブを作成すること
- Map発行のバックアップ、置換、削除同期、進捗部分リセット
- ゲーム実行中の発行拒否と`実行中なので保存できません`表示
- 保存失敗時にScene遷移しないこと
- 同一シードに対するゲームとEditorの生成結果一致
- ゲームReleaseへEditor・Debugコードが含まれないこと
