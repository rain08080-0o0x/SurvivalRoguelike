# 操作概要

## ビルド

リポジトリルート `C:\HAL\個人制作\NarakuProto` で次を実行します。

```powershell
powershell -ExecutionPolicy Bypass -File .\build_debug_x64.ps1
```

出力先は `DX22_Project\x64\Debug\DX22_Project.exe` です。テクスチャなどの`Assets/...`を解決するため、直接実行する場合の作業ディレクトリは`DX22_Project`にします。Visual Studioから実行する場合は通常のプロジェクト作業ディレクトリを使用します。

## 動作確認

1. ゲームシーンへ入り、移動、採掘、戦闘、帰還、ロープ、階層遷移を一通り操作します。
2. 床の欠損セル、上層透明度、階層遷移フェード、ロープ穴、階層ゲートの表示が従来どおりか確認します。
3. インゲーム中に地形グリッドやキャラクター周囲などの線が表示されないことを確認します。
4. `C` キーで当たり判定デバッグを切り替え、範囲の球形表示は切り替わる一方、線は出ないことを確認します。
5. エリア移動後も床が欠けず、色と透明度が更新されることを確認します。
6. 生存中の敵が左上の緑スライムで表示され、カメラを回しても常に正面を向くことを確認します。
7. 敵撃破時は従来どおり敵が消え、食料を落とすことを確認します。

## 敵スプライト

使用画像は`DX22_Project/Assets/Texture/Character/Enemy/enemy.png`です。

- 画像全体: 648×1152
- 分割: 9列×16行
- 1セル: 72×72
- 使用キャラクター: 左上の緑スライム
- 使用フレーム: 左上3×4ブロックの中央列・先頭行
- 表示サイズ: ワールド上で1.2×1.2
- 描画方式: 全生存敵を1つの動的ビルボードバッチで1ドロー

### 今後のタスク: 敵スプライトアニメーション

今回の実装は静止画です。将来、同じ9×16スプライトシートのUVを敵状態と経過時間に応じて変更します。

優先する状態は次のとおりです。

1. 攻撃予備動作
2. 体当たり攻撃
3. 移動
4. 被弾
5. 待機
6. 死亡

アニメーションを追加する際も、敵ごとに`Sprite::Draw()`を呼ばず、現在の`EnemyBillboardVertex`へフレームUVを設定して1バッチを維持します。ゲーム判定のタイミングは既存の`telegraphTimer`や`chargeTimer`を正とし、アニメーションフレームから攻撃判定を発生させないでください。

## パフォーマンス確認

`F2` でパフォーマンスオーバーレイを表示できます。同じマップ、同じカメラ位置、同じ解像度で、最初の数秒を除外して比較します。

重点的に見る値は次のとおりです。

- FPSとフレーム時間
- `Scene Draw`: シーン側の描画命令とCPU描画準備の変化
- `Present`: VSync待ちや表示同期待ちの影響
- `Update`: ゲームロジック側に回帰がないか

`Present` が大きく、FPSがモニターのリフレッシュレート付近で固定される場合はVSyncの影響が強いため、今回の描画最適化だけをFPS値から評価しないでください。Debug構成は最適化されていないため、最終性能は同条件のRelease構成でも測定してください。

## 地形構成を変更した場合

通常のエリア生成・切り替えでは自動で `RebuildTerrainFloorBatch()` が呼ばれるため、追加操作は不要です。

新しく「床として描く四角形の種類」を追加する場合は、次の2点をセットで変更します。

1. `RebuildTerrainFloorBatch()` の `quadCapacity` に最大枚数を加える。
2. `Draw3DField()` で `AppendTerrainFloorQuad()` を呼ぶ。

容量計算を追加し忘れると、配列上限を超えた四角形は安全のため追加されず、表示が欠けます。ゲームロジックには影響しませんが、必ず最大構成のマップで表示確認してください。

## 線を再導入する必要が生じた場合

インゲームへ恒常的な線描画を戻すと、今回除去したCPU頂点生成、2.8 MBの線バッファ転送、1ドローが再発します。開発時だけ必要なら、リリース時に必ず無効になるデバッグ限定経路とし、セル4辺を毎フレーム重複生成しない方式にしてください。

地形境界を表示する必要がある場合は、マップ構築時に重複辺を除いて静的な線メッシュを1回作成し、地形変更時だけ再構築する方法が適しています。

## ドローコール増加を調べる手順

1. `Draw3DField()` などのフレーム描画関数内で、セル・オブジェクトのループ中に `Draw()` が呼ばれていないか検索します。
2. 同じメッシュ、シェーダー、ブレンド、テクスチャで描けるものは、頂点結合、インスタンシング、テクスチャ配列の順で一括化を検討します。
3. 表示対象判定とゲーム更新判定を混ぜず、描画経路だけを変更します。
4. 変更前後で同じ操作を行い、表示、当たり判定、保存データ、FPSオーバーレイを比較します。
5. 理論値は `Q回 → 1回` の式で記録し、実測値とは分けて残します。

## 地面テクスチャを追加する場合

マップごとに地面種類が変わってもドローコールを増やさない推奨構成は `Texture2DArray` です。

- 全地面画像の幅、高さ、ピクセル形式、ミップ数を統一する。
- 画像を1つのテクスチャ配列へ格納する。
- `groundTextureId` を配列スライス番号へ対応付ける。
- `TerrainFloorVertex` にUVと配列番号を追加する。
- ピクセルシェーダーで `Texture2DArray.Sample()` を1回行う。
- 1バッチ、1マテリアルのまま異なる地面を描く。

画像仕様、UV、サンプラー、既存IDとの対応が確定していないため、今回この機能は実装していません。
