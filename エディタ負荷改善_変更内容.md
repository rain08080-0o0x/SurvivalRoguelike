# エディタ負荷改善の変更内容

## 1. 対応の背景

エディタの動作が重い原因を調査したところ、計測上は主に次の処理時間が大きくなっていました。

- 小ステージHierarchyの描画
- 奈落塔ピースエディタ全体の更新・描画

この結果を起点として、Hierarchyやピースエディタの表示量だけでなく、その内側で毎フレーム実行されている処理と、処理時間を測るプロファイラ自身の負荷を調査しました。

実装へ入る前の作業状態は、次のコミットとして保存しています。

- コミット: `23a0788`
- コミットメッセージ: `ピースエディタと計測機能の現状を保存`

このコミットを変更前の比較・復元基準として使用できます。

## 2. 調査で判明した根本原因

### 2.1 細粒度プロファイラの計測負荷

ピースエディタ関連では、`EDITOR_PROFILE_FUNCTION()`が204か所に配置されていました。

座標取得、範囲判定、セルインデックス計算、選択判定、文字列変換など、本来は非常に短時間で終わる関数にも計測が入っていました。各計測スコープでは、主に次の処理が発生します。

- 高分解能時計の取得
- 関数名からのクラス名抽出
- `std::string`の生成
- `unordered_map`の検索と更新
- 関数別・クラス別の呼出回数と時間の集計

これらが全頂点・全セル走査の内側から大量に呼ばれることで、本来の編集処理よりプロファイラ管理処理が大きくなる状態でした。

また、F3でプロファイラウィンドウを閉じても、変更前は表示だけが消え、計測処理自体は継続していました。そのため、通常の編集時にも継続的な負荷が発生していました。

Hierarchyウィンドウの計測時間には、その内側で呼ばれた細粒度プロファイラの管理時間も含まれます。このため、Hierarchy自体の処理量以上に「Hierarchyが重い」という結果が強調されていました。

### 2.2 Hierarchy表示内容の毎フレーム再構築

小ステージHierarchyでは、表示のたびに次の処理を実行していました。

- 現在編集中のファイルパスを正規化して小文字化
- 登録エントリから表示順ポインタ配列を生成
- 登録エントリをソート
- 日付、完成・下書き状態、ファイル名を連結
- UTF-16文字列をUTF-8へ変換
- ツールチップ用パスをUTF-8へ変換
- 全登録項目に対するImGui項目生成

現在の`piece_hierarchy.cfg`は18件ですが、登録内容や並び順が変わっていないフレームでも同じ処理を繰り返していました。

### 2.3 3Dプレビューの無条件描画

`SceneNarakuPieceEditor::Draw()`は、3Dプレビューウィンドウの表示状態を確認する前に`RenderTerrainPreviewToTexture()`を呼んでいました。

このため、3Dプレビューを閉じても次の処理が継続していました。

- プレビュー用レンダーターゲットの設定とクリア
- 全頂点・全セルの走査
- 地形グリッドと各種マーカーのライン生成
- 環境オブジェクトの3D描画
- ライン頂点バッファの転送と描画

表示されない結果を毎フレーム生成していたことが、ピースエディタ全体の負荷要因になっていました。

### 2.4 ピッキング時の反復計算

頂点・セルのピッキングでは、候補ごとにワールド座標をスクリーン座標へ投影していました。

変更前の`ProjectWorldToScreen()`は、呼ばれるたびにビュー行列と射影行列をロードして乗算していました。セル中心の取得では4頂点分の座標取得も行うため、マウスがプレビュー内にある間は同じ種類の計算が多数繰り返されていました。

最寄り候補の比較にも、各候補で平方根を計算していました。

### 2.5 ネイティブメニューの毎フレーム更新

メインループから`UpdateNativeEditorMenu()`が毎フレーム呼ばれていました。

ピースエディタでは、状態が変化していない場合も次のWindows API処理を繰り返していました。

- 10項目分の`CheckMenuItem()`
- 編集状態ラベルに対する`ModifyMenuW()`
- `DrawMenuBar()`によるメニューバー再描画

これはピースエディタのUpdate・Draw計測の外側で発生するため、ピースエディタ内部の関数別結果だけでは見落としやすい負荷でした。

## 3. 実装した変更

### 3.1 プロファイラを必要時のみ動作させる変更

対象ファイル:

- `DX22_Project/EditorPerformanceProfiler.h`
- `DX22_Project/EditorPerformanceProfiler.cpp`

変更内容:

- プロファイラウィンドウの初期状態を非表示へ変更
- 計測が有効か判定する`IsRecordingEnabled()`を追加
- 非表示中は`BeginFrame()`の集計処理を実行しないよう変更
- 非表示中の`EditorPerformanceScope`では時計取得、クラス名抽出、マップ更新を実行しないよう変更
- F3で再表示したときは、以前の古い統計を残さず集計状態を初期化
- 高頻度で呼ばれる単純な座標・範囲・選択判定関数から細粒度計測を除外

変更理由:

プロファイラは調査時だけ必要であり、通常編集時の処理経路へ常時大きな負荷を加えるべきではないためです。また、非常に小さい関数を個別計測すると、計測値より計測処理そのものの割合が大きくなり、結果の信頼性も下がります。

プロファイラは引き続きF3で表示できます。表示中のみ計測を実行します。

### 3.2 小ステージHierarchyの表示キャッシュ

対象ファイル:

- `DX22_Project/SceneNarakuPieceEditor.h`
- `DX22_Project/SceneNarakuPieceEditorDocument.cpp`

変更内容:

- ソート済み順序、正規化済みパス、表示ラベル、ツールチップを保持する表示キャッシュを追加
- 次の場合だけキャッシュを再構築するよう変更
  - Hierarchy設定の再読込
  - 登録項目の追加・更新
  - 登録項目の削除
  - ソート種別の変更
  - 昇順・降順の変更
- 表示範囲内の項目だけImGuiへ渡す`ImGuiListClipper`を追加

変更理由:

Hierarchyの内容が変わらないフレームでは、ソート、文字列連結、UTF変換をやり直す必要がないためです。登録件数が今後増えた場合も、画面外項目のUI生成を抑えられます。

保存形式と`piece_hierarchy.cfg`の内容は変更していません。

### 3.3 非表示プレビューの描画停止

対象ファイル:

- `DX22_Project/SceneNarakuPieceEditor.cpp`

変更内容:

- `m_showPreviewWindow`が`true`の場合だけ`RenderTerrainPreviewToTexture()`を呼ぶよう変更

変更理由:

プレビューが閉じている状態ではレンダーターゲットへ描画しても画面へ使用されないためです。これにより、プレビュー非表示時は全頂点・全セル走査、ライン生成、環境モデル描画をまとめて省略できます。

### 3.4 ピッキング計算の削減

対象ファイル:

- `DX22_Project/SceneNarakuPieceEditor.h`
- `DX22_Project/SceneNarakuPieceEditorPreview.cpp`
- `DX22_Project/SceneNarakuPieceEditorEditing.cpp`

変更内容:

- カメラ行列更新時にビュー射影行列を計算し、`m_viewProjectionMatrix`へ保存
- 頂点・セル投影時は保存済みビュー射影行列を使用
- 最寄り候補判定を距離から距離二乗の比較へ変更し、候補ごとの`sqrt()`を削除
- 投影処理から細粒度プロファイラを除外

変更理由:

候補数に比例して同じ行列乗算と平方根計算を繰り返していたためです。判定結果を変えずに、候補1件あたりの処理を削減しています。

ピッキング距離のしきい値と選択仕様は変更していません。

### 3.5 ネイティブメニュー更新の差分化

対象ファイル:

- `DX22_Project/SceneNarakuPieceEditor.h`
- `DX22_Project/SceneNarakuPieceEditor.cpp`

変更内容:

- 各ツールウィンドウの表示状態をビットマスクとして保存
- 前回反映した編集状態ラベルを保存
- 表示状態が変わった場合だけ`CheckMenuItem()`を実行
- 編集状態ラベルが変わった場合だけ`ModifyMenuW()`を実行
- いずれかが変わった場合だけ`DrawMenuBar()`を実行

変更理由:

メニュー状態が同一のフレームでは、Windows APIを呼び直す必要がないためです。メインループ側の呼出し構造を変更せず、ピースエディタ側で不要なOS更新だけを除外しています。

### 3.6 Release x64のUTF-8ビルド設定

対象ファイル:

- `DX22_Project/DX22_Project.vcxproj`

変更内容:

- Release x64のコンパイラオプションへ`/utf-8`を追加

変更理由:

Debug x64には`/utf-8`が設定されていましたが、Release x64には設定されていませんでした。そのため、UTF-8で保存されたプロファイラ関連ソースをReleaseビルドすると、CP932として誤解釈され構文エラーになっていました。

DebugとReleaseで同じソースを同じ文字コードとして解釈させるため、Release x64にも同じ設定を適用しました。

## 4. 変更していない仕様

今回の変更では、次の外部仕様を変更していません。

- ピースJSONの保存形式
- `piece_hierarchy.cfg`の保存形式
- Hierarchyの登録、削除、並び替え、クリック読込の操作
- 頂点・セルの選択距離
- 地形編集、配置ツール、Undo・Redoの操作
- 環境モデルのデータ形式
- ゲーム本編のランタイム処理

## 5. 実施した検証

### 5.1 差分検証

実行コマンド:

```powershell
git diff --check
```

結果:

- 差分エラーなし
- GitのLFからCRLFへの変換予告のみ出力

### 5.2 Debug x64ビルド

実行コマンド:

```powershell
.\build_debug_x64.ps1
```

結果:

- ビルド成功
- エラー0件
- 既存の`Defines.h`文字コード問題を中心に2996件の警告

### 5.3 Release x64ビルド

初回ビルドでは、Release x64に`/utf-8`がなく、`EditorPerformanceProfiler.cpp`がCP932として解釈されて失敗しました。

`DX22_Project.vcxproj`へ`/utf-8`を追加した後、次のコマンドを再実行しました。

```powershell
& 'C:\Program Files\Microsoft Visual Studio\2022\Enterprise\MSBuild\Current\Bin\MSBuild.exe' `
    DX22_Project\DX22_Project.vcxproj `
    /t:Build /p:Configuration=Release /p:Platform=x64 /m /nologo /verbosity:minimal
```

結果:

- ビルド成功
- `DX22_Project/x64/Release/DX22_Project.exe`を生成

## 6. 未検証事項

次の項目は、コード確認とビルドだけでは判断できないため未検証です。

- 実機上での変更前後のCPUフレーム時間
- FPSの改善量
- 16x16と32x32ピースの負荷差
- Hierarchy表示ON/OFF時の実測差
- プレビュー表示ON/OFF時の実測差
- F3でプロファイラを有効にした場合の計測オーバーヘッド
- Hierarchyの再読込、並び替え、クリック読込の目視操作確認
- 頂点・セル・配置オブジェクトのピッキング操作確認

FPSはVSyncや`Present`待ちの影響を受けるため、実測ではFPSだけでなくCPU側のUpdate、Draw、Presentを分けて比較する必要があります。

## 7. 残っている注意点

### 7.1 プロファイラ表示中の負荷

プロファイラをF3で開いている間は、関数名集計と時間計測が有効になります。通常編集時の負荷は停止しましたが、プロファイラ表示中の計測オーバーヘッドが完全になくなるわけではありません。

高頻度関数から計測を除外したため変更前より影響は小さくなっていますが、最終的な性能評価ではプロファイラOFF時の外部フレーム計測も併用してください。

### 7.2 既存の文字コード警告

Debug・Releaseともに、`Defines.h`など既存ファイルの文字コードに由来する大量の警告が残っています。今回のエディタ負荷改善とは別問題のため、警告対象ファイルの内容は変更していません。

### 7.3 実装変更のGit状態

実装前の状態はコミット`23a0788`として保存済みです。

この文書作成時点では、エディタ負荷改善のコード変更はそのコミット以降の作業ツリー差分として残しています。変更前後を比較する場合は、`23a0788`を基準にしてください。
