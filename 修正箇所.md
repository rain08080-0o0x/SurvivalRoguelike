# 修正箇所

## 目的

`SceneNarakuProto` のインゲーム描画について、ゲーム進行、当たり判定、マップ生成、保存形式を変えずに CPU 側の描画命令数を減らしました。また、要件どおりインゲーム中の線描画を停止しました。

## 変更したコード

### `DX22_Project/SceneNarakuProto.h`

- 地形床専用の頂点形式 `TerrainFloorVertex` を追加しました。
  - 位置: `float3`
  - 色: `float4`
  - 1頂点あたり28バイト
- 地形床バッチ用の `MeshBuffer`、`VertexShader`、`PixelShader` と、再利用する CPU 頂点配列を追加しました。
- 初期化、解放、容量再構築、四角形追加、一括描画の各メソッドを追加しました。
- セルごとに `Sprite::Draw()` を行っていた `DrawTransparentFloor3D` の宣言と、白1×1テクスチャのメンバーを削除しました。
- 敵ビルボード専用の `EnemyBillboardVertex`、動的メッシュ、シェーダー、敵テクスチャ、再利用するCPU頂点配列を追加しました。
- 敵ビルボードの初期化、解放、容量再構築、一括描画メソッドを追加しました。

### `DX22_Project/SceneNarakuProto.cpp`

#### 地形床の一括描画

- `InitializeTerrainFloorBatch()` で、位置と頂点色だけを扱う床専用シェーダーを作成します。
- `RebuildTerrainFloorBatch()` で、現在の地形レイヤー、ロープ穴、階層ゲートを収容できる動的頂点バッファを作成します。
- `AppendTerrainFloorQuad()` で、描画対象の床1枚を6頂点の三角形リストとして CPU 配列へ追加します。
- `DrawTerrainFloorBatch()` で、ビュー・プロジェクション行列を1回設定し、頂点バッファを1回更新して全床を1回で描画します。
- `ResetRun()`、`ActivateArea()`、`BuildCurrentAreaRuntime()` の各マップ構成確定後にバッファ容量を再構築します。これによりエリアや地形が変わっても必要容量が追従します。

#### `Draw3DField()`

- 削除セル、有効頂点、プレイヤー遮蔽の判定は変更していません。
- `groundTextureId` による既存の色と透明度、上層の透明度、階層遷移中のフェードを維持しています。
- 床セル、ロープ穴、階層ゲートの板を1つの頂点配列へ集約し、最後に1回だけ描画するよう変更しました。
- 既存と同じアルファブレンド、両面描画、深度設定を使います。
- 地形グリッド、帰還地点、ロープ、採掘地点、遺物、食料、敵、プレイヤー、ピン、攻撃範囲、当たり判定範囲に対する `Geometory::AddLine()` と、最後の `Geometory::DrawLines()` を削除しました。
- 箱、球、攻撃ヒットエフェクトは残しており、線以外の既存表示には手を加えていません。

#### 敵の静止ビルボード表示

- `Assets/Texture/Character/Enemy/enemy.png`を読み込みます。
- 画像は648×1152、9列×16行で、1セルは72×72です。
- 左上にある緑スライムの3×4ブロックから、停止姿勢に相当する中央列・先頭行を表示します。
- 生存中の全敵について、カメラへ正対する四角形を同じ動的頂点バッファへ追加し、1回で描画します。
- PNGのアルファを使用し、透明ピクセルはピクセルシェーダーで破棄して透明領域が深度を書き込まないようにしました。
- ピクセルアートがぼやけないよう、敵描画中だけポイントサンプリングへ切り替え、直後に既存の線形サンプリングへ戻します。
- 旧デバッグ球による敵本体表示を削除しました。

## 変更していない処理

- プレイヤー、敵、採掘、食料、遺物、帰還、ロープ、階層遷移の更新処理
- 当たり判定と移動可能判定
- マップ生成アルゴリズム、ピースデータ、JSON形式
- `groundTextureId` の保存値と色の対応
- `MeshBuffer`、`Shader`、`Sprite`、`Geometory` の共通API
- 他シーンの描画
- 敵AI、移動、予備動作、体当たり、被弾、HP、死亡、ドロップ処理

## 意図した表示上の変更

インゲーム中の線は表示されません。これは要件に基づく変更です。`C` キーで当たり判定デバッグを有効にした場合も、球形の範囲表示は残りますが線は表示されません。

敵本体は灰色のデバッグ球ではなく、左上の緑スライムの静止ビルボードで表示されます。待機、移動、攻撃、被弾、死亡のスプライトアニメーションは今後のタスクです。

## 検証結果

- 実行: `powershell -ExecutionPolicy Bypass -File .\build_debug_x64.ps1`
- 構成: `Debug | x64`
- 結果: 成功、エラー0件
- 初回の全コンパイル: 警告1376件。今回の変更箇所以外を含む既存警告で、主に型変換、文字コード、プリプロセッサ関連です。
- 最終の差分ビルド: 警告0件、エラー0件
- 静的確認: `SceneNarakuProto` 内に `Geometory::AddLine()`、`Geometory::DrawLines()`、旧 `DrawTransparentFloor3D()`、旧白テクスチャメンバーが残っていないことを確認しました。

実機上のFPS、GPU時間、表示一致は未計測です。計算上の削減量と実測方法は `最適化.md` と `操作概要.md` に記載しています。
