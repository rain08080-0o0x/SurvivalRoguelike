# 奈落塔 開発記録帳

## このファイルの扱い

このファイルは、奈落塔プロトタイプで実施した作業を日付別に記録するためのログです。
今後、仕様変更、実装、修正、調査、ビルド確認を行った場合は、このファイルへ追記します。

更新時の詳細ルールは [ドキュメント更新ルール.md](ドキュメント更新ルール.md) を参照します。

## 2026-05-12

### プロジェクト作成

- `C:\HAL\個人制作\NarakuProto` にプロトタイプ用プロジェクトを作成。
- 既存プロジェクト `C:\HAL\4-2after\DX22\Program\DX22_Project` から、DirectX、ImGui、Input、Model、Assimp、DirectXTex周辺を含む構成を拝借。
- `build_debug_x64.ps1` による Debug x64 ビルドを確認。

### プロトタイプシーン追加

- `SceneNarakuProto.h` を追加。
- `SceneNarakuProto.cpp` を追加。
- `SceneManager` に `SCENE_NARAKU_PROTO` を追加。
- 起動時に奈落塔プロトタイプシーンへ入るように調整。

### 基本ループ実装

- プレイヤー移動、走り、ステップ、ジャンプを実装。
- スタミナを実装。
- 重量システムを実装。
- 上昇負荷のプロトタイプ用ルールを実装。
- 採掘ポイントを10か所配置。
- 4級旧器の取得、所持、破棄、売却を実装。
- 帰還、売却、再潜行を実装。
- 死亡リザルトを実装。
- 地図ピンの設置/削除を実装。

### UI

- ImGuiの文面を日本語化。
- HUD、所持品、旧器発見確認、帰還/死亡リザルト、地図ピン用UIを実装。

### 入力調整

- Shift短押しでステップ、Shift長押しで走りに分岐する処理を調整。
- Shiftの判定を左右Shiftの物理キーで見るように調整。
- 走りが反応しない問題を修正。
- ステップが反応しない問題を修正。
- 単押し判定のしきい値を確認し、0.18秒基準で運用。

### 3Dデバッグ描画

- 既存の `Geometory` を使って、プレイヤー、敵、採掘ポイント、ロープ、旧器、帰還地点、ピンを3D表示。
- 斜め見下ろしカメラを追加。
- 3D表示時に箱/球のワールド行列が崩れる問題を修正。
- `Main.cpp` の共通デバッググリッドが奈落プロトと重なる問題を修正。
- カメラ基準移動を導入。
- 左右移動が反転していた問題を修正。

## 2026-05-13

### ロープ操作修正

- ロープ使用中にスタミナが0になると、昇降できず操作不能に近い状態になる問題を確認。
- ロープ使用中に `A` / `D` を押すと、スタミナがなくてもロープから離脱できるように修正。
- ロープから離脱した時、少し横へずらして再度ロープ判定に吸われにくくした。
- ロープに掴まって停止しているだけなら、スタミナが回復するように修正。
- HUDの操作説明に `ロープ中A/D 離脱` を追加。
- Debug x64 ビルド成功を確認。

### ドキュメント整備

- `.sln` と同じディレクトリに企画草案、タスクリスト、開発記録帳、ドキュメント更新ルールを作成。
- 今後の変更時に、関連ドキュメントへ追記する方針を明文化。

### 半透明床・仮地形

- 既存の `Sprite` を使い、1x1白テクスチャにRGBA色を掛けて半透明床を描画する方式を追加。
- 地表全体を薄い半透明床として表示。
- ロープ周辺に下層床を仮配置。
- 崖穴の目印として暗い半透明面を追加。
- 画像素材なしで動く構成にしたため、必要になったら画像テクスチャへ差し替え可能。
- Debug x64 ビルド成功を確認。

### 地形判定・一段下の床

- `FloorRegion` を追加し、地上床と一段下の床を移動判定用データとして持つように変更。
- 通常移動、ステップ、ノックバックが現在深度の床外へ出ないように制限。
- `RopePoint` を追加し、ロープに上端深度と下端深度を持たせた。
- ロープ昇降中の深度を上端から下端の範囲に制限。
- ロープから横へ離れる時は、現在深度に床がある場合だけ離脱できるように変更。
- 半透明床の描画を、実際の床判定データから生成する方式に変更。
- Debug x64 ビルド成功を確認。

## 2026-06-29
- 小ステージ方式へ移行するため、`NarakuPieceData.h/.cpp` と `SceneNarakuPieceEditor.h/.cpp` を追加した。
- 小ステージJSONの Drafts / Completed 保存フォルダを追加した。
- 小ステージエディタSceneを追加した。
- 16x16 小ステージの地形編集、頂点/セル選択、複数選択、高さ編集、セル属性編集、削除/復元、採掘ポイント/ロープ/開始帰還候補の配置編集を進めた。
- Fileメニューから Save / Load を行う形にした。
- Save は小ウィンドウ、Load は Windows 標準ダイアログで操作する形にした。
- Viewメニューで各ウィンドウの表示 / 非表示を切り替えられるようにした。
- 保存済み小ステージを `小ステージHierarchy` に一覧表示し、クリックで読み込めるようにした。
- Hierarchy登録情報を `Assets/Naraku/Pieces/piece_hierarchy.cfg` に保存するようにした。
- 編集中小ステージ名をメインウィンドウタイトルへ表示するようにした。
- メニューバーと ImGui ウィンドウが重ならないよう、WorkPos 基準の初回配置と上端補正を追加した。
- Debug x64 ビルド成功を確認した。
- 注意点として、`SceneNarakuPieceEditor.*` や `NarakuPieceData.*` は新規追加ファイルのため、コミット対象に含める必要がある。
- 小ステージエディタのメニューバーと ImGui ウィンドウがまだ重なる問題を再修正した。
- WorkPos 単独ではなく、実測したメニューバー高さ m_mainMenuBarHeight と GetMenuSafeTopY() を使って安全上端Yを求めるようにした。
- ApplySafeWindowPlacement() と EnsureWindowBelowMainMenuBar() を同じ安全上端Y基準に統一した。
- Debug x64 ビルド成功を確認した。
- メニューバー被り対策として、ウィンドウ上端の安全余白 m_menuBarWindowPaddingY を追加し、28px確保するようにした。
- GetMenuSafeTopY() を、メニューバー高さと追加安全余白を必ず考慮する計算に変更した。
- Debug x64 ビルド成功を確認した。

## 2026-06-30
- 小ステージエディタのメニューを、元の奈落塔地形エディタと同じ Win32 ネイティブメニュー方式へ変更した。
- File / View をメインウィンドウ上部のネイティブメニューへ移し、ImGui メニューバー依存をなくした。
- 小ステージエディタのメニューバーと ImGui ウィンドウ被りを構造的に解消した。
- Save は次描画で安全に保存ポップアップを開く方式へ整理し、Load は既存の Windows 標準ダイアログ呼び出しを維持した。
- Debug x64 ビルド成功を確認した。
- 別スレッド / 別担当への作業継承用に `引き継ぎメモ.md` を追加した。

## 2026-07-12
- 今回の3件は、3x3接続と探索導線の検証に直結する要望として、いずれもプロト版へ分類した。
- 対象は、外周セル歩行可能化と開始・帰還地点到達の検証、地面当たり判定の3Dデバッグ表示、Tキー地図での採掘済み地点記録と拡縮対応。
- 実装は別担当が進行中であり、本記録では分類追記のみを行った。

## 2026-07-13

### 3x3マップ外周セル歩行可能化と開始・帰還地点到達バグの修正
- `NarakuStageGenerator.cpp` の `ApplyBoundaryClosure` において、3x3マップ外周セルの `blocked` フラグを解除した際、強制的に `cliffEdge` フラグを付与していた処理を削除。
- これにより、スロープなどの緩やかな高低差で崖境界制限（`0.20f` 制限）に引っかかる問題が解消され、外周に設置された開始地点や帰還地点にプレイヤーが正常にアクセス可能になった。

### 当たり判定コライダーのデバッグ表示機能の強化
- `'C'` キーを押すことで、3D空間上のコライダーデバッグ表示（`m_showCollisionDebug`）をリアルタイムでON/OFFトグルする機能を追加。
- コライダーデバッグ表示が有効な間、各セルの通行不可（Blocked：赤）、崖境界（CliffEdge：黄）、危険地形（Hazard：紫）、削除セル（Removed：ピンク）の境界線をワイヤーフレームとして3D描画するように拡張。
- 帰還地点（半径 `1.4f`）および未採掘の採掘ポイント（半径 `1.0f`）の周囲に、インタラクト可能な当たり判定範囲を示すデバッグ球体を描画し、判定の当たりやすさを視覚的に分かりやすくした。

### 採掘済み採掘ポイントのメモリ上での永続化
- `SceneNarakuProto` 内に、プレイセッション中に採掘されたポイントを保持するための `m_permanentlyMinedRelics` メンバー（旧器名リスト）を追加。
- 死亡や生還によってシーンがリスタート（`ResetRun()`）した際にも、一度採掘したポイントが最初から採掘済み（グレー表示）かつ発見済みとしてマップデータ上に保持され続けるように修正。

### Tキーマップでの地形セル2D描画および拡縮連動の強化
- `DrawMapControls`（Tキーマップウィンドウ）を完全に書き換え、`m_mapZoom` による拡縮とマウスホイール操作、およびプレイヤー位置（`mapFocus`）への追従処理を正しく実装。
- マップウィンドウ内に、プレイヤーの現在深度に近い地形レイヤー of グリッドを2D矩形として描画する処理を追加。
- 通行可能エリア、通行不可エリア、崖境界、危険地形を異なる色で塗り分け、ImGui の `PushClipRect` / `PopClipRect` を用いてウィンドウ外へのはみ出しをクリップ。これにより、2Dマップとして実用的な機能へと拡張された。
- 開発 Debug x64 ビルドの成功を確認。
- `ScreenToWorld` および `WorldToCanvas` の定義（実装）がヘッダーの引数（`zoom`, `focusPos`）と異なっていた不整合を修正し、拡縮を考慮した逆変換ロジックを実装。ミニマップ（`DrawMiniMap`）も正しく復元。

### プレイヤー位置および小ステージ名デバッグウィンドウの追加
- マップ（`MapData`）に連結配置された各小ステージ（ピース）のファイル名を保持する `pieceNames` メンバを追加。JSONでのシリアライズ・デシリアライズに対応させました。
- `NarakuStageGenerator.cpp` の 3x3マップ生成時に、各配置グリッドのピース名を `mapData.pieceNames` に格納するように修正。
- `SceneNarakuProto` に常時表示される別ウィンドウ「プレイヤー位置デバッグ」を追加。プレイヤーの現在座標（X, Z）、高さ（Y）、現在深度、現在いる3x3内のグリッド座標、その位置に対応する小ステージ名、および足元のセル属性（Blocked や CliffEdge など）を詳細に表示できるようにし、想定外の壁での衝突原因を突き止めやすくしました。

### プレイヤー移動制限範囲の修正 (kWorldHalfSize の拡張)
- 3x3連結マップ全体のサイズ（1ピース 15セル × セルサイズ2.0f ＝ 30m四方、3x3連結で全体90m四方）に対して、`SceneNarakuProto.cpp` 内の `kWorldHalfSize` が `30.0f`（60m四方）に固定されていた問題を修正。
- `kWorldHalfSize = 45.0f`（90m四方分、-45.0f 〜 +45.0f）に修正したことで、プレイヤーがX, Z座標 `30.0f` より外周部分に進入した際に、透明な壁クランプで阻まれる不具合が完全に解消されました。外周に配置されたすべての開始・帰還地点へ正常に到達できるようになりました。

### 採掘（探窟）進行度ゲージの中央オーバーレイ追加
- HUD側に仮表示していた進行度バーを削除し、メインフィールドを描画している `DrawField()` において、採掘実行中（`m_miningIndex >= 0`）に進行度（0.0f〜1.0f）と「採掘中...」テキストをメインウィンドウ中央に直接オーバーレイ描画するように実装。これにより、ゲーム画面に集中したまま直感的に採掘の進捗状況を把握できるようになりました。

### 採掘（探窟）進行度ゲージの中央オーバーレイの修正 (DrawFieldから独立化)
- プレイ中に通常描画される3Dフィールド（`Draw3DField`）の前面にオーバーレイするために、独立した ImGui ウィンドウとして `DrawMiningProgressBar()` メソッドを実装。
- `Draw()` 内から毎フレーム呼び出されることで、3Dフィールドが描画されている状態でも、画面中央にマウス操作を透過するキャストバー（進行度）が正しく表示されるように修正しました。

## 2026-07-16

### 装備・遺物・店舗・潜行前後フロー
- 頭・胴装備とつるはしの所有・装備状態を追加し、革装備一式と錆びれたつるはしを初期装備にした。
- つるはし5種の採掘速度を100%、135%、180%、200%、250%として採掘時間へ反映した。攻撃力は変更していない。
- 採掘完了時に7種類から遺物を抽選し、その時点で種類別の重量と売却額を確定するようにした。
- 未知の種類は未鑑定表示とし、帰還時に自動鑑定する。一度鑑定した種類は同じプレイ中の次回採掘から判別できるようにした。
- 自宅在庫と潜行中所持品を分離し、自宅で装備と次回持ち込み品を変更できるようにした。
- 商店へ食料、強化遺物3種の購入と遺物売却を追加した。食料は価格5、重量1、HP回復量2で、売却不可とした。
- 武具屋へ提示価格表どおりの頭・胴装備とつるはしを追加し、割引購入と金のみ購入を選択できるようにした。
- 帰還地点で確認を表示し、帰還後に商店、武具屋、自宅、持ち込みなしの再潜行を選択できるようにした。
- 敵撃破時に食料をフィールドへ落とし、Fキーで拾えるようにした。プロトでは商店販売食料と同一品として扱う。
- 死亡時は潜行中の遺物と食料を失い、所有武具、地上在庫、所持金、鑑定履歴は維持するようにした。
- 進行状況のファイル保存と、製品段階での敵ドロップ食料・商店食料の分離は後続タスクとした。

### 所持品ID衝突と食料取得処理の修正
- 所持品一覧の各行を配列番号で `PushID` / `PopID` し、同名の未鑑定遺物が複数あってもImGui IDが衝突しないようにした。
- 敵ドロップ食料の取得判定が誤って帰還処理内へ入っていたため、探索中の `TryInteract()` へ移動した。
- 橙色の食料マーカー付近でHUDへ `F: 食料を拾う` を表示し、Fキーで取得できるようにした。

### 採掘進捗・遺物取得確認の配置修正
- 採掘進捗表示が固定画面サイズを基準にしていたため、メインビューポートの作業領域中央を基準に変更した。
- 採掘後の遺物取得確認も固定座標を廃止し、同じメインウィンドウ中央へ配置するようにした。

### 商店の売却一覧表示修正
- 在庫0の遺物を売却一覧へ表示しないようにし、最後の1個を売却した後は行が消えるようにした。
- 在庫0の行で `SameLine()` だけが残り、次の遺物が同じ行へ連結される問題を解消した。
- 売却可能な遺物がない場合の空表示を追加した。

### 未鑑定遺物の売値表示修正
- 探索中の所持品では、未鑑定遺物の重量を表示したまま売値を `?` として伏せるようにした。
- 一度鑑定済みの種類は、従来どおり所持品へ売値を表示する。

### 武具屋の商品表示重なり修正
- 装備の価格説明と頭・胴の所有状態／購入ボタンを別行へ分け、長い商品名や価格でも表示が重ならないようにした。
- 武器も価格説明と所有状態／購入ボタンを別行へ分けた。

## 2026-07-17

### プロト方位コンパス・遺物装備セット効果・3x3自動生成
- Editorの3Dプレビューと同じ四方位コンパスをプロト画面右上へ追加し、探索カメラのヨー角・仰角へ追従させた。
- 頭と胴の両方に遺物装備を装備した場合だけ、歩行速度1.2倍、走行速度2.0倍、HP毎秒0.2回復を発動するようにした。
- プロト起動時と潜行開始時に3x3マップ生成を最大5回試し、成功時だけ探索と持ち込み品消費を開始するようにした。
- 5回とも生成または読込に失敗した場合は、`FirstLayer.json`を表示して自宅へ戻すようにした。
- 自動生成と重複していたプレイテスト調整内の手動3x3生成ボタンを削除した。

### 4x4固定マップ生成
- 既存の3x3生成処理をグリッドサイズ共通処理へ整理し、`GenerateFixed4x4Map`から4x4マップを生成・検証・JSON保存できるようにした。
- 4x4では外周12枠、中央4枠として抽選し、開始・帰還ステージは四隅を除く外周8枠から選ぶようにした。
- 中央候補が4種類以上ある場合は中央4枠で同じピースを重複使用せず、4種類未満の場合は重複を許可するようにした。
- プロトの潜行開始時は4x4生成を最大5回試し、全失敗時は既存の3x3生成結果、それも読めない場合は`FirstLayer.json`を表示して自宅へ戻すようにした。
- プレイヤー移動範囲と地図座標変換を読み込んだTerrainLayer全体の寸法から計算し、3x3と4x4の両方を扱えるようにした。

### 探索ステータスゲージ表示
- 通常のImGuiウィンドウだったHUDを、メインウィンドウ左上固定の透明オーバーレイへ変更した。
- 表示内容をHP、スタミナ、精神力の3ゲージだけに絞り、数値やその他の状態表示を取り除いた。

### 攻撃モーション・命中演出・スカイスフィア
- プレイヤー攻撃中につるはし形状のラインを横振りし、攻撃開始ごとに左右を反転して連続攻撃が往復して見えるようにした。
- 1回のプレイヤー攻撃で同じ敵へ複数回ダメージが入らないよう、攻撃単位の命中管理を追加した。
- 敵の体当たりを一定速度から、突進開始時は遅く終了へ向けて加速する移動へ変更した。
- プレイヤー攻撃命中時、`Assets/Texture/Effect/yellow_bom.png`の横10コマを30fps相当でUV切り替えし、敵位置へビルボード描画するようにした。
- 敵の体当たり命中時、短時間で減衰するカメラシェイクを追加した。
- `Assets/Model/sky/sky.obj`をカメラ中心へ追従させ、表面カリングと深度無効でスカイスフィアとして描画するようにした。

### 層内ロープ・重量上限・中央通知・スカイスフィア修正
- 小ステージのロープ上端／下端セルを3x3・4x4生成結果のワールド座標へ変換し、生成JSONへ保存するようにした。
- マップのロープ形式を上端XZ・下端XZ・各レイヤーIDへ拡張した。旧形式の`x`／`z`は両端へ複製して読み込み、既存JSONとの互換性を維持した。
- 同一レイヤーIDのロープを層内高低差移動として扱い、Fで端点から掴み、W／Sで実際の3D線分上を移動できるようにした。異なるレイヤーIDは将来の層間移動用として保持し、今回はロード処理を追加していない。
- 上昇負荷を深度差ではなく実際のワールドY上昇量から計算し、左上HUDへデバッグゲージを追加した。
- 遺物装備を頭・胴の両方へ装備した場合、重量上限を100から200へ増加させた。所持品に現在重量、上限、割合を表示した。
- Eキーで食料を使ってHPを2回復し、食料不足、重量超過による走行不可、重量超過によるステップ不可をメインビューポート中央へ通知するようにした。
- スカイスフィアを両面描画へ変更し、OBJの面向きに依存せず内側から見えるようにした。

### α版の層間口・エリア保持・ロード画面
- 小ステージJSONへ層入口／層出口の役割、層間ロープ端点、層出口ロード地点を追加し、Editorから配置・削除・保存・読込できるようにした。
- 4x4生成器へ層入口／層出口の必須指定を追加した。通常配置後、四方の接続カテゴリが完全一致する専用ピースへ置換するため、既存の接続条件を崩さない。
- 初期エリアは層出口を1個必須、子エリアは層入口を必須、最大深度未満では層出口も必須として生成するようにした。最大深度は3とした。
- 各層間口と接続先エリアを1対1で記録し、地形、採掘状態、敵、ドロップ、地図ピンを潜行中のメモリへ退避・復元するようにした。訪問済みエリアは再生成せず双方向に往復できる。
- 未訪問の層出口では黒背景、中央の「ロード中...」、右下の進捗バーだけを表示する。生成失敗はF操作ごとに累積し、5回失敗すると対象層間口を使用不能にする。
- 層間ロープ移動中はプレイヤーを上下へ移動させ、遷移元の地面を段階的に透明化する。上昇時は移動量に応じて上昇負荷を加算する。
- `Assets/Config/naraku_proto_playtest.json`へプレイテスト調整値を分離し、起動時読込と調整ウィンドウの上書き保存ボタンを追加した。
- Debug x64ビルド成功を確認した。層入口／層出口専用のCompleted小ステージは未作成のため、実機での層間往復確認はコンテンツ作成後に行う。

## 2026-07-20

### モデルサイズ設定プレビュー
#### Assets一覧の表示寸法
- Assetsのモデルタイルを初期96pxへ縮小し、72pxから160pxまで調節できる表示サイズスライダーを追加した。
- 選択寸法とウィンドウ幅から列数を自動計算し、ウィンドウ拡大時はタイルではなく表示列が増えるようにした。

#### 環境モデル登録の永続化修正
- 環境モデル登録簿の読込・保存先を作業ディレクトリ依存からプロジェクトルート基準の`Assets/Naraku/environment_models.cfg`へ変更した。
- 登録簿保存前に保存先ディレクトリを作成し、モデルファイルもプロジェクトルート基準のパスで保存・復元するようにした。
- 登録、設定更新、削除で登録簿の保存に失敗した場合はメモリ上の変更を元へ戻し、成功扱いしないようにした。
- New ModelとModel Settingの設定画面へ、入力中のX/Y/Zサイズを即時反映する固定斜め上方向のモデルビューを追加した。
- 小ステージの1セル寸法を基準にした地面グリッドも描画し、配置時の相対サイズを確認できるようにした。
- New Modelでは選択直後に一時読込したモデルを追加確定時に登録側へ引き渡し、キャンセル時とEditor終了時には一時モデルと専用描画資源を解放する。

### Assetsモデルサムネイル表示
- 小ステージEditorのAssetsウィンドウを、登録モデル名だけの一覧から固定寸法の画像付きタイルへ変更した。
- モデル読込時に全メッシュの頂点境界を計算し、底面中央を基準に固定された斜め上方向のカメラからサムネイルを描画するようにした。
- モデルごとの境界寸法と登録時の既定サイズからカメラ距離を決めるため、大きさが異なるモデルもタイル内へ収まりやすくした。
- モデル設定で既定サイズを変更した場合はサムネイルを再生成し、登録削除またはEditor終了時には専用RenderTargetとDepthStencilを解放する。

### 小ステージ基本情報の層間口役割表示修正
- `層間口役割`のImGuiコードが誤ってピース名変更処理内へ配置され、基本情報ウィンドウに表示されていなかった問題を修正した。
- 役割選択を基本情報ウィンドウの奈落階層直下へ移動し、`なし`、`層入口`、`層出口`を通常表示から選択できるようにした。
- C++コンパイルは成功した。通常の出力先は実行中の`DX22_Project.exe`にロックされていたため、一時出力先へ切り替えて最終リンクまで成功を確認した。更新後バイナリでの画面確認は未実施。

### NarakuPieceDataヘッダーの文字化け修正
- `NarakuPieceData.h`に残っていた文字化けコメント99行を削除し、現在の列挙型、構造体、メンバー、公開関数に対応する日本語コメントへ書き直した。
- 型、列挙値、既定値、関数シグネチャ、JSON仕様は変更していない。
- `NarakuPieceData.h/.cpp`の文字化けパターンが0件であることと、UTF-8 BOMが維持されていることを確認した。
- 一時出力先を使用したDebug x64ビルドとリンクに成功した。

### 生成マップの開始・帰還地点統一
- 小ステージの`startReturnCandidate.cell`を開始地点にだけ使い、帰還地点を同じTerrainLayer内の最遠セルへ自動変更していた処理を削除した。
- Editorで設定した「開始・帰還地点」を生成マップの`playerStartPoint`と`returnPoint`の両方へ設定し、座標とTerrainLayerが一致するようにした。
- 修正前に生成されたJSONを読み込んだ場合もずれないよう、プロトのマップ初期化とエリア読込で帰還座標・深度を開始地点から設定するようにした。
- 通常の`x64/Debug/DX22_Project.exe`を再ビルドし、開始・帰還地点統一を含む実行ファイルへ更新した。

### カメラ遮蔽地形の非表示と調整値保存UI
- カメラからプレイヤー中心までの線分と地形セルの三角形を交差判定し、視線を遮るセルの床、グリッド線、当たり判定デバッグ線を描画しないようにした。
- 非表示化は描画処理だけに限定し、地形データと衝突判定は変更していない。
- プレイテスト調整の保存ボタンを全調整項目の下からウィンドウ上部へ移し、`調整値を保存`として常に見つけやすい位置へ配置した。
- 保存先と起動時読込は既存の`Assets/Config/naraku_proto_playtest.json`を継続して使用する。

### 小ステージ環境オブジェクト配置とモデルAssets
- 小ステージEditorの編集モードへ`環境オブジェクト配置`を追加し、登録モデルをセル単位で個数上限なく配置できるようにした。
- 環境オブジェクトは通常ロープ・層間ロープとのセル共有だけを許可し、採掘ポイント、開始・帰還地点、層間ロード地点、別の環境オブジェクトとの重複を禁止した。後からゲームオブジェクトを置く場合も同じ制約を適用する。
- 小ステージJSONへ`environmentObjects`を追加し、モデルID、セル座標、X/Y/Zスケールを保存・読込するようにした。項目がない既存JSONは空の配置として読み込む。
- `Models`メニューへ`New Model`、`Delete`、`Model Setting`を追加し、モデル名、モデルパス、既定サイズを`Assets/Naraku/environment_models.cfg`へ登録するようにした。初期登録モデルは0件とした。
- Assetsウィンドウへ登録モデル一覧、選択モデル情報、配置済み環境オブジェクトの個別サイズ編集と削除を追加した。
- 登録モデルを3Dプレビューへ描画し、選択中オブジェクトへ選択枠を表示するようにした。
- Fileメニューを`New File / Delete / 区切り / Load / Save / 区切り / Rename`へ並べ替え、ViewとModelsメニューも用途別に区切った。

## 2026-07-22

### 環境オブジェクトの生成マップ保存とプロト描画
- 3x3/4x4生成時に小ステージの環境オブジェクトをセル中心のワールド座標へ変換し、生成マップへ統合するようにした。
- 生成マップJSONへ`environmentObjects`を追加し、モデルID、XZ座標、所属レイヤーID、X/Y/Zスケールを保存・読込するようにした。項目がない既存マップは空一覧として読み込む。
- プロトの再潜行時に`Assets/Naraku/environment_models.cfg`を読み、モデルIDに対応するモデルをロードするようにした。
- モデル頂点境界から底面中央を求め、所属TerrainLayerの地形高へ合わせて環境オブジェクトを描画するようにした。
- 環境オブジェクトには当たり判定、回転、インタラクト処理をまだ追加していない。

## 2026-09-04

### ゲーム性強化追加要素
- 第一～第五層のドロップ率、敵能力、EXP、空腹、帰還報酬、出現上限を深度データへ集約し、プレイ可能な第一～第三層へ反映した。
- レベル1～100の等比補間EXP、逓減成長、装備倍率、死亡時レベル低下、探窟放棄時の0.5レベル後退、Lv100以降の保護を追加した。
- 奈落内で取得した未安定化遺物から活性度を算出し、敵の索敵範囲、攻撃後クールタイム、危険活性時の予兆時間へ反映した。
- 攻撃的遺物の追加範囲攻撃、生存的遺物の致死防止、精神力回復遺物の現地使用、固有遺物「欲望の揺籃」の持ち帰り報酬と進行フラグを実装した。
- 縄張り型の木密度配置条件、個体ランク、巡回、縄張り外からの帰還と、両敵種のランダム出現・5分リポップを追加した。
- ルート傾向、7.5m視認情報、セル探索率EXP、初エリア・初ルートEXP、Q短押し感知、Q長押し上昇負荷無効化を追加した。
- 満腹度、行動別消費、空腹デバフ、携帯食料、餓死、50Gのレストランを追加した。
- 採掘・撃破・深度別滞在・初到達・新種・固有遺物を個別集計する帰還報酬とリザルト内訳を追加した。
- 一時ファイル検証後に正式ファイルへ置換する、バージョン付きローカルセーブと自宅での任意保存を追加した。

### 検証
- 実装前に仕様書と企画草案をコミットし、作業ブランチをリモートへプッシュした。
- Debug x64ビルドが0エラーで完了することを確認した。
- プロジェクト全体には既存ファイルの文字コードに起因する警告が残っている。
- 第四・第五層は数値データのみで、地形・小ステージ・環境コンテンツは未実装のままとした。

### 地図・所持品タブと装備情報表示
- Tキー画面で所持品と地図を同時表示していた構造を分離し、Qで所持品、Eで地図へ切り替えるようにした。探索中のQ精神力能力とはモードで分離している。
- 地図画面へ現在の層を「第○層」で表示するようにした。
- 自宅の所有装備一覧と武具屋の商品一覧へ、頭・胴・武器それぞれの補正値を表示するようにした。
- 自宅と武具屋へ、現在装備、HP、スタミナ、精神力、重量、攻撃、防御、移動、回復、採掘、ロープ速度をまとめた現在ステータス欄を追加した。
- Debug x64ビルドが0エラーで完了し、変更対象のコンパイラ診断が0件であることを確認した。

### 商店・武具屋の価格再調整
- 携帯食料を10G、武具強化遺物を600G、武器・装備強化遺物を700Gへ変更した。
- 強化素材の売却価格を購入価格の10%となる60G、70G、70Gへ変更した。
- 武具屋の素材併用価格、金のみ価格、必要素材数を確定表へ更新し、頭と胴で異なる素材数を個別に表示・消費するようにした。
- 中間の遺物シリーズは確定した素材数を維持し、最高装備は頭を武具17・装備15、胴を武具19・装備19、遺物付きつるはしを武具11・武器21へ変更した。
- 前段階装備の所有を購入条件には追加していない。
- Debug x64ビルドが0エラーで完了することを確認した。既存ファイルの文字コード警告は継続している。

### 能力最大値に応じたHUDゲージ幅
- HPゲージをLv100最大HP12,000でゲームウィンドウ横幅の75%となる比例幅へ変更した。
- スタミナと精神力はHPと同じ能力値基準の幅を10倍し、Lv100基礎値でそれぞれ横幅50%、43.75%となるようにした。
- 満腹度、奈落活性度、上昇負荷ゲージは従来の固定長を維持した。
- 能力値、成長曲線、現在値、セーブ形式は変更していない。
- Debug x64ビルドが0エラーで完了することを確認した。

### 所持金・レベルの進行デバッグ
- プレイテスト調整ウィンドウへ所持金とレベルの直接入力欄を追加した。
- 所持金は0以上、レベルは1～100へ制限し、変更時に進行セーブへ即時保存するようにした。
- レベル変更時は現在EXPを0へ戻し、HP、スタミナ、精神力は変更前の割合を維持するようにした。
- Lv100以降の余剰EXPと保護数は変更対象外とした。
- Debug x64ビルドが0エラーで完了することを確認した。

### 全5層の上層・中層・下層化と分岐構造
- 第一層から第五層を上層・中層・下層の15段階へ分割し、各段階に2～4個の独立エリアを抽選するようにした。
- 潜行開始時に全エリアと双方向接続を計画し、全エリアが開始地点から到達可能、各エリアの層間口合計が最大4個、中間エリアが上下へ最低1接続となるまで再抽選するようにした。
- 同じ2エリア間の複数接続を許可し、接続ごとの一意なIDで往路と復路の対応する層間口を特定するようにした。
- 第一層上層の1エリアだけに開始・帰還地点を配置し、他エリアでは開始・帰還用小ステージを生成候補から除外した。
- 第五層下層を上り口だけの終端とし、第二層から第五層は第一層の完成済み小ステージを暫定共用した。
- 接続先マップは初回調査時に遅延生成し、失敗しても接続計画を維持したまま同じ層間口から再試行できるようにした。
- 地図とルート傾向へ、親層・上中下・エリア番号を表示するようにした。

## 2026-09-05

### 最大HP・HUD幅と層間調査位置の修正
- 最大HPをLv1で100、Lv100で1,200となる成長範囲へ変更した。
- スタミナと精神力のHUD幅に掛けていた10倍の表示補正を外し、HPと同じ能力値あたりの幅で表示するようにした。
- 未生成エリアの調査時に、プレビュー生成処理がプレイヤーを接続先の最初の入口座標へ移動させていたことが位置ずれの原因だった。
- 調査用の接続先生成ではプレイヤーを移動させず、実際に層間移動が完了した時だけ対応する接続IDの層間口へ配置するようにした。

### 生還時の精神力回復
- 生還による帰還確定時に、現在精神力を最大精神力まで全回復するようにした。
- 死亡と探窟放棄には回復を適用せず、生還処理内で回復後の値を進行セーブへ保存する。
- レストラン利用時は、既存の満腹度全回復と最大HP75%回復に加え、最大精神力の50%分を現在精神力へ加算するようにした。

### 進行セーブの相対パス化
- 進行セーブだけがマップ機能の固定プロジェクト絶対パスを経由していたため、配置場所によって任意保存に失敗する状態を解消した。
- 保存と読込の両方を、実行時のカレントディレクトリ基準の`Assets/Save/naraku_proto_save.dat`へ統一した。
- 一時ファイルへの書込み、内容検証、正式ファイルへの置換という既存の保存手順は維持した。

## 2026-09-07

### Editor・ゲーム・残存機能の3プロジェクト分離

#### 変更内容
- 1プロジェクトへ混在していた機能を、ゲーム用`奈落塔`、ステージ編集用`NarakuEditor`、残存機能用`DX22_Project`の3プロジェクトへ分離し、同じSolutionから個別にビルドできるようにした。
- Solution構成をx64のDebug／Releaseへ統一し、Win32構成を廃止した。
- 出力先を`Output/<構成名>/Game`、`Output/<構成名>/Editor`、`Output/<構成名>/Legacy`へ分離した。
- ゲーム側ではEditorシーンを通常ビルド対象から外し、Releaseではデバッグウィンドウ、Editor用ネイティブメニュー、パフォーマンス計測をコンパイル対象から除外した。
- ゲーム内の潜行、自宅・店舗、リザルト表示の責務を分離し、`NarakuGameSession`でシーン間の一時データを保持する構成へ変更した。
- シーン切替前に進行データを保存し、保存失敗時は遷移を中止して再試行できるようにした。アプリ終了は探窟放棄と同じ経路で処理するようにした。

#### 変更意図
- ゲーム実行時に不要なEditor機能を読み込まない構成にし、実行ファイルの責務と依存範囲を明確にするため。
- ゲーム、ステージEditor、既存の残存機能を独立して起動・検証できるようにし、今後の機能追加で影響範囲を限定するため。
- シーン分割後も潜行中の状態をセーブファイル経由ではなく一時データとして安全に引き継ぎ、セーブを永続化のみに使用するため。

### Assets・共通ソース・マップ発行の分離対応

#### 変更内容
- Visual Studioからの実行では各ProjectDir、exe直接起動では各exe配置先を基準として、相対パスの`Assets`を参照する構成へ変更した。
- ゲームの進行セーブをゲーム側`Assets/Save`へ保存し、旧`DX22_Project/Assets/Save`からの移行処理を追加した。
- ゲームを同期元として必要な共通ソースをEditorへ同期する`Sync-SeparatedSources.ps1`と、実行用Assetsを各出力先へ配置する`Stage-RuntimeAssets.ps1`を追加した。
- Editorの`Assets/Maps`を検証後にゲーム側へ発行する`Publish-NarakuMaps.ps1`を追加した。発行前バックアップを最大5世代保持し、失敗時はロールバックする。
- マップ発行時にマップバージョンを更新し、ゲーム側でバージョン不一致を検出した場合はマップ依存の進捗だけをリセットするようにした。
- 発行対象ファイルがゲーム実行中で使用されている場合は、`実行中なので保存できません`として発行を中止するようにした。
- 固定されていたプロジェクト絶対パスを削除し、マップと小ステージ関連の参照を相対パスへ統一した。

#### 変更意図
- 開発環境の絶対パスに依存せず、Visual Studio実行と配布先からの直接起動で同じAssets構造を使えるようにするため。
- Editorの生成物をゲームへ明示的に発行する運用とし、プレビューや編集中のデータがゲーム本番データを意図せず上書きすることを防ぐため。
- マップ形式や内容が変わった時に、古いマップ依存進捗との不整合を残さないため。

### Editor生成プレビュー

#### 変更内容
- MenuBarのViewから開ける生成プレビューを追加し、第一層上層から第五層下層までの15段階を対象にした。
- 深度、上層・中層・下層、エリア番号、エリア数2～4、64bit乱数シードを指定できるようにした。
- シードの固定、同一条件での再生成、新しいシードでの生成に対応した。
- ゲーム本番と同じ生成データと乱数処理を使いながら、プレビュー結果はメモリ内だけに保持し、本番マップへ保存しないようにした。
- 歩行確認では敵を生成したまま停止し、採掘、空腹、水分、死亡、スタミナ、精神力のゲーム進行処理を無効化した。
- 俯瞰表示と歩行表示、任意段階からの開始、層間移動、開始地点への復帰、落下・水没時の復帰に対応した。

#### 変更意図
- ゲームを通して15段階を順番に潜行しなくても、Editor上で指定条件の生成結果と接続を短時間で確認できるようにするため。
- 本番と異なる生成器を持たず、ゲームとEditorの生成結果が将来ずれる問題を防ぐため。
- プレビュー操作によってセーブ、マップ、Editor文書などの永続データを変更しないため。

### 検証
- 3プロジェクトについてDebug x64とRelease x64の計6構成がビルドできることを確認した。
- 生成一致テストで、15段階のエリア数、接続ID、配置された小ステージIDがゲーム側とEditor側で一致することを確認した。
- マップ発行、バックアップ復元、実行中競合エラー、旧セーブ移行、マップバージョン保存を確認した。
- Visual Studio基準と出力先基準の両方から計6実行ファイルを起動し、参照元の`Assets`以外を書き換えないことを確認した。
- Release版ゲームにEditor関連文字列が含まれないことを確認した。
- 変更差分に対して`git diff --check`を実行し、空白エラーがないことを確認した。

### 継続事項
- 未保存の小ステージ編集内容をプレビューへ反映する際の3択UIは未実装。
- マップ発行時のModelコピーは参照中のModelだけではなく、現在はModel一式を対象としている。
- ゲームとEditorの主要UIを通した一連の手動操作確認、意図的に保存を失敗させる遷移中止確認は未実施。
- 既存`Defines.h`の文字コードに起因するコンパイラ警告は残っている。

### 日本語文字化けと文字コード警告の修正

#### 変更内容
- Shift-JISで保存されていた3プロジェクトの`Defines.h`をUTF-8へ統一し、崩れていた日本語コメントと`APP_TITLE`を正常な文字列へ修正した。
- `Defines.h`の`#include <Windows.h>w`に付いていた余分な`w`を削除した。
- ゲーム起動直後の「奈落塔」をANSI版Win32 APIへ渡していた処理を、Unicode版のウィンドウクラス登録・生成・終了処理へ変更した。
- 小ステージの日本語ファイル名を`wstring`から`char`へ1文字ずつ切り詰めていた処理を、既存のUTF-8変換へ変更した。
- 分離元の起動処理とステージ生成器に残っていた文字化けコメントを、現在の処理内容に沿った日本語へ修正した。

#### 変更意図
- `/utf-8`でコンパイルする3プロジェクトにShift-JISソースが混在し、文字列破損と大量の文字コード警告が発生する状態を解消するため。
- WindowsのANSIコードページに依存せず、日本語のウィンドウ名と小ステージ名を同じUTF-8運用で保持するため。

#### 検証
- Debug x64とRelease x64で3プロジェクトをビルドし、いずれも警告0・エラー0で完了することを確認した。
- 3つの`Defines.h`が厳密なUTF-8として読み取れることと、ゲームからEditorへの共通ソース同期検査が成功することを確認した。
- Debug版のゲーム、NarakuEditor、残存機能を個別起動した。ゲームの「自宅 - 潜行準備」とEditorの日本語ウィンドウ名が正常に取得できることを確認した。

## 2026-09-08

### 小ステージEditorからの生成プレビュー起動

#### 変更内容
- 小ステージEditorの`View`メニューに`Generated Preview`を追加した。
- メニュー選択時に未保存の小ステージがある場合は既存の保存確認を行い、キャンセル時は画面遷移しないようにした。
- ステージEditorへの遷移要求に一回限りのプレビュー表示状態を持たせ、遷移直後に15段階生成プレビューを自動表示するようにした。

#### 変更意図
- 小ステージの編集中に、ステージEditorを開いてから再度メニュー操作する手順を省き、生成結果の確認へ直接移動できるようにするため。

#### 検証
- NarakuEditorのDebug x64とRelease x64をビルドし、どちらもエラーなしで完了することを確認した。
- Debug版を小ステージEditorで起動し、`Generated Preview`メニューコマンドの送信後も正常に稼働し続けることを確認した。

### 深度・レベル別の消費・回復補正

#### 変更内容
- 第一層から第五層までのスタミナ消費、スタミナ回復、精神力消費、満腹度消費に、深度・レベル別の倍率表を追加した。
- Lv1～10は最初の表値を使い、Lv11以降は10レベルごとの基準値間を直線補間するようにした。
- スタミナ消費補正を走行、ロープ昇降、採掘、ジャンプ、回避、通常攻撃、攻撃的遺物の追加消費へ適用した。
- スタミナ回復は能力成長倍率と深度・レベル回復補正を乗算するようにした。
- 従来の深度だけの満腹度補正を廃止し、新しい深度・レベル補正へ置き換えた。
- Q短押しとQ長押しの精神力消費に新しい倍率を適用し、上昇負荷などの精神力ダメージは補正対象外とした。

#### Q長押しの上昇負荷無効化
- 従来の「3分以内の最初の1回」を廃止し、Lv30～39の1秒からLv100の8秒まで10レベルごとに1秒延長する時間制へ変更した。
- 効果時間中は回数制限なしで新しい上昇負荷発症を防ぎ、一度防いでも効果を終了しないようにした。
- 発動前から継続中の上昇負荷効果には干渉せず、発動後の発症だけを無効化するようにした。
- 再使用時は精神力を再度消費し、効果時間を現在レベルの値で上書きする。HUDの残り時間表示は継続して使用する。

#### 変更意図
- 深層ほど低レベル時の行動継続が難しくなり、レベル上昇によって消費負担が段階的に緩和される成長感を反映するため。
- Q長押しを、発症1回だけを待つ使い方から、上昇行動に合わせて発動する判断型の防御能力にするため。

#### 検証
- ゲーム正本からEditorの歩行プレビュー用ソースへ同期し、同期差分がないことを確認した。
- 奈落塔とNarakuEditorのDebug x64・Release x64をビルドし、全4構成がエラーなしで完了することを確認した。
- Debug版の奈落塔を起動し、初期化後も正常に稼働し続けることを確認した。
- 変更差分に対して`git diff --check`を実行し、空白エラーがないことを確認した。

### 水分・水場・調理システム

#### 変更内容
- 最大100、初期値70の水分パラメーターを追加し、探窟中は満腹度と同じ行動量・深度／レベル倍率で消費するようにした。水分0から60秒後に脱水の視界不良が始まり、さらに60秒かけて画面占有率50%まで進行する。給水後は60秒かけて解除する。
- 通常食料、加熱食料、レストランで水分を75回復し、レストラン内に無料の給水所を追加した。
- 水筒を商店へ500G・重量15・容量100で追加した。空の水筒だけを満水にでき、水質を「なし／安全／未煮沸／煮沸済」で個体ごとに保持する。1回の飲水量は25とし、内容物だけを全量破棄できるようにした。
- 料理セットを商店へ100G・重量5・最大5回使用の消耗品として追加した。使用開始時に1回消費し、60秒間、移動・攻撃・回避・被弾・ロープ・落下・層間移動をせずに維持すると、未煮沸水1本の煮沸または通常食料1個の加熱が完了する。中断時は対象を変化させず、使用回数だけを失う。
- 加熱食料をHP40、満腹度25、水分75、精神力5回復として追加した。
- 未煮沸水を飲むたびに、第一層10%、第二層30%、第四層80%で食中毒判定を行い、発症時は現在HPの10%を一度だけ減らすようにした。
- 小ステージEditorのセルへ水場深度「なし／溜り／池／湖」を追加し、JSONへ保存するようにした。溜りはスタミナ消費1.2倍、池は走行不可かつ歩行時に走行相当のスタミナを消費、湖は進入不可とした。
- 水分、加熱食料、水筒の水量・水質・持ち込み設定、料理セットの残り回数・持ち込み設定をセーブ対象へ追加し、セーブバージョンを2へ更新した。直前形式のバージョン1は水分70・追加アイテムなしとして読み込めるようにした。

#### 変更意図
- 深層探索に水分確保と未煮沸水のリスク判断を追加し、食料・水筒・料理セットの持ち込み構成と現地調理に意味を持たせるため。
- 水場の深さを見た目だけで推測せずEditorデータで確定し、ゲーム生成とEditorプレビューで同じ通行・消費ルールを使えるようにするため。
- 既存セーブを破損扱いにせず、今回追加した永続データへ一世代分だけ移行できるようにするため。

#### 検証
- ゲーム正本からEditorの歩行プレビュー用ソースへ同期し、同期差分がないことを確認した。
- 同一シードのゲーム生成とEditor生成について、段階数、接続ID、小ステージ配置IDが一致することを確認した。
- 奈落塔とNarakuEditorのDebug x64・Release x64をビルドし、全4構成がエラーなしで完了することを確認した。
- Debug版の奈落塔とNarakuEditorを各5秒起動し、プロセスが異常終了しないことを確認した。奈落塔の既存セーブは起動前に退避し、確認後に元へ戻した。
- 変更差分に対して`git diff --check`を実行し、空白エラーがないことを確認した。

### 深度・レベル別の上昇負荷

#### 変更内容
- 上昇負荷の発症間隔を累計上昇量10mから20mへ変更し、停止・平地移動・下降中のゲージ回復は従来どおり毎秒1mとした。
- 第一層と第二層では、レベル別の精神力低下とアイリスアウトを発症させ、精神力低下量と同じ秒数だけ視界不良を継続するようにした。
- 第三層の暫定効果を第二層基準とし、視界不良は同じ強度、精神力低下量と継続時間は1.5倍にした。
- 第四層では防御力を適用しない最大HP割合ダメージを発生させ、致死時には生存的遺物が発動できるようにした。
- 第五層では盲目と移動負荷を同じ時間だけ発生させ、WASD入力中は最大1秒に1回、最大HP割合ダメージを受けるようにした。再発症時は残り時間を加算する。
- 上昇負荷表はLv1～10を最初の値とし、Lv11以降は10レベルごとの表値間を直線補間するようにした。
- Q長押しによる無効化中は、新たに成立した複合効果全体を防ぎ、既に発症中の効果には干渉しない従来確定仕様を維持した。
- 視界不良と盲目の残り時間をHUDへ表示した。

#### 変更意図
- 深度とプレイヤーレベルに応じて上昇時の危険性を変え、浅層で成長を実感しながら深層では移動判断そのものが危険になる仕様を反映するため。
- 第三層の幻覚・幻聴が実装可能になるまでは、確定した暫定効果でゲーム進行を成立させるため。
- Q長押しを発症後の解除手段ではなく、危険な上昇を始める前に使う予防手段として維持するため。

#### 検証
- ゲーム正本からEditorの歩行プレビュー用ソースへ同期し、同期差分がないことを確認した。
- 奈落塔とNarakuEditorのDebug x64・Release x64をビルドし、全4構成がエラーなしで完了することを確認した。
- Debug版の奈落塔を起動し、初期化後も正常に稼働し続けることを確認した。
- 変更差分に対して`git diff --check`を実行し、空白エラーがないことを確認した。

### 小ステージEditorの水場属性配置ツール

#### 変更内容
- ゲームオブジェクト配置モードの配置ツールへ「水場属性」を追加した。
- 配置ツール上で「なし（解除）／溜り／池／湖」を選択し、3Dプレビュー上のセルを左クリックして水場属性を設定できるようにした。
- ホバー中セルの現在の水場属性を配置ツールへ表示した。
- 水場属性を持つセルの外周と対角線を水色で描画し、通常セルと識別できるようにした。
- 水場の描画色は溜りを従来の水色とし、池、湖の順に暗くして属性を見分けられるようにした。
- 水場属性を歩行可否・ロープ・採掘・敵スポーン属性から独立させ、属性変更時に他のセル属性を上書きしないようにした。湖の進入不可処理は生成時に水場属性から適用する。
- 水場属性の設定と解除を既存のUndo／Redo履歴へ含めた。

#### 変更意図
- セルを地形編集で選択してから個別設定する手順に加え、配置ツールから水場を直接割り当てられるようにするため。
- 湖から池などへ変更した際に、湖設定によって他のセル属性が失われたままになる問題を防ぐため。

#### 検証
- NarakuEditorのDebug x64とRelease x64をビルドし、両構成がエラーなしで完了することを確認した。
- 生成一致検証を実行し、段階数、接続ID、小ステージ配置IDがゲーム側とEditor側で一致することを確認した。
- Debug版NarakuEditorを5秒間起動し、プロセスが異常終了しないことを確認した。
- 変更差分に対して`git diff --check`を実行し、空白エラーがないことを確認した。

### 水場セルの配置制限とEditor登録情報の永続化

#### 変更内容
- 溜り、池、湖のいずれかが設定されたセルへ、採掘ポイント、ロープ端点、開始・帰還地点、層間口端点、環境オブジェクトを配置できないようにした。
- 配置物が存在するセルへ新しく水場属性を設定する操作も禁止し、水場属性の解除だけは許可した。
- 既存データ内で水場と配置物が重複している場合、セル座標付きの検証エラーを表示するようにした。
- Editorのビルド後Assets配置処理で、`environment_models.cfg`と`piece_hierarchy.cfg`がすでに保存先へ存在する場合は上書きしないようにした。

#### 変更意図
- 水場上に採掘地点やモデルなどが生成される不正な小ステージデータを、編集時点で防ぐため。
- EditorでモデルやHierarchyを登録した後、次回起動前のビルド処理が古い基準Assetsをコピーして登録内容を消していた問題を解消するため。

#### 検証
- NarakuEditorのDebug x64とRelease x64をビルドし、両構成がエラーなしで完了することを確認した。
- Debugビルド前後で、プロジェクト側と出力先側それぞれのモデル登録簿・Hierarchy登録簿のSHA-256が変化しないことを確認した。
- 生成一致検証を実行し、段階数、接続ID、小ステージ配置IDがゲーム側とEditor側で一致することを確認した。
- 変更差分に対して`git diff --check`を実行し、空白エラーがないことを確認した。

### 小ステージEditorからゲームへの発行

#### 変更内容
- 小ステージEditorのFileメニューへ「Publish Pieces to Game」と「Restore Published Pieces」を追加した。
- 未保存の編集がある場合は発行と復元を中止し、先に保存するよう通知するようにした。
- 起動中のEditorが参照しているAssetsから、小ステージJSON、環境モデル登録簿、Modelsをゲームプロジェクトへ同期する発行スクリプトを追加した。
- piece_hierarchy.cfgはEditor専用のため発行対象から除外し、発行前に全小ステージJSONを検証するようにした。
- 発行前データは最新5世代まで保存し、直近世代を復元できるようにした。
- 発行・復元中に失敗した場合は、小ステージ、Models、モデル登録簿、マップ版番号を操作前の状態へ戻すようにした。
- 発行・復元の完了時にmap_version.txtを更新し、ゲーム側で生成進捗がリセット対象になるようにした。
- ゲーム実行中または別の発行処理中は「実行中なので保存できません」として発行を中止するようにした。

#### 変更意図
- 小ステージEditorで作成したデータを手作業でゲーム側へコピーする必要をなくし、追加・更新・削除を一つの発行操作で反映するため。
- Editor専用のHierarchyをゲームに混入させず、モデルと登録簿の不整合も防ぎながら安全に差し替えるため。

#### 検証
- PowerShellの構文解析で発行スクリプトに構文エラーがないことを確認した。
- 実ゲームAssetsを書き換えない隔離環境で発行と復元を実行し、22件のJSONが発行され、piece_hierarchy.cfgが除外され、発行前の21ファイル構成へ復元できることを確認した。
- NarakuEditorのDebug x64とRelease x64をビルドし、両構成がエラーなしで完了することを確認した。

### 通常クエスト、週内時間、違約金・借金

#### 変更内容
- 自宅、商店、武具屋、レストラン、依頼受付の全画面へ共通の地上施設ナビゲーションを追加し、どの施設からでも他の全施設へ直接移動できるようにした。
- レストランから開ける依頼受付を追加し、左側の7件一覧と右側の詳細、受注、報告、残り期限、進捗を表示するようにした。
- 通常クエスト10種類を均等抽選し、同種最大2件、同時受注最大3件、階級と最大到達深度による対象深度制限を実装した。
- 未受注依頼の小・中・大・週末リセットへゲーム内時間と深度倍率を適用し、受注期限と報告後5分の再募集待ちは停止画面を除く実プレイ時間で進めるようにした。
- 討伐、敵発見、採掘地点発見、受注後に取得した強化素材の持ち帰り、落とし物、救助、受注時納品を既存の探索・帰還処理へ接続した。
- 期限切れ時に報酬10%の違約金を課し、所持金、売値の低い非食料品、借金の順に充当するようにした。装備中の武具は売却時に外し、固有遺物は受付で任意に1個1,000Gとして借金返済へ使えるようにした。
- 探窟報酬から借金を先に徴収するようにした。
- クエスト、進捗、期限、借金、階級、最大到達深度、週内時刻、週シードをセーブ対象へ追加し、セーブバージョンを3へ更新した。直前形式のバージョン2は新規クエスト盤を生成して移行する。

#### 変更意図
- 依頼受付やレストランを特定施設経由でしか開けない導線をなくし、地上施設間の移動規則を統一するため。
- クエストを探索中の討伐・発見・回収・物資判断と接続し、地上施設へ戻って報告する反復目標を作るため。
- 深層ほど週内時間が速く進む仕様と、受注後の期限は実時間で扱う仕様を分離し、画面停止中やアプリ終了中に不意に失敗しないようにするため。
- 違約金不足を所持品処分と将来報酬からの徴収へつなぎ、支払不能でも進行データを破綻させないため。

#### 検証
- ゲーム正本からEditorの歩行プレビュー用ソースへ同期し、同期差分がないことを確認した。
- 奈落塔とNarakuEditorのDebug x64・Release x64をビルドし、全4構成が警告・エラーなしで完了することを確認した。
- 同一シードのゲーム生成とEditor生成について、段階数、接続ID、小ステージ配置IDが一致することを確認した。
- 変更対象のC++差分へ`git diff --check`を実行し、空白エラーがないことを確認した。

### 地上施設の共通移動導線

#### 変更内容
- 自宅、商店、武具屋、レストラン、依頼受付の全画面へ、5施設すべてに直接移動できる共通ナビゲーションを追加した。
- 現在開いている施設のボタンは無効表示にし、同じ画面を開き直さないようにした。

#### 変更意図
- 特定施設を経由しないと別施設へ移動できない導線を解消し、地上ではどの施設からでも目的の施設へ直接移動できるようにするため。

#### 検証
- ゲーム正本からEditorの歩行プレビュー用ソースへ同期し、同期差分がないことを確認した。
- 奈落塔とNarakuEditorのDebug x64・Release x64をビルドし、全4構成が警告・エラーなしで完了することを確認した。
- 変更対象の差分へ`git diff --check`を実行し、空白エラーがないことを確認した。

### 重要クエスト・拠点メタデータ・固定生成

#### 変更内容
- 重要クエストを通常クエストとは別タブで管理し、前提達成による自動受注、進捗、受付での報酬受領、一度限りの完了状態を実装した。
- 層到達、層踏破、敵討伐、縄張り型討伐、第二拠点・前衛拠点発見、固有遺物の初回生還持ち帰りを既存の探索処理へ接続した。
- 最初の重要クエストが進行するまで依頼受付だけをロックし、自宅、商店、武具屋、レストランは従来どおり利用可能にした。
- 小ステージEditorへ第二拠点・前衛拠点の種別を追加し、拠点範囲をモデルではなく線でプレビュー表示するようにした。
- 第二層中層と第五層下層のエリア番号2へ、対応する拠点付き小ステージを必ず配置する生成条件を追加した。
- 第二拠点を中央2x2セル、前衛拠点を中央3x3セルとしてマップデータへ記録し、ゲーム側でも線だけで表示するようにした。
- 前衛拠点の発見を第五層の終着点到達として扱うようにした。
- セーブ形式をバージョン4へ更新し、重要クエスト状態を保存対象へ追加した。旧バージョン3以前は読み込まず、新規データとして開始する。

#### 変更意図
- 重要クエストを週更新型の通常依頼から分離し、奈落への到達・踏破・拠点発見を恒久的な進行目標として扱うため。
- 第二拠点と前衛拠点を毎回同じ段階・同じエリア番号へ確実に生成しつつ、Editorで小ステージ単位の配置データを管理できるようにするため。
- 拠点の仮表示へ追加モデルを要求せず、後から正式な外観へ置き換えられるようにするため。

#### 検証
- 奈落塔とNarakuEditorのDebug x64・Release x64をビルドし、全4構成が警告・エラーなしで完了することを確認した。
- 同一シードのゲーム生成とEditor生成について、段階数、接続ID、小ステージ配置IDが一致することを確認した。
- 奈落塔Debug版を起動し、起動直後に異常終了しないことを確認した。
- 変更差分へ`git diff --check`を実行し、空白エラーがないことを確認した。

#### 未検証
- 起動確認はプロセスを強制終了したため、終了時または任意保存操作によってバージョン4のセーブファイルが実際に書き出されるところまでは確認していない。

### 生成進捗・失敗理由表示と拠点生成不具合の修正

#### 変更内容
- 15段階生成プレビューを一括同期生成から1フレーム1エリアの段階生成へ変更し、完了エリア数、総エリア数、現在の深度・上中下・エリア番号、進捗率をロード画面へ表示するようにした。
- 通常の層間口から未生成エリアを調査する際も、生成対象の深度・段階・エリア番号と進捗率をロード画面へ表示するようにした。
- 生成失敗時に、失敗した工程またはエリア、乱数シード、生成器が返した詳細理由をGUIへ表示するようにした。
- 同じ失敗情報を`Assets/Logs/naraku_generation_failure.log`へ追記するようにした。
- 層間入口・出口用だった`sample_center005.json`と`sample_center006.json`から拠点属性を外し、第二拠点・前衛拠点を専用の小ステージJSONへ分離した。

#### 変更意図
- 歩行確認ボタン押下後に画面が無反応に見える状態をなくし、15段階生成がどこまで完了したか確認できるようにするため。
- 生成失敗を自宅画面への復帰だけで終わらせず、問題のある段階・エリア・小ステージ条件を特定できるようにするため。
- 層間口用小ステージが複数配置された際に拠点情報まで複製され、「同じ種類の拠点が複数あります」と失敗していた根本原因を解消するため。

#### 検証
- 奈落塔とNarakuEditorのDebug x64、Release x64をビルドし、全構成がエラーなしで完了することを確認した。
- 不具合修正前に、第1層上層エリア2と「同じ種類の拠点が複数あります」という詳細が失敗ログへ記録されることを確認した。
- 拠点専用データを分離後、15段階の生成一致テストが成功することを確認した。
- Editor、ゲーム、Debug／Release出力の全Assetsで、拠点用データと層間入口・出口用データが分離されていることを確認した。

### 生成プレビューの帰還可能エリア開始指定

#### 変更内容
- Generated Previewの設定へ「開始・帰還地点のあるエリアでスポーン」を追加した。
- 有効時は深度・段階・開始エリア番号の指定より帰還可能エリアを優先し、正式な開始・帰還地点を持つエリアから歩行確認を開始するようにした。
- 無効時は従来どおり、指定した深度・段階・開始エリア番号から開始する。
- プレビュー設定ファイルへ選択状態を保存し、ゲームとEditorの生成一致テストで開始エリアが実際に帰還可能であることを検証するようにした。

#### 変更意図
- 指定されたエリアに正式な開始・帰還地点がない場合、代替スポーンが北西端の最初の利用可能セルへ置かれて脱出できなくなることがあるため、正式な帰還地点を持つエリアを明示的に開始位置へ選べるようにするため。

#### 検証
- 奈落塔とNarakuEditorのDebug x64・Release x64をビルドし、全4構成がエラーなしで完了することを確認した。
- 「開始・帰還地点のあるエリアでスポーン」を有効にした設定で15段階の生成一致テストを実行し、選択された開始エリアが帰還可能エリアであることと、段階数・接続ID・小ステージ配置IDが一致することを確認した。

### 小ステージの深度・上中下層タグ

#### 変更内容
- 小ステージEditorの奈落階層指定を数値入力から「指定なし／第一層～第七層」のドロップダウンへ変更した。
- 奈落階層とは独立して「指定なし／上層／中層／下層」を指定できる上中下層タグを追加した。
- 新規小ステージの両タグを指定なしとし、既存の小ステージJSON全22件も両タグが指定なしになるよう移行した。
- 生成器がLayer1からLayer7までの完成小ステージを読み込み、対象の深度・上中下層と一致しない指定付き候補を除外するようにした。
- 配置条件を満たす候補のうち、深度と上中下層の一致タグ数が最も多い候補を優先し、同じ優先度の候補内でランダム抽選するようにした。指定付き候補が配置条件を満たさない場合は指定なし候補を使用する。
- 同じ選別規則を通常配置、開始・帰還地点、層間口、第二拠点・前衛拠点へ適用した。
- 旧`abyssLayer`値による地形のY方向オフセットを外し、奈落階層値を抽選タグとしてのみ扱うようにした。

#### 変更意図
- 深度専用、上中下層専用、両方を限定した小ステージを作成できるようにしつつ、指定なしの既存小ステージを全段階共通の候補として維持するため。
- 指定タグによって小ステージ自体の高さが変わる旧挙動をなくし、タグを生成候補の選別だけへ使用するため。

#### 検証
- 既存JSON全22件が`abyssLayer: 0`かつ`sublayerTag: none`へ移行され、JSONとして読み取れることを確認した。
- 指定なし候補の適合、一致指定の適合、不一致指定の除外、深度と上中下層がともに一致する候補の最優先を自動検証した。
- 奈落塔とNarakuEditorのDebug x64・Release x64をビルドし、全4構成がエラーなしで完了することを確認した。
- 15段階の生成一致テストが成功し、段階数、接続ID、小ステージ配置IDがゲーム側とEditor側で一致することを確認した。

### 歩行不可セルの空中通過・滑落・安全復帰

#### 変更内容
- 地上移動では従来どおり歩行不可セルを通れないまま、ジャンプ中と落下中だけ歩行不可セルへ侵入できる空中移動判定を追加した。
- 削除セルは空中でも侵入できない判定を維持した。
- 傾斜のある歩行不可セルでは四隅の地形高から低い方向を求め、自動で滑落するようにした。
- 傾斜方向がない歩行不可セルでは侵入時の水平速度を維持し、削除セルや地形外に阻まれた場合は進行方向を反転するようにした。
- 歩行不可セルから5秒以内に脱出できなかった場合、最後に記録した歩行可能な接地位置へ復帰するようにした。
- 湖では地表から1m下へ落ちた時点で安全位置へ復帰し、防御力を適用せず最大HPの15%を失うようにした。致死時は生存的遺物を適用する。
- 安全復帰による座標差を移動EXPへ加算しないようにした。

#### 変更意図
- 歩行不可属性を地上歩行の制限として残しながら、ジャンプによる飛び越しや傾斜からの滑落を成立させるため。
- 平坦な歩行不可セル、連続する斜面、湖などで脱出不能になった場合にも、5秒で探索を継続できる状態へ戻すため。

#### 検証
- ゲーム正本からEditorの歩行プレビュー用ソースへ同期し、同期差分がないことを確認した。
- 奈落塔とNarakuEditorのDebug x64・Release x64をビルドし、全4構成がエラーなしで完了することを確認した。
- 15段階の生成一致テストが成功し、段階数、接続ID、小ステージ配置IDがゲーム側とEditor側で一致することを確認した。

#### 未検証
- 実際の操作による歩行不可セルへのジャンプ進入、斜面滑落、反転、5秒復帰、湖落下ダメージは手動確認していない。

### 死亡時の荷物回収と階級保険

#### 変更内容
- 死亡した親深度が現在階級の保険対象内である場合、探索中に所持していた遺物、食料、加熱食料、水筒、料理セットを回収候補として死亡リザルトへ退避するようにした。
- 回収費を死亡時点の所持金に対して第一～第三層30%、第四層40%、第五層50%で確定し、四捨五入かつ最低1Gとした。
- 死亡リザルトへ回収対象の詳細と費用を表示し、「回収する」か確認付きの「永久に破棄する」を選ぶまで再挑戦・自宅遷移を無効にした。
- 回収時は所持金から費用を支払い、不足分を借金へ加算したうえで全対象を自宅保管へ戻すようにした。回収した遺物は安定化済みとし、次回持ち込み設定は解除した。
- 武器、防具、クエスト品は回収対象外とした。死亡前に取得済みだった落とし物クエスト品は、同じ指定位置で再取得できる状態へ戻すようにした。
- 回収待ちの荷物、費用、死亡リザルト情報をセーブ対象へ追加し、選択前に終了しても次回起動時に死亡リザルトを再表示するようにした。
- セーブバージョンを5へ更新し、直前のバージョン4は回収待ちなしとして読み込めるようにした。

#### 変更意図
- 階級に応じた保険範囲を死亡ペナルティへ反映し、探索中の荷物を費用と引き換えに取り戻す判断を死亡時に発生させるため。
- アプリ終了で回収選択を回避したり荷物状態が消失したりしないよう、未解決状態を永続化するため。

#### 検証
- 奈落塔とNarakuEditorのDebug x64・Release x64をビルドし、回収処理とセーブバージョン5のコードが全構成でコンパイル・リンクできることを確認した。
- ゲーム側とEditor側の共有ソース一致、および15段階生成の一致テストが成功することを確認した。

#### 未検証
- 実際の死亡操作による階級範囲判定、回収・破棄選択、借金加算、選択前終了後の再表示は手動確認していない。

### 緊急クエスト・昇格試験

#### 変更内容
- 依頼受付へ「緊急クエスト」タブを追加し、青印・黒印・紫印への昇格試験を手動受注できるようにした。
- 青印はLv20かつ第三層到達、黒印はLv35かつ第四層到達、紫印はLv55かつ第五層到達を解放条件とした。
- 受注後に指定深度で新規取得し、生還時に所持している換金用遺物と強化系遺物だけを判定するようにした。必要数は青3/1、黒2/5、紫3/10とした。
- 条件を満たした生還後は受付で報告可能とし、報告時に遺物を消費せず、階級、金、EXP、3種類から均等抽選した強化系遺物を付与するようにした。
- 必要数未達での通常帰還、死亡、探索放棄、潜行中のアプリ終了を失敗とし、次のゲーム内週が始まるまで再受注できないようにした。
- 現在階級の保険対象より深い親深度へ初めて降りる際に警告を表示し、承認した潜行中だけ再表示を抑止するようにした。キャンセル後に再び層間口を調べた場合は再表示する。
- 昇格試験の状態、受注時の取得番号、進捗、週内失敗ロックをセーブ対象へ追加した。セーブバージョンを6へ更新し、バージョン5は昇格試験未開始として移行するようにした。

#### 変更意図
- 重要クエストによる到達進行と階級別の死亡保険を、プレイヤーが選んで挑む昇格試験で接続するため。
- 受注前の在庫や別深度の遺物を流用できないようにし、指定深度から必要物資を持って生還すること自体を試験にするため。

#### 検証
- 奈落塔とNarakuEditorのDebug x64・Release x64をビルドし、昇格試験、警告画面、セーブバージョン6のコードが全構成でコンパイル・リンクできることを確認した。
- ゲーム側とEditor側の共有ソース一致、および15段階生成の一致テストが成功することを確認した。

#### 未検証
- 実操作による各試験の解放、受注、深度別取得判定、帰還失敗、翌週復帰、報告・昇格、保険対象外警告は手動確認していない。

### ゲーム内時間倍率とDebugエリアワープ

#### 変更内容
- 地上で実時間30分がゲーム内1日となるよう、ゲーム内時計の基準速度を48倍へ変更した。
- 探索中のゲーム内時計には、48倍の基準速度へ従来の親深度別時間倍率を追加で適用するようにした。
- 受注中クエストの期限と完了後の再生成待ち5分は、従来どおり実時間で進行する仕様を維持した。
- Debug版の奈落塔に、親層・上中下層・エリア番号を指定して生成計画上のエリアへ直接ワープできる機能を追加した。
- 未生成のワープ先は既存の本番用生成処理で生成し、入口があれば入口、なければ開始地点へ配置するようにした。
- デバッグワープでは通常の層間遷移を介さず、探索EXP、初到達報酬、保険対象外警告を発生させないようにした。

#### 変更意図
- 一週間単位の依頼更新を実時間の長期間待たず確認でき、深度別の時間進行差も体感できるゲーム内時計にするため。
- 15段階の任意地点を、通常の接続順をすべて辿らずDebugビルドで検証できるようにするため。

#### 検証
- 奈落塔とNarakuEditorのDebug x64・Release x64をビルドし、全4構成がエラーなしで完了することを確認した。
- ゲーム正本とEditor側の共有ソースに同期差分がないことを確認した。
- 15段階生成一致テストが成功し、段階数、接続ID、小ステージ配置IDがゲーム側とEditor側で一致することを確認した。
- `git diff --check`で空白エラーがないことを確認した。

#### 未検証
- 実操作による30分＝1日換算、深度倍率、各ワープ先の生成・配置は手動確認していない。

### 週次ワールド状態と採掘地点の復活

#### 変更内容
- 同じゲーム内週では、15段階の接続構成、生成済みマップ、敵、採掘地点、地面の遺物・食料、発見情報、ピンを引き継ぐ週次ワールド状態を追加した。
- 週次ワールドを `Assets/Save/WeeklyWorld` へ保存し、アプリ再起動後も同じ週の生成結果と変化状態を復元するようにした。
- 生還、死亡、探窟放棄でワールドを再抽選せず、敵の復活残り時間、採掘地点の復活残り時間、週内の探索情報を維持するようにした。
- 採掘済み地点は実時間10分で復活させ、採掘結果が地面に残っている間は復活させないようにした。
- 採掘地点ごとの採掘回数と週シードから次の遺物を決定し、再起動で同じ採掘結果を引き直せないようにした。拾得または放置を選んだ直後に保存する。
- 敵の5分復活と採掘地点の10分復活は、探索中または地上で操作可能な時間だけ実時間で進めるようにした。所持品、ポーズ相当画面、ロード、層間移動、アプリ終了中は進ませず、深度時間倍率も適用しない。
- ゲーム内週が変わった時点で現在の潜行は維持し、次回潜行開始時に新しい週シードでワールドを再生成するようにした。切替予定は通知、地図、依頼受付に表示する。
- 地図と依頼受付に、月曜日00:00を週の起点とするゲーム内曜日・時刻を表示するようにした。
- セーブバージョンを7へ更新し、直前のバージョン6を週次ワールドなしとして読み込めるようにした。マップ版本が変わった場合は保存済み週次ワールドを使用しない。

#### 変更意図
- 生還や死亡の度にエリア、敵、採掘結果が再抽選される状態をなくし、一週間の奈落を継続して探索する形にするため。
- 再起動やエリア往復による報酬の再抽選・多重取得を防ぎつつ、復活時間をオフライン中に蓄積させないため。

#### 検証
- 奈落塔とNarakuEditorのDebug x64・Release x64をビルドし、全4構成がエラーなしで完了することを確認した。
- ゲーム正本とEditor側の共有ソースに同期差分がないことを確認した。
- 15段階生成一致テストが成功し、段階数、接続ID、小ステージ配置IDがゲーム側とEditor側で一致することを確認した。
- `git diff --check` で空白エラーがないことを確認した。

#### 未検証
- 実操作による週内マップ復元、死亡・放棄後の状態維持、採掘10分復活、未回収遺物による復活待機、週越境後の次回潜行再生成は手動確認していない。

### Debugゲーム内日時変更

#### 変更内容
- Debug版のゲームにある「プレイテスト調整」の進行デバッグへ、曜日・時・分を指定する日時変更UIを追加した。
- 現在時刻より後の日時は同じ週、現在時刻以前の日時は次週の指定日時として扱うようにした。
- 週を越える日時変更では、通常の時計更新と同じ週シード更新、週次依頼更新、次回潜行時のワールド再生成予約を実行するようにした。
- 日時変更では敵の5分復活、採掘の10分復活、受注中依頼の期限、完了後の5分待機を進めず、デバッグ操作によるタイマー蓄積を防いだ。
- 変更後の日時と週状態は即時セーブするようにした。
- UIと操作処理はDebug版の奈落塔だけにコンパイルし、Release版とEditorでは使用できないようにした。

#### 変更意図
- 週次ワールドと週更新の確認を、実時間で数日分待たずに行えるようにするため。
- 日付のみを試験対象とし、復活報酬や依頼期限を意図せず変化させないため。

#### 検証
- 奈落塔のDebug x64・Release x64をビルドし、日時変更処理がDebugだけに入った状態でエラーなしで完了することを確認した。
- NarakuEditorのDebug x64・Release x64を完全再ビルドし、両構成がリンクまで成功することを確認した。
- ゲーム正本とEditor側の共有ソースに同期差分がないことを確認した。
- 15段階生成一致テストが成功した。
- `git diff --check` で空白エラーがないことを確認した。

#### 未検証
- 実操作による同一週内の日時変更、週越境、再起動後の日時保持、および実時間系タイマーが進まないことは手動確認していない。

### Debugカメラ距離調整

#### 変更内容
- Debug版の「プレイテスト調整」にあるカメラ項目で、プレイヤーとカラメの距離をスライダー調整できるようにした。
- 既存の最小距離6.0を下限、変更前の標準距離13.8564を上限とした。
- 距離は従来どおり調整値の保存・読込み対象とし、ホイール操作と同じ正規化処理を使用する。

#### 変更意図
- 現在の視野を最も引いた状態として保ちつつ、Debug画面からカメラをプレイヤーに近づけて確認できるようにするため。

#### 検証
- 奈落塔とNarakuEditorのDebug x64・Release x64をビルドし、全4構成がエラーなしで完了することを確認した。
- ゲーム正本とEditor側の共有ソースに同期差分がないことを確認した。
- 15段階生成一致テストが成功した。
- `git diff --check` で空白エラーがないことを確認した。

#### 未検証
- 実操作によるスライダー操作、ホイールとの併用、調整値保存後の再起動反映は手動確認していない。

## 2026-09-11

### 地上UIの3ペインレイアウト化と商店・武具屋の刷新

#### 変更意図
- 地上施設（自宅、商店、武具屋、レストラン、依頼受付）において、従来は情報が直線的に羅列され下にスクロールしないと見づらかった課題を解消するため。
- 装備情報、次回持込品、施設固有機能、プレイヤー基本ステータスを同一画面内で見渡せるようにするため。
- 商店において、1回ずつしか売買できず大量のアイテムを扱う際の操作負担が大きかったため、一括売買機能（直接入力・増減ボタン）を導入するため。
- 武具屋において、防具と武器が混在し、効果やステータス上昇量、遺物素材の必要情報が把握しづらかったため、タブ切り替え、左右分割での詳細確認、および別ウィンドウでの遺物併用／金のみ購入を選択可能にするため。

#### 変更内容
- **共通3ペインレイアウト (`DrawTownFrame`) の導入**:
  - ウィンドウ内を左（30%）、中（40%）、右（30%）の3ペインに分割。
  - **左ペイン (`DrawTownLeftPane`)**:
    - 装備中の武器、頭防具、胴体防具の名前のみを表示（ステータス上昇量は排他）。
    - 次回持込アイテム一覧を独立したスクロール枠内に箇条書き表示（持込個数、単体重量、合計重量、出撃時総重量/最大重量プレビュー）。
  - **中央ペイン**:
    - 選択中の施設（自宅 / 商店 / 武具屋 / レストラン / 依頼受付）の固有操作領域。
  - **右ペイン (`DrawTownRightPane`)**:
    - プレイヤー情報を指定13項目（Lv、階級、所持金、HP、スタミナ、精神力、重量、満腹度、水分、攻撃力、防御力、スタミナ回復速度、採掘速度、ロープ昇降速度）に絞り、罫線付き2列テーブルで表示。不要な余計な数値を非表示化。
- **商店の一新 (`DrawGeneralShop`, `DrawShopTransactionModal`)**:
  - 「購入」「売却」の2タブ切り替えに対応。
  - 各商品のボタン押下で「取引個数選択」モーダルウィンドウを展開。
  - 直接入力（`ImGui::InputInt`）に加え、増減ボタン（`-100`, `-10`, `-1`, `+1`, `+10`, `+100`）、最大数/最小数ボタンを配置。
  - 下限1、所持金や所持数による上限ガード、取引後の所持金増減プレビュー表示を追加。
- **武具屋の一新 (`DrawArmory`, `DrawArmoryPurchaseModal`)**:
  - 「防具」「武器」の2タブ切り替えに対応。
  - 中央ペイン内で左右2分割（左側: 商品一覧、右側: 選択商品の詳細情報）。
  - 各商品の下に「購入」と「情報を見る」の2ボタンを配置。
  - 右側詳細欄に、部位別の効果説明（頭: 防御/精神力、胴体: HP/防御力、武器: 攻撃力/採掘速度など）、装備時の具体的ステータス上昇量、遺物素材併用時の必要遺物名・所持数・割引価格を表示。
  - 「購入」ボタン押下時に別ウィンドウ（モーダル）を開き、防具部位選択（頭 / 胴体）、必要量（金のみ、または遺物＋金）を表示した上で、「遺物を消費して買う」「金のみを消費して買う」の2択ボタンを提供。

#### 変更ファイル
- `NarakuGame/Source/SceneNarakuProto.h`:
  - 3ペイン共通フレーム (`DrawTownFrame`, `DrawTownLeftPane`, `DrawTownRightPane`) 宣言追加。
  - 商店一括取引状態構造体 (`ShopTransactionState`) およびモーダル関数宣言追加。
  - 武具屋状態構造体 (`ArmoryUIState`) および購入モーダル関数宣言追加。
- `NarakuGame/Source/SceneNarakuProto.cpp`:
  - 3ペイン共通描画の実装および全地上施設（自宅・商店・武具屋・レストラン・依頼受付）の共通フレーム化。
  - 商店の2タブ化および一括売買モーダル・取引ロジックの実装。
  - 武具屋の2タブ化、左右分割表示、購入モーダル（遺物消費／金のみ選択）の実装。
- `NarakuEditor/Source/SceneNarakuProto.h`, `NarakuEditor/Source/SceneNarakuProto.cpp`:
  - `Tools/Sync-SeparatedSources.ps1` による共通ソースの同期反映。

#### 検証
- `Tools/Sync-SeparatedSources.ps1` を実行し、`NarakuGame` の変更が `NarakuEditor` へ完全に同期されることを確認した。
- `MSBuild` により `DX22_Project.sln`（Debug x64）をビルドし、`奈落塔.exe`、`NarakuEditor.exe`、`DX22_Project.exe` の全プロジェクトがコンパイルエラー・リンクエラーなしでビルド成功することを確認した。
- ビルド後のアセットステージング（`Tools/Stage-RuntimeAssets.ps1`）が正常に完了することを確認した。
- `git diff --check` で不要な空白・改行エラーがないことを確認した。

#### 未検証
- 実操作による地上3ペインレイアウトの視認性、商店の一括売買モーダル挙動、武具屋の購入モーダル（遺物併用/金のみ）および防具部位選択の動作確認は手動確認していない。

### 地上施設切り替え時のウィンドウ位置・サイズ固定化

#### 変更意図
- 地上施設（自宅、商店、武具屋、レストラン、依頼受付）を切り替えるたびに、ImGui内部で異なるウィンドウとして識別され、ウィンドウの位置やサイズが初期化・変動してしまう問題を解消するため。
- ユーザーが手動でリサイズや位置調整を行った際、そのサイズと位置を保持したまま施設間を自由に移動できるようにするため。

#### 変更内容
- `SceneNarakuProto.cpp` の `DrawTownFrame` において、`ImGui::Begin` に渡すウィンドウ文字列に `###TownMainWindow` のID overrideを付与。
- 表示上のタイトル名（自宅・商店・武具屋等）は維持したまま、ImGui内部のウィンドウIDを全地上施設で統一。
- これにより、施設を切り替えても同一ウィンドウとして認識され、位置・サイズが完全に保持されるようにした。
- `Tools/Stage-RuntimeAssets.ps1` において、`Backup` フォルダの除外および既存と同一サイズのファイル・DLLの上書きコピーをスキップする判定を追加し、開発実行中のファイルロックによるビルド失敗を防止。

#### 変更ファイル
- `NarakuGame/Source/SceneNarakuProto.cpp`:
  - `DrawTownFrame` 内の `ImGui::Begin` 呼び出しに `###TownMainWindow` を付与。
- `NarakuEditor/Source/SceneNarakuProto.cpp`:
  - `Tools/Sync-SeparatedSources.ps1` による共通ソースの同期反映。
- `Tools/Stage-RuntimeAssets.ps1`:
  - 不要なバックアップ除外および同一サイズファイルの重複コピー回避。

#### 検証
- `Tools/Sync-SeparatedSources.ps1` によるエディタ側へのソース同期完了。
- `MSBuild` により全プロジェクト（`奈落塔.exe`、`NarakuEditor.exe`、`DX22_Project.exe`）の Debug x64 ビルドおよびアセットステージングがエラーなしで完了することを確認。
- `git diff --check 開発記録帳.md` による空白・改行チェック完了。

#### 未検証
- 実操作による各種地上施設間を行き来した際のウィンドウ位置・サイズ保持の体感確認。

### プレイヤー情報表下へのゲーム内日時表示

#### 変更意図
- 地上ウィンドウの右ペイン（プレイヤー情報）において、ステータス表の下に現在のゲーム内日時（曜日・時刻）を配置し、現在時刻や週更新の把握を容易にするため。

#### 変更内容
- `SceneNarakuProto.cpp` の `DrawTownRightPane` において、指定13項目のステータステーブルの直下にゲーム内日時表示を追加。
- `m_gameWeekSeconds` から現在の曜日（月〜日）および時刻（時:分）を算出し、「【日時】 ○曜日 HH:MM」として表示。
- 週更新フラグ（`m_weekResetPending`）が立っている場合は、その直下に注意喚起テキスト（※次回潜行時に新しい週へ更新されます）を表示。

#### 変更ファイル
- `NarakuGame/Source/SceneNarakuProto.cpp`:
  - `DrawTownRightPane` 内のテーブル直下にゲーム内日時表示処理を追加。
- `NarakuEditor/Source/SceneNarakuProto.cpp`:
  - `Tools/Sync-SeparatedSources.ps1` による共通ソースの同期反映。

#### 検証
- `Tools/Sync-SeparatedSources.ps1` によるエディタ側へのソース同期完了。
- `MSBuild` により全プロジェクト（`奈落塔.exe`、`NarakuEditor.exe`、`DX22_Project.exe`）の Debug x64 ビルドおよびアセットステージングがエラーなしで完了することを確認。
- `git diff --check 開発記録帳.md` による空白・改行チェック完了。

#### 未検証
- 実操作による右ペイン下でのゲーム内日時の視認性確認。

### 2026-09-11 地上固定マップ・施設配置・直通門の実装

#### 変更意図
- 施設をメニュー間の直接遷移ではなく、地上マップを歩いて訪れるゲーム内導線へ変更するため。
- 小ステージEditorで地上用区画と施設位置を編集し、同じデータをゲーム側の固定地上マップへ反映できるようにするため。
- 奈落塔入口から通常潜行と階級限定の直通潜行を選べるようにし、週単位の利用料金上昇をゲーム進行へ組み込むため。
- マップと所持品の操作キーを整理し、食料使用を所持品画面へ統一するため。

#### 変更内容
- 地上専用の8x8小ステージ形式、地上フラグ、施設種別、自施設の2x2配置セル、正面方向、相対モデルパス、位置・拡大率・90度単位回転を小ステージデータへ追加した。
- 小ステージEditorへ地上用8x8切替と施設設定UIを追加し、施設範囲への採掘地点・環境物配置を禁止した。プレビューでは施設範囲を線枠で表示する。
- 自宅、商店、武具屋、レストラン兼受注受付、奈落塔出入口の5区画を固定配置した32x16セル相当の地上マップを追加した。
- 地上では施設正面でFキーを押して各UIを開く。施設UIを閉じると元の施設前へ戻り、新規開始・ロード・死亡後は自宅、生還後は奈落塔出入口へ戻るようにした。
- 地上では戦闘・採掘・空腹・水分・精神力・上昇負荷・危険処理を停止し、移動中だけゲーム内時間を進めるようにした。施設、マップ、所持品を開いている間は時間と復活タイマーを停止する。
- レストランと受注受付を同じ施設UIのタブとして統合し、受注受付の解放条件は維持した。
- 奈落塔出入口UIへ通常潜行、黒印以上の第二層上層直通、紫印以上の第三層上層直通を追加した。直通料金は週内の行き先別利用回数ごとに250G増え、週更新で利用回数と到着先をリセットする。
- 直通先は上層内の歩行可能で平坦な2x2領域を優先し、層間口・採掘地点・敵・湖を避けて配置する。確保できない場合は1セルへ縮退し、それも失敗した場合は料金を消費せず入口へ戻して理由を表示する。
- Eキーをマップ、Iキーを所持品へ変更し、TキーとEキーによる食料即時使用を廃止した。地上の所持品画面では食料使用ボタンを表示しない。
- 地上地図に施設位置と説明を表示し、地上用ピンを奈落内のピンと分離して保存するようにした。
- セーブ形式をv8へ更新し、v7を読み込み可能な直前形式として維持した。地上ピン、週内直通利用回数、直通先エリアと到着位置を保存対象へ追加した。
- 自宅モデルを相対パス `Assets/Model/ground/shack7.fbx` で登録し、ゲーム・Editorの実行先へ地上区画データと共に配置するようにした。

#### 変更ファイル
- `NarakuGame/Source/NarakuPieceData.h`, `NarakuGame/Source/NarakuPieceData.cpp`
- `NarakuGame/Source/NarakuMapData.h`
- `NarakuGame/Source/SceneNarakuProto.h`, `NarakuGame/Source/SceneNarakuProto.cpp`
- `NarakuEditor/Source/SceneNarakuPieceEditorDocument.cpp`
- `NarakuEditor/Source/SceneNarakuPieceEditorEditing.cpp`
- `NarakuEditor/Source/SceneNarakuPieceEditorPreview.cpp`
- `NarakuEditor/Source/SceneNarakuPieceEditorWindows.cpp`
- `NarakuEditor/Source/SceneNarakuProto.h`, `NarakuEditor/Source/SceneNarakuProto.cpp`
- `Assets/Naraku/Pieces/Surface/Completed/surface_home.json`
- `Assets/Naraku/Pieces/Surface/Completed/surface_shop.json`
- `Assets/Naraku/Pieces/Surface/Completed/surface_armory.json`
- `Assets/Naraku/Pieces/Surface/Completed/surface_restaurant.json`
- `Assets/Naraku/Pieces/Surface/Completed/surface_abyss_entrance.json`
- `Assets/Naraku/environment_models.cfg`
- `Assets/Model/ground/shack7.fbx`

#### 検証
- `NarakuGame` Debug x64 / Release x64をビルドし、警告0・エラー0で成功した。
- `NarakuEditor` Debug x64 / Release x64をビルドし、エラーなく成功した。
- `Tools/Sync-SeparatedSources.ps1 -CheckOnly` でゲーム側共有ソースとEditor側コピーの一致を確認した。
- `Tools/Test-SeparatedGenerationParity.ps1` で段階数、接続ID、小ステージ配置IDの生成一致を確認した。
- Debugゲーム実行先に地上区画JSON、自宅モデル、モデルカタログが配置されることを確認した。
- `git diff --check` で差分の空白エラーがないことを確認した。

#### 未検証
- ゲーム実操作による5施設間の歩行、Fキー操作、地上地図、E/Iキー切替、直通門の料金・階級制限・到着地点の確認。
- 小ステージEditor実操作による地上8x8区画の編集、施設設定、保存、線枠プレビューの確認。

### 2026-09-11 操作説明書の作成

#### 変更意図
- プロジェクトルート（.sln と同一階層）にゲーム本編およびエディタの操作方法を網羅した利用者・開発者向けマニュアルを設置するため。

#### 変更内容
- `操作説明.md` を作成。
  - ゲーム本編（探索・インゲーム）のキーボード＆マウス操作、ゲームパッド（XInput / DirectInput）操作一覧。
  - 最新の入力仕様（E: 地図、I: 所持品、Q: 精神力技、F: インタラクト、F11: フルスクリーン等）を反映。
  - 地上拠点（街・施設）の歩行・入場操作。
  - 地形・ピースエディタ（NarakuEditor）のカメラ操作、編集ショートカット。
  - サバイバル要素（上昇負荷・重量・スタミナ）の注意点とクイックリファレンス表を収録。

#### 変更ファイル
- `操作説明.md` (新規追加)
- `開発記録帳.md`

### 2026-09-11 E/Iキー入力・モデル部品散らばり・施設プレビューの不具合修正

#### 原因
- E/Iキーで画面が開かない問題:
  `SceneNarakuProto.cpp` で地上や探索中にE/Iキーをトリガー判定して `Mode::Inventory` へ遷移させた直後、同一フレーム内で直後の `if (m_mode == Mode::Inventory)` ブロックが同一キー入力を再度検出してしまい、即座に元の画面へ復帰していたため一瞬で開閉されていた。地上地図の確認不能もこの問題に起因していた。
- Editorで環境モデル部品が散らばる問題:
  FBX内のメッシュ頂点に対してノード階層変換（平行移動・回転・拡大）を頂点に焼き込まず、通常シェーダーで描画していたため、FBX内部で複数ノードに分割されている小屋などの部品が離れた原点位置に配置されていた。
- 小屋モデルのテクスチャが表示されない問題:
  テクスチャ群が `NarakuEditor/Assets/Model/ground/textures/` にのみ存在し、共通Assetsおよびゲーム実行先Assetsへ配置されていなかった。また、`_model.cpp` のマテリアル読込探索処理が `texture`（単数形）しか検索しておらず、`textures`（複数形）フォルダに対応していなかった。
- 施設の向き・回転を視覚的に調整できない問題:
  小ステージプレビューで施設モデルを描画しておらずワイヤーボックスのみを表示していたため、位置補正・拡大率・Y回転の結果を確認できなかった。また、正面方向を示す矢印がなく、接続設定ウィンドウの高さが小さく設定UIが隠れやすかった。

#### 変更意図
- 地上でEキー（地図）およびIキー（所持品）が確実に開き、同一キーやESCで正常に開閉・切替できるようにするため。
- アニメーションモデルの挙動を破壊することなく、静的環境モデル専用にFBXノード階層変換を頂点へ焼き込んで一体化して描画するため。
- 小屋等のテクスチャを全実行先で正常に解決・表示できるようにするため。
- 小ステージEditor上で施設モデル、位置補正、拡大率、Y回転、正面矢印を即時視覚確認できるようにするため。

#### 変更内容
- `NarakuGame/Source/SceneNarakuProto.cpp`:
  - E/Iキー入力後のインベントリ判定を `else if (m_mode == Mode::Inventory)` に変更し、同一フレーム内多重判定を防止。
  - `LoadEnvironmentModels` で `Model::LoadStatic` を使用するように変更。
- `NarakuGame/Source/Model.h`, `NarakuGame/Source/Model.cpp`:
  - 静的モデル専用の `LoadStatic` を追加し、Assimpの `aiProcess_PreTransformVertices` フラグによりノード階層変換を頂点座標へ焼き込むように実装（通常のアニメーションモデル読込 `Load` には影響なし）。
- `NarakuGame/Source/_model.cpp`:
  - テクスチャ検索パスへ `textures\` サブフォルダ探索を追加。
- `Assets/Model/ground/textures/`:
  - `Bluetarp.jpg`, `WoodFrame.jpg`, `metal1.jpg` 〜 `metal4.jpg` を共通Assetsおよび各プロジェクトAssetsへ配置。
- `NarakuEditor/Source/SceneNarakuPieceEditorEnvironment.cpp`:
  - 環境モデルカタログ読込、プレビューポップアップ読込、新規登録時において `LoadStatic` を使用するように変更。
- `NarakuEditor/Source/SceneNarakuPieceEditorPreview.cpp`:
  - 拠点モデルプレビューで `LoadStatic` を使用するように変更。
  - 地上施設設定がある場合、環境モデルカタログまたは直接読込で施設モデルを取得し、位置補正・拡大率・Y回転を即時反映して3D描画するように拡張。
  - 施設正面（North/South/East/West）を示す矢印（軸線とハの字矢羽）を3Dプレビュー上に描画。
- `NarakuEditor/Source/SceneNarakuPieceEditorWindows.cpp`:
  - 接続設定ウィンドウの初期サイズを縦長（380x420）に拡張し、地上施設設定領域をスクロール可能な枠（`BeginChild`）内に配置して全項目が隠れないように改善。
- `Tools/Sync-SeparatedSources.ps1`:
  - 共有ファイルを同期し、Editor側ソースとの一致を確認。

#### 変更ファイル
- `NarakuGame/Source/SceneNarakuProto.cpp`
- `NarakuGame/Source/Model.h`
- `NarakuGame/Source/Model.cpp`
- `NarakuGame/Source/_model.cpp`
- `NarakuEditor/Source/SceneNarakuProto.cpp` (同期)
- `NarakuEditor/Source/Model.h` (同期)
- `NarakuEditor/Source/Model.cpp` (同期)
- `NarakuEditor/Source/_model.cpp` (同期)
- `NarakuEditor/Source/SceneNarakuPieceEditorEnvironment.cpp`
- `NarakuEditor/Source/SceneNarakuPieceEditorPreview.cpp`
- `NarakuEditor/Source/SceneNarakuPieceEditorWindows.cpp`
- `Assets/Model/ground/textures/*`
- `開発記録帳.md`

#### 検証
- `Tools/Sync-SeparatedSources.ps1 -CheckOnly`: 差分なしで同期成功。
- `Tools/Test-SeparatedGenerationParity.ps1`: 段階数、接続ID、小ステージ配置IDの生成一致を確認。
- `NarakuGame` (Debug|x64): エラー0でビルド成功、Assetsステージング成功。
- `NarakuGame` (Release|x64): エラー0でビルド成功、Assetsステージング成功。
- `NarakuEditor` (Debug|x64): エラー0でビルド成功、Assetsステージング成功。
- `NarakuEditor` (Release|x64): エラー0でビルド成功、Assetsステージング成功。

### 2026-09-11 施設モデル読込とプレビューキャッシュの追加修正

#### 変更意図
- 日本語を含むプロジェクトパスでも、地上施設の静的FBXを通常モデルと同じ経路で確実に読み込めるようにするため。
- 小ステージを開き直した場合や、同一パスの施設モデルを更新した場合に、古いプレビューや解放されないモデル資源が残らないようにするため。

#### 変更内容
- `NarakuGame/Source/Model.cpp`:
  - Assimp用のUTF-8／ワイド文字変換とワイド文字対応I/Oを復元し、`Load`と`LoadStatic`が共通のパス読込処理を使用するようにした。
  - `LoadStatic`からAssimpの`ReadFile`を直接呼ばず、日本語を含む絶対パスと関連ファイルを扱える読込処理を経由するように修正した。
- `NarakuEditor/Source/SceneNarakuPieceEditor.h`, `SceneNarakuPieceEditor.cpp`, `SceneNarakuPieceEditorDocument.cpp`, `SceneNarakuPieceEditorPreview.cpp`:
  - 地上施設の直接指定モデルを、関数内staticではなくシーン所有のキャッシュへ変更した。
  - シーン破棄、新規作成、小ステージ読込時にキャッシュを解放するようにした。
  - モデルファイルの最終更新時刻を記録し、同じパスのファイルが更新された場合も再読込するようにした。
- `Tools/Sync-SeparatedSources.ps1`:
  - 共通ソースを同期し、ゲーム側とEditor側の`Model.cpp`を一致させた。

#### 検証
- `NarakuGame` Debug x64 / Release x64をビルドし、エラー0で成功した。
- `NarakuEditor` Debug x64 / Release x64をビルドし、エラー0で成功した。
- `Tools/Sync-SeparatedSources.ps1 -CheckOnly`で共有ソースの一致を確認した。
- `git diff --check`は終了コード0。既存ファイルのLFからCRLFへの変換警告のみだった。

#### 未検証・既知事項
- 実際のEditor画面で、日本語パスにある施設モデルの表示、同一パス上書き後の再読込、複数小ステージ切替時の見た目は未確認。
- `Tools/Test-SeparatedGenerationParity.ps1`は、段階別エリア数・接続ID・小ステージ配置IDの不一致により終了コード1となった。今回修正したモデル読込・プレビューキャッシュとは別領域のため、この作業では生成処理を変更していない。

### 2026-09-11 第二拠点・前衛拠点の追加仕様整理

#### 変更意図
- 奈落内拠点のサービス、専用品、曜日別売却倍率、時間経過を実装前に仕様書へ固定し、地上レストランの既存仕様と区別するため。
- 未実装の釣り機能を後続作業として追跡できるようにするため。

#### 変更内容
- `NarakuProto_追加要素仕様書.md`へ第二拠点と前衛拠点の専用UI、価格、宿泊、売却、専用品、装備効果、上昇負荷防御優先順位を追記した。
- 地上レストランは既存仕様を維持し、200G・HP／満腹度80%回復・精神力回復なしの食事は前衛拠点専用であることを明記した。
- 判断不能な日曜日の売却倍率、魚の回復対象などは未確定事項として分離した。
- `タスクリスト.md`へ第五層の釣り機能と「見たことない魚」の釣果追加を登録した。

#### 追加確定（2026-09-12）
- 第二拠点の食事を地上レストランと同一回復量・精神力回復ありとし、無料給水は直接飲用と水筒補充の両方へ対応する仕様に確定した。
- 宿泊による午前7時までの時間経過中も、クエスト期限、週更新、採掘地点復活、敵再出現を進行させる仕様に確定した。
- 日曜日の0.75倍対象を換金用遺物へ訂正し、曜日別売却表の矛盾を解消した。
- 前衛拠点の無料給水、未知の武器の壁貫通、魚の満腹度・精神力回復量、重量、売値を確定した。
- 現在のセーブバージョン8を確認し、拠点機能実装時はバージョン9へ更新する仕様に確定した。

#### 変更ファイル
- `NarakuProto_追加要素仕様書.md`
- `タスクリスト.md`
- `開発記録帳.md`

### 2026-09-12 第二拠点・前衛拠点機能の実装

#### 変更意図
- 生成済みの第二拠点と前衛拠点を、発見対象だけでなく深層探窟を支える利用可能な施設にするため。
- 拠点専用品、装備、武器、持続効果をセーブ・死亡回収・持ち込みの既存ループへ統合するため。

#### 変更内容
- 拠点から一定距離内でFキーを押すと、第二拠点または前衛拠点の専用UIを開くようにした。
- 第二拠点に食事、無料給水、次の午前7時までの宿泊、曜日別倍率による所持遺物売却を追加した。宿泊中は週更新、クエスト、採掘地点、敵の復活時間も進める。
- 前衛拠点に地上と同価格の商品・武具、専用品、200Gの食事、無料給水を追加した。通常遺物の売却と、潜行中に所持する強化素材を使った素材併用購入にも対応した。
- 行動食1号の5分間満腹度減少無効と30秒間水分消費1.25倍、魚の生食・調理、未調理500G・調理済み400Gの売却を追加した。
- 未知の頭・胴を同時装備した時のセット効果と、致命的な第六・第七層の上昇負荷に対するカートリッジ優先防御を追加した。
- 未知の武器に1秒溜め、壁無視の前方直線判定、全敵即死、通常攻撃の2倍のスタミナ消費、5秒の再使用待ちを追加した。
- セーブ形式をv9へ更新し、v8を1つ前の形式として読み込む変換処理と、新規アイテム・持続時間・死亡回収状態の保存を追加した。
- ゲーム側の共通ソースをEditor側へ同期した。

#### 変更ファイル
- `NarakuGame/Source/SceneNarakuProto.h`
- `NarakuGame/Source/SceneNarakuProto.cpp`
- `NarakuEditor/Source/SceneNarakuProto.h`
- `NarakuEditor/Source/SceneNarakuProto.cpp`
- `NarakuProto_追加要素仕様書.md`
- `タスクリスト.md`
- `開発記録帳.md`

#### 検証
- `NarakuGame` Debug x64 / Release x64をビルドし、エラー0で成功した。
- `NarakuEditor` Debug x64 / Release x64をビルドし、エラー0で成功した。
- `Tools/Sync-SeparatedSources.ps1 -CheckOnly`で共有ソースの一致を確認した。
- `Tools/Test-SeparatedGenerationParity.ps1`で、段階数、接続ID、小ステージ配置IDの一致を確認した。

#### 未検証・既知事項
- 実プレイ上での拠点へのFキー入場、宿泊の時間跳躍、売却倍率、専用装備・武器の操作感は手動確認が必要。
- 行動食1号とカートリッジの重量は、既存の小型消耗品に合わせて暫定1としている。

### 2026-09-12 小ステージEditor 3Dプレビューの東西軸修正

#### 変更意図
- 高さグリッドとゲーム側に対し、小ステージEditorの3Dプレビューだけ東西方向の基準が反転していた問題を解消するため。

#### 根本原因
- ゲーム側の水平カメラ位置は`X=sin(yaw), Z=cos(yaw)`であるのに対し、小ステージEditorは`X=cos(yaw), Z=sin(yaw)`を使用していた。
- Editor側はマウスX移動の符号も反転し、カメラ操作感だけを補正していたため、ヨー角と東西軸の定義がゲーム側と一致していなかった。

#### 変更内容
- 3Dプレビューのカメラ位置、前方ベクトル、パン操作で使用するヨー角定義をゲーム側と同じ`X=sin(yaw), Z=cos(yaw)`へ統一した。
- Alt+左ドラッグの水平回転は、ゲーム側と同じ符号でヨー角を更新するようにした。
- 初期ヨー角もゲーム側と同じ45度にし、同じヨー角でEditorとゲームが同じ方向から地形を見るようにした。
- 地形、高さ、セル、配置物の保存座標は変更していない。

#### 変更ファイル
- `NarakuEditor/Source/NarakuPieceEditorInternal.h`
- `NarakuEditor/Source/SceneNarakuPieceEditorPreview.cpp`
- `NarakuEditor/Source/SceneNarakuPieceEditorEnvironment.cpp`
- `開発記録帳.md`

#### 検証
- `NarakuEditor` Release x64はエラー0でビルド成功した。
- Debug x64は全ソースのコンパイルまで成功したが、起動中の`Output/Debug/Editor/NarakuEditor.exe`を上書きできずリンクのみ失敗した。

#### 未検証・既知事項
- 起動中のEditor画面は修正前のDebug実行ファイルである。Editorを終了してDebugビルドし直した後、東西コンパス、高さグリッドと異なる高さの配置、東西端の配置物を手動確認する必要がある。

### 2026-09-12 小ステージEditor設定UI復旧・東西鏡映修正

#### 変更意図
- 地上小ステージ、深度・上中下層タグ、奈落内拠点、水場属性のデータと処理は存在する一方、編集UIへ接続されていなかった状態を解消するため。
- 右上コンパスの東西表示だけでなく、地形・配置物・選択判定を含む3Dプレビュー全体を同じ東西基準へ統一するため。

#### 根本原因
- 設定用ラベルと保存項目の追加後、基本情報・新規作成・接続設定・配置ツールから対応値を編集するUIが欠落していた。
- 前回の修正はゲームとEditorのカメラ角度定義だけを統一しており、セルX座標から3DプレビューのワールドX座標へ変換する箇所を鏡映していなかった。

#### 変更内容
- 新規作成画面へ「奈落16x16／地上8x8」の選択を追加し、地上を選んだ場合は地上用データと保存先を使用するようにした。
- 基本情報へ「指定なし／第一層～第七層」の奈落階層と「指定なし／上層／中層／下層」のタグを復旧した。
- 接続設定へ第二拠点／前衛拠点の拠点種別を復旧し、ステージ役割との整合を同時に保つようにした。
- 配置ツールへ水場属性の解除／溜り／池／湖を復旧した。
- 3Dプレビューの頂点X座標を共通変換箇所で鏡映した。地形、セル、採掘地点、ロープ、開始・帰還地点、層間口、拠点、地上施設、環境物、ホバー・選択判定が同じ変換を使用する。
- コンパスの東西ベクトル、地上施設の東西正面、X位置補正、Y回転、拠点モデルのX位置補正も鏡映後の座標へ合わせた。
- 既存JSONのセル座標とゲーム側の生成データは変更していない。

#### 変更ファイル
- `NarakuEditor/Source/SceneNarakuPieceEditor.h`
- `NarakuEditor/Source/SceneNarakuPieceEditor.cpp`
- `NarakuEditor/Source/SceneNarakuPieceEditorDocument.cpp`
- `NarakuEditor/Source/SceneNarakuPieceEditorEditing.cpp`
- `NarakuEditor/Source/SceneNarakuPieceEditorPreview.cpp`
- `NarakuEditor/Source/SceneNarakuPieceEditorWindows.cpp`
- `開発記録帳.md`

#### 検証
- `NarakuEditor` Debug x64 / Release x64をビルドし、エラー0で成功した。
- `Tools/Test-SeparatedGenerationParity.ps1`で段階数、接続ID、小ステージ配置IDの一致を確認した。
- 欠落していた4組の設定用ラベルがすべてUIから参照されていることを静的検索で確認した。
- `git diff --check`は終了コード0。既存ファイルのLFからCRLFへの変換警告のみだった。

#### 未検証・既知事項
- 東西非対称の高さ・配置物を持つ小ステージで、コンパス、3Dプレビュー、高さグリッド、ゲーム表示を実画面比較する必要がある。
- 新規地上8x8作成後の施設設定、奈落階層・上中下層・拠点・水場UIは実画面操作確認が必要。

### 2026-09-13 第五層釣りシステム

#### 変更意図
- 池と湖を移動制約だけでなく、第五層固有の食料・売却資源として利用できるようにするため。
- 釣り地点を小ステージデータから本番生成へ一貫して渡し、週内の使用回数を探索結果として保持するため。

#### 変更内容
- 小ステージEditorへ釣り地点ツールを追加し、歩行可能な岸セルと隣接する池・湖セルをJSONへ保存するようにした。
- 生成Mapへ釣り地点の位置、水場区分、IDを渡し、ゲーム側でFキーの開始確認、5～15秒の待機、タイミング入力、1秒の引き上げを実装した。
- 1地点5回、ゲーム内10分で1回復、7.5m発見、地図表示、行動・被弾による中断、最大スタミナ10%消費、重量30の事前空き判定を追加した。
- 池60/25/15、湖30/40/30のサイズ抽選と、第五層の湖で先行する5%の「見たことない魚」抽選を追加した。
- 小・中・大の生食・調理・重量・売却処理、自宅保管、持ち込み、死亡時回収を追加した。
- セーブ形式をv10へ更新し、v9を直前形式として読み込むようにした。釣り地点状態は週内エリア保存へ追加した。

#### 検証
- `NarakuGame` Debug x64をビルドし、警告0・エラー0で成功した。
- `NarakuEditor` Debug x64をビルドし、エラー0で成功した（既存のsize_t変換警告7件）。

#### 未検証・既知事項
- 実画面で釣り地点を含む小ステージを作成・発行し、第五層で各入力、中断、抽選、回復タイマー、地図表示を確認する必要がある。

### 2026-09-14 釣り地点の表示・配置操作改善

#### 変更意図
- 釣り地点の配置場所を3Dプレビューで判別しやすくし、水場属性を設定したセルから直感的に配置できるようにするため。
- 池・湖の周囲が溜りの場合でも、その溜りを岸として釣り地点を登録できるようにするため。

#### 変更内容
- 3Dプレビューの釣り地点を、岸の竿、水面の浮き、両者を結ぶ糸で表示するようにした。選択時は黄色かつ拡大表示する。
- 池・湖セルをクリックすると、北、東、南、西の順で配置可能な未使用の陸地・溜りを探して岸として自動配置するようにした。
- 配置済み、歩行不可、削除済み、採掘地点や環境オブジェクトと重なる岸は自動選択から除外した。
- 配置可能な岸がない場合は、その旨をEditor上へ表示するようにした。
- 岸セルを直接クリックする従来の配置操作を維持し、その場合の対象水面も北、東、南、西の順で決定するようにした。
- 小ステージデータ検証で歩行可能な溜りを岸として許可した。釣り対象は池・湖だけとし、岸との直接隣接を必須とする。

#### 変更ファイル
- `NarakuEditor/Source/SceneNarakuPieceEditorEditing.cpp`
- `NarakuEditor/Source/SceneNarakuPieceEditorPreview.cpp`
- `NarakuEditor/Source/SceneNarakuPieceEditorWindows.cpp`
- `NarakuGame/Source/NarakuPieceData.cpp`
- `NarakuGame/Source/NarakuPieceData.h`
- `開発記録帳.md`

#### 検証
- `NarakuEditor` Release x64はエラー0でビルド成功した。
- `NarakuEditor` Debug x64は変更コードのコンパイルまで完了したが、起動中の`NarakuEditor.exe`を上書きできずリンクに失敗した。既存のsize_t変換警告7件が残る。
- `NarakuGame` Debug x64 / Release x64をビルドし、エラー0で成功した。Debugには既存のsize_t変換警告7件が残る。
- 変更対象ファイルの`git diff --check`は終了コード0。既存ファイルのLFからCRLFへの変換警告のみだった。

#### 未検証・既知事項
- Editor実画面で池・湖をクリックし、陸地・溜りへの岸配置、優先順、選択表示、保存後の再読込を確認する必要がある。

### 2026-09-14 釣り地点の配置整合性保護

#### 変更意図
- 配置済みの釣り地点をセル属性変更や別オブジェクトの配置で不正状態にできる問題を防ぐため。
- 複数セル編集で一部だけが変更され、編集結果が分かりにくくなることを防ぐため。

#### 変更内容
- 釣り地点の岸セルは削除、歩行不可化、池・湖への変更をできないようにした。
- 釣り地点の対象水面は削除、通常セル・溜りへの変更をできないようにした。
- 岸の陸地と溜りの切り替え、対象水面の池と湖の切り替えは許可した。
- 釣り地点の岸または対象水面への環境オブジェクト配置と、岸への別のグリッド配置物を拒否するようにした。
- 複数セルの属性変更は、釣り地点を不正にするセルが含まれていた場合に変更全体を中止するようにした。
- 拒否時は変更しなかった理由をEditorのメッセージへ表示するようにした。
- 小ステージデータ検証へ、釣り地点の対象水面が削除セルではないことを追加した。

#### 変更ファイル
- `NarakuEditor/Source/SceneNarakuPieceEditor.h`
- `NarakuEditor/Source/SceneNarakuPieceEditor.cpp`
- `NarakuEditor/Source/SceneNarakuPieceEditorEditing.cpp`
- `NarakuEditor/Source/SceneNarakuPieceEditorEnvironment.cpp`
- `NarakuGame/Source/NarakuPieceData.cpp`
- `開発記録帳.md`

#### 検証
- `NarakuEditor` Debug x64 / Release x64をビルドし、エラー0で成功した。Debugには既存のsize_t変換警告7件が残る。
- `NarakuGame` Debug x64 / Release x64をビルドし、エラー0で成功した。Debugには既存のsize_t変換警告7件が残る。
- 変更対象ファイルの`git diff --check`は終了コード0。既存ファイルのLFからCRLFへの変換警告のみだった。

#### 未検証・既知事項
- Editor実画面で各ブロック操作と、許可される陸地・溜りおよび池・湖間の変更を確認する必要がある。

### 2026-09-14 落下中のロープ把持

#### 変更意図
- ロープ端まで移動できない落下中でも、近くを通過したロープをFキーでつかめるようにするため。
- ロープをつかんだ後に、それまでの落下距離によるダメージが残らないようにするため。

#### 変更内容
- プレイヤーの足元とロープ上下端を結ぶ三次元線分との最近接距離を判定する処理を追加した。
- 落下中にFキーを押した時、ロープ線分から半径0.5m以内であれば最近接位置へ吸着するようにした。
- 把持時に落下速度、滞空時間、落下開始高、歩行不可セル上の空中移動状態をリセットした。
- 通常時の端点からの乗降、ロープ昇降、足場がある場合の横離脱は従来仕様を維持した。
- Game側の実装をEditorの歩行確認へ同期した。

#### 変更ファイル
- `NarakuGame/Source/SceneNarakuProto.cpp`
- `NarakuGame/Source/SceneNarakuProto.h`
- `NarakuEditor/Source/SceneNarakuProto.cpp`
- `NarakuEditor/Source/SceneNarakuProto.h`
- `開発記録帳.md`

#### 検証
- `NarakuGame` Debug x64 / Release x64をビルドし、エラー0で成功した。
- `NarakuEditor` Debug x64 / Release x64をビルドし、エラー0で成功した。
- 分離ソースの同期チェックは成功した。
- 変更対象ファイルの`git diff --check`は終了コード0。既存ファイルのLFからCRLFへの変換警告のみだった。

#### 未検証・既知事項
- 実画面で落下中にロープ中央付近をFキーでつかめること、直径1mの外側ではつかめないこと、把持後に落下ダメージが発生しないことを確認する必要がある。

### 2026-09-14 ロープ3Dモデル表示

#### 変更意図
- 上下端のデバッグ箱だけだったロープへ、追加されたFBXモデルを使った視認可能な本体表示を加えるため。
- ロープごとに長さや傾きが異なっても、個別のモデル設定を増やさず配置データへ追従させるため。

#### 変更内容
- `RoupeWithSceleton.fbx`の頂点境界から長軸、中心、寸法を読み取るロープ専用モデル資源を追加した。
- 各ロープの上下端を結ぶ三次元線分に長軸を向け、全長へ合うよう拡縮して描画するようにした。
- ロープの表示直径を約8cmへ統一した。操作用の直径1m判定は変更していない。
- FBX内に残る未同梱TIFへの参照に依存せず、同梱の`txtr.png`を拡散テクスチャとして明示適用した。
- モデル読込に失敗した場合だけ、従来の上下端デバッグ箱を表示するようにした。
- ロープ素材を共有Assetsへ登録し、Game、Editor、各出力先へ配置できるようにした。
- Game側の実装をEditorの歩行確認へ同期した。

#### 変更ファイル
- `Assets/Model/rope/source/RoupeWithSceleton.fbx`
- `Assets/Model/rope/textures/txtr.png`
- `Assets/Model/rope/textures/nm.png`
- `NarakuGame/Source/SceneNarakuProto.cpp`
- `NarakuGame/Source/SceneNarakuProto.h`
- `NarakuEditor/Source/SceneNarakuProto.cpp`
- `NarakuEditor/Source/SceneNarakuProto.h`
- `開発記録帳.md`

#### 検証
- `NarakuGame` Debug x64 / Release x64をビルドし、エラー0で成功した。
- `NarakuEditor` Debug x64 / Release x64をビルドし、エラー0で成功した。
- FBXと拡散テクスチャがGame、Editor、Debug、Releaseの各Assetsへ配置されたことを確認した。
- 分離ソースの同期チェックは成功した。
- 変更対象ファイルの`git diff --check`は終了コード0。既存ファイルのLFからCRLFへの変換警告のみだった。

#### 未検証・既知事項
- 実画面でモデルの向き、太さ、テクスチャ表示、上下端への接続を確認する必要がある。
- `nm.png`は現行マテリアルとシェーダーが法線マップに対応していないため、今回は描画へ適用していない。

### 2026-09-14 ロープ上端の支柱配置

#### 変更意図
- ロープ本体が上層の地面へ直接埋まって見える状態を解消し、固定箇所を視覚的に分かるようにするため。

#### 変更内容
- 追加された`rope.fbx`をロープ上端用の支柱モデルとして共有Assetsへ登録した。
- 支柱モデルの境界から底面中央と高さを取得し、底面が上層地面へ接するように配置した。
- 支柱を高さ約1.6mへ正規化し、ロープの降下方向に向けて回転するようにした。
- 支柱をロープ穴から降下方向と反対へ約0.55mずらし、ロープ本体の表示上端を支柱上部へ接続した。
- 支柱の木材表示へ既存の`tree_bark1.jpg`を明示適用した。
- ロープの操作位置、把持判定、昇降経路は変更していない。
- Game側の実装をEditorの歩行確認へ同期した。

#### 変更ファイル
- `Assets/Model/rope/rope.fbx`
- `NarakuGame/Source/SceneNarakuProto.cpp`
- `NarakuGame/Source/SceneNarakuProto.h`
- `NarakuEditor/Source/SceneNarakuProto.cpp`
- `NarakuEditor/Source/SceneNarakuProto.h`
- `開発記録帳.md`

#### 検証
- `NarakuGame` Debug x64 / Release x64をビルドし、エラー0で成功した。Debugには既存のsize_t変換警告7件が残る。
- `NarakuEditor` Debug x64 / Release x64をビルドし、エラー0で成功した。
- 支柱FBXがGame、Editor、Debug、Releaseの各Assetsへ配置されたことを確認した。
- 分離ソースの同期チェックは成功した。
- 変更対象ファイルの`git diff --check`は終了コード0。既存ファイルのLFからCRLFへの変換警告のみだった。

#### 未検証・既知事項
- 実画面で支柱の向き、大きさ、地面への接地、ロープとの接続位置を確認する必要がある。

### 2026-09-14 ロープ支柱の向きと接続位置調整

#### 変更意図
- 支柱の横棒が想定と逆向きになり、ロープが横棒先端へ接続されていなかったため。

#### 変更内容
- 支柱へ従来の自動方向計算に加えて、Y軸周りの180度回転を適用した。
- 支柱とロープ上端の距離を固定値から、拡縮後モデルの水平半径を使う計算へ変更した。
- 支柱の横棒先端がロープ上端のXZ座標へ一致するよう、支柱中心位置を逆算した。
- ロープ本体の表示上端を支柱上部の高さ1.5mへ調整した。
- 操作用のロープ線分、把持判定、昇降経路は変更していない。

#### 変更ファイル
- `NarakuGame/Source/SceneNarakuProto.cpp`
- `NarakuEditor/Source/SceneNarakuProto.cpp`
- `開発記録帳.md`

#### 検証
- `NarakuGame` Debug x64 / Release x64をビルドし、エラー0で成功した。Debugには既存のsize_t変換警告7件が残る。
- `NarakuEditor` Debug x64 / Release x64をビルドし、エラー0で成功した。
- 分離ソースの同期チェックは成功した。
- 変更対象ファイルの`git diff --check`は終了コード0。既存ファイルのLFからCRLFへの変換警告のみだった。

#### 未検証・既知事項
- 実画面で横棒先端とロープ上端が一致し、支柱が指定方向を向いていることを確認する必要がある。

### 2026-09-14 ロープ支柱のセル中央配置

#### 変更意図
- 支柱をセル内の端へ寄せず中央へ置き、横棒とロープだけがセル外へ張り出す配置にするため。

#### 変更内容
- ロープ上端が属する地形セルを取得し、支柱のXZ位置をセル中心へ固定した。
- セル中心から降下方向のセル外周までの距離を算出し、ロープ表示上端を直径ぶん外側へ出した。
- 斜め方向でも正方形セルの内側に残らないよう、方向成分に応じて外周との交点距離を計算した。
- 支柱の高さと太さを維持し、横棒方向だけを接続位置まで伸縮するようにした。
- 操作用のロープ座標、把持判定、昇降経路、マップ表示は変更していない。

#### 変更ファイル
- `NarakuGame/Source/SceneNarakuProto.cpp`
- `NarakuEditor/Source/SceneNarakuProto.cpp`
- `開発記録帳.md`

#### 検証
- `NarakuGame` Debug x64 / Release x64をビルドし、エラー0で成功した。Debugには既存のsize_t変換警告7件が残る。
- `NarakuEditor` Debug x64 / Release x64をビルドし、エラー0で成功した。
- 分離ソースの同期チェックは成功した。
- 変更対象ファイルの`git diff --check`は終了コード0。既存ファイルのLFからCRLFへの変換警告のみだった。

#### 未検証・既知事項
- 実画面で支柱がセル中央にあり、横棒先端とロープがセル境界外で接続されることを確認する必要がある。

### 2026-09-14 ロープ昇降経路と落下ダメージの調整

#### 変更意図
- 支柱先端から下端へ描画されるロープと、プレイヤーが昇降する内部経路が異なり、昇降中にプレイヤーがロープを横切る問題を解消する。
- 8m以上の落下でHPに関係なく即死していたため、最大HPに応じて段階的に被害が増える猶予を設ける。

#### 変更内容
- 支柱位置、表示上端、下端をまとめたロープ端点計算を追加し、描画、通常昇降、落下中の把持判定で共用した。
- ロープの昇降速度計算を、表示されているロープの実際の長さ基準へ変更した。
- 上端到達後は従来どおり支柱セル側の足場へ降り、下端到達後は下側の足場へ降りる動作を維持した。
- 2m以下は無傷、2m超から20m未満は最大HPの0～100%を直線補間したダメージ、20m以上は即死へ変更した。
- 落下ダメージは防御力を適用せず、生存的遺物でも防げない既存方針を維持した。

#### 変更ファイル
- `NarakuGame/Source/SceneNarakuProto.cpp`
- `NarakuGame/Source/SceneNarakuProto.h`
- `NarakuEditor/Source/SceneNarakuProto.cpp`
- `NarakuEditor/Source/SceneNarakuProto.h`
- `開発記録帳.md`

#### 検証
- `NarakuGame` Debug x64 / Release x64でコンパイルとリンクが成功した。Debugには既存のsize_t変換警告7件が残る。
- `NarakuEditor` Debug x64 / Release x64でコンパイルとリンクが成功した。
- 分離ソースの同期チェックは成功した。
- 変更対象ファイルの`git diff --check`は終了コード0。既存ファイルのLFからCRLFへの変換警告のみだった。

#### 未検証・既知事項
- 実画面で上端・下端・落下中の各把持位置がロープ本体と一致することを確認する必要がある。
- 2m、10m、20m付近からの落下で意図したHP減少になることを実プレイで確認する必要がある。

### 2026-09-14 ロープのぶら下がり位置と接地離脱

#### 変更意図
- ロープ座標がプレイヤーの足元に割り当てられていたため、昇降位置によってロープがプレイヤーの上下を貫通して見える問題を解消する。
- プレイヤーをロープの下へ配置すると下端で地面へ埋まるため、足が地面へ達した段階で安全に離脱させる。

#### 変更内容
- ロープ上の座標を頭上の把持位置として扱い、プレイヤーの足元を1.4m下へ配置した。
- 通常把持と落下中把持のどちらにも同じ足元位置計算を適用した。
- 下降中の足元が下側地形へ達した場合、現在XZ位置が歩行可能ならその場で自動離脱するようにした。
- 着地点が歩行禁止、削除セル、または床なしの場合は自動離脱せず、それ以上地面へ進入しないようにした。
- Fキーによる端点離脱にも同じ着地可能判定を追加した。

#### 変更ファイル
- `NarakuGame/Source/SceneNarakuProto.cpp`
- `NarakuGame/Source/SceneNarakuProto.h`
- `NarakuEditor/Source/SceneNarakuProto.cpp`
- `NarakuEditor/Source/SceneNarakuProto.h`
- `開発記録帳.md`

#### 検証
- `NarakuGame` Debug x64 / Release x64でコンパイルとリンクが成功した。Debugには既存のsize_t変換警告7件が残る。
- `NarakuEditor` Debug x64 / Release x64でコンパイルとリンクが成功した。
- 分離ソースの同期チェックは成功した。
- 変更対象ファイルの`git diff --check`は終了コード0。既存ファイルのLFからCRLFへの変換警告のみだった。

#### 未検証・既知事項
- 実画面でロープがプレイヤー頭上の把持位置へ揃うことを確認する必要がある。
- 通常床では接地時に自動離脱し、歩行禁止セルと削除セルではロープ上に留まることを実プレイで確認する必要がある。

### 2026-09-14 ロープ下端からの把持位置調整

#### 変更意図
- 下端側からロープを掴んだ直後、足元が下端地面より下へ配置されてプレイヤーが埋まる問題を解消する。

#### 変更内容
- 下端把持時に進捗100%へ固定する処理を廃止した。
- プレイヤーの足元が下端地面より0.1m上になる把持位置を、ロープの上端・下端のワールド高さから逆算するようにした。
- 算出した進捗はロープ範囲内へ制限し、上端把持、落下中把持、下降時の自動離脱には影響させていない。

#### 変更ファイル
- `NarakuGame/Source/SceneNarakuProto.cpp`
- `NarakuGame/Source/SceneNarakuProto.h`
- `NarakuEditor/Source/SceneNarakuProto.cpp`
- `NarakuEditor/Source/SceneNarakuProto.h`
- `開発記録帳.md`

#### 検証
- `NarakuGame` Debug x64 / Release x64でコンパイルとリンクが成功した。Debugには既存のsize_t変換警告7件が残る。
- `NarakuEditor` Debug x64 / Release x64でコンパイルとリンクが成功した。
- 分離ソースの同期チェックは成功した。
- 変更対象ファイルの`git diff --check`は終了コード0。既存ファイルのLFからCRLFへの変換警告のみだった。

#### 未検証・既知事項
- 傾斜したロープの下端側から掴んだ際、足元が地面より約0.1m上に見えることを実画面で確認する必要がある。

### 2026-09-14 第二拠点・前衛拠点UIの地上レイアウト統一

#### 変更意図
- 第二拠点と前衛拠点だけが個別サイズの単一ペインUIになっていたため、地上施設と同じ情報配置と操作感へ統一する。

#### 変更内容
- 地上施設の共通フレームへ戻り先モードと戻るボタン名を渡せるようにした。
- 第二拠点と前衛拠点を、地上施設と同じ1180x760の3ペイン構成へ移行した。
- 左ペインに装備情報と次回持ち込み品、中央ペインに既存の拠点タブ、右ペインに現在のプレイヤーステータスを表示する。
- 拠点側では共通フレームの戻る操作を「拠点を出る」とし、探索画面へ戻る既存動作を維持した。
- 商品、価格、回復量、売却倍率、宿泊、購入処理は変更していない。

#### 変更ファイル
- `NarakuGame/Source/SceneNarakuProto.cpp`
- `NarakuGame/Source/SceneNarakuProto.h`
- `NarakuEditor/Source/SceneNarakuProto.cpp`
- `NarakuEditor/Source/SceneNarakuProto.h`
- `開発記録帳.md`

#### 検証
- `NarakuGame` Debug x64 / Release x64でコンパイルとリンクが成功した。Debugには既存のsize_t変換警告7件が残る。
- `NarakuEditor` Debug x64 / Release x64でコンパイルとリンクが成功した。
- 分離ソースの同期チェックは成功した。
- 変更対象ファイルの`git diff --check`は終了コード0。既存ファイルのLFからCRLFへの変換警告のみだった。

#### 未検証・既知事項
- 実画面で両拠点の3ペイン比率、タブ内容のスクロール、戻る操作を確認する必要がある。
- 地上アイテム「ライト」は仕様検討のみで、今回のコードには追加していない。

### 2026-09-14 深度別照明と携帯ライト

#### 変更意図
- 深度ごとの暗さをゲームプレイへ反映し、時間制の携帯ライトを探索準備と視界管理の資源として追加する。

#### 変更内容
- 第一～第四層の真下方向光と、第五層の前衛拠点有無によるポイントライト／横方向光を実装した。
- モデル用Lambertシェーダーへ方向光、ポイントライト、スポットライトを統合し、地形・水面・敵・プレイヤーにも同じ場面照度を反映した。
- 地上商店へライトを追加し、15分寿命、破損、自動切替、個体別持ち込み、廃棄、通常／破損売却、第二拠点の破損品相場を実装した。
- ライト専用キーコンフィグ項目を初期未割り当てで追加し、所持品からも点灯・消灯できるようにした。
- 死亡時の自動返却、生還時返却、放棄時消失、各UI中の時間停止、層間移動時の点灯維持を実装した。
- セーブをv11へ更新し、v10を直前形式として読み込み、ライトの残り時間・破損・持ち込み設定を保存するようにした。

#### 変更ファイル
- `NarakuGame/Source/Input.cpp`、`Input.h`、`InputConfig.cpp`、`InputConfig.h`
- `NarakuGame/Source/SceneNarakuProto.cpp`、`SceneNarakuProto.h`
- `NarakuGame/Source/ShaderList.cpp`、`ShaderList.h`
- 同期された`NarakuEditor/Source`および`DX22_Project`
- `NarakuProto_追加要素仕様書.md`
- `開発記録帳.md`

#### 検証
- 分離ソース同期に成功した。
- `NarakuGame`と`NarakuEditor`のDebug x64／Release x64でコンパイルとリンクに成功した。
- Debugゲームを5秒間起動し、初期化直後に異常終了しないことを確認した。

#### 未検証・既知事項
- 実画面で各深度の明度、第五層の光源切替、ライトの照射範囲と寿命消費、売買・死亡返却・セーブ復元を確認する必要がある。
- 影は仕様どおり未実装のため、携帯ライトは壁越しにも光が届く場合がある。

### 2026-09-14 統合メニュー復帰入力と照明範囲の修正

#### 変更意図
- EキーのマップとIキーの所持品を閉じた後、カメラ以外の入力がガードされ続ける不具合を解消する。
- 携帯ライトの照射がセル単位に見える状態を解消し、カメラ操作と一致した視認性を得る。

#### 変更内容
- 入力ガードの解除判定を全仮想キー走査から、現在設定されているゲームアクション入力の解放確認へ変更した。
- 地形と水面の照明をセル中心のCPU着色から、ワールド座標・面法線を使うピクセル単位のシェーダー計算へ変更した。
- 携帯ライトをカメラ正面方向、45mのスポットライトへ変更した。
- 第五層の前衛拠点のないエリアは、一様な方向光ではなく北西側のステージ外光源から距離減衰するように変更した。

### 2026-09-14 地形照明の三角形状アーティファクト修正

#### 変更意図
- ピクセル単位照明の導入後に、地形を構成する三角形ごとの法線差が過度に強調された表示を解消する。

#### 変更内容
- 各三角形へ同一のフラット法線を与える方式を廃止した。
- 各地形頂点の前後左右の高さ差から法線を求め、隣接セルで共有した頂点法線をGPU上で補間するようにした。
- 地形照明の最低値を5%へ調整し、光の裏側で黒い三角形が発生しにくいようにした。

### 2026-09-14 地形照明の安定化とカメラ遮蔽処理の置換

#### 変更意図
- 非平面セルの三角形分割が地形の明暗に出る状態と、カメラ操作中に遮蔽地形が消失してちらつく状態を解消する。

#### 変更内容
- 地形と水面の照明法線を上向きに統一し、セルの三角形分割が明暗へ影響しないようにした。ライトの円錐角と距離減衰は引き続きピクセル単位で計算する。
- カメラとプレイヤーの間を遮る地形セルを描画対象から外す処理を廃止した。
- 同じ地形交差判定をカメラ衝突に使い、衝突時は即座にプレイヤー側へ寄せ、解消後は毎秒8mで設定距離へ戻すようにした。

### 2026-09-14 携帯ライトの地面照射修正

#### 変更意図
- 高さのあるモデルは明るくなる一方、カメラの見下ろし角と同じ方向へ出した光が足元へ刺さり、前方の地面を照らさない状態を解消する。

#### 変更内容
- 携帯ライトの方向を、カメラの水平方向に追従しつつ下へ5度傾ける方式へ変更した。
- 外側照射角を45度から60度へ広げ、内側の30度から外側の60度までを滑らかに減衰させた。
- 地形と水面のスポット光は面法線で追加減衰させず、円錐角と距離によって前方の地面を照らすようにした。

### 2026-09-14 地形スポットライト補間値の修正

#### 変更意図
- 携帯ライトでモデルは明るくなる一方、地面と水面が明るくならない不具合を解消する。

#### 変更内容
- 地形用頂点シェーダーからピクセルシェーダーへワールド座標と法線を明示的に渡すようにした。
- 未初期化のワールド座標を使っていたスポットライトの距離・円錐角計算を、実際の地形位置に基づく計算へ修正した。
