# 最適化の技術解説

## 結論

最大のボトルネック候補だった「床1セルにつき1回の `Sprite::Draw()`」を、表示対象の床を動的頂点バッファへまとめる方式へ変更しました。現在の4×4生成マップを上限モデルにすると、地形床のドローコールは最大3600回から1回になります。ゲーム中の線描画も0回になり、線用頂点バッファの毎フレーム転送も発生しません。敵も個別のデバッグ球ではなく、全生存敵を1つのビルボードバッチで描画します。

## 計算の前提

現在の固定生成条件は次のとおりです。

- マップ: 4×4ピース = 16ピース
- 1ピース: 16×16頂点
- 1ピースのセル: `(16 - 1) × (16 - 1) = 225`
- 地形セル上限: `16 × 225 = 3600`

以下では、実際に床へ追加された四角形数を `Q` とします。削除セル、無効頂点、プレイヤーを遮るセルは除外されるため、通常は `Q <= 3600` です。ロープ穴と階層ゲートの小さな床板は別途 `2 × ロープ数 + ゲート数` が加算されます。

## ドローコール

| 対象 | 変更前 | 変更後 | 最大3600セル時の削減 |
|---|---:|---:|---:|
| 地形床 | `Q` 回 | `Q > 0` なら1回 | 3600 → 1、99.9722%削減 |
| 地形グリッドなどの線 | 1回 | 0回 | 100%削減 |
| 敵本体 | 生存敵数 `E` 回 | `E > 0` なら1回 | 現在の3体では3 → 1、66.67%削減 |
| 地形床と線の合計 | 3601回程度 | 1回 | 約99.9722%削減 |

敵以外の箱、当たり判定球、攻撃ヒットエフェクトのドローコールは、表示と挙動を維持するため変更していません。

## 敵ビルボードの転送量

敵頂点は位置`float3`とUV`float2`で1頂点20バイト、1体あたり6頂点です。現在の3体では、確保容量と毎フレーム転送量は`3 × 6 × 20 = 360`バイトです。ビュー・プロジェクション行列128バイトを1回更新し、テクスチャ、シェーダー、頂点バッファを1回ずつ設定して1ドローします。

敵数分のワールド行列更新と球メッシュ描画を行わず、CPU側でカメラ正対済み頂点を連続配列へ生成します。敵の移動とカメラ回転があるため頂点位置は毎フレーム更新しますが、敵数が増えてもドローコールは1回のままです。

## API呼び出しとデータ転送

### 変更前

`Sprite::Draw()` は床1枚ごとに次を行っていました。

- 頂点シェーダー定数バッファ更新: 2回
- 頂点・ピクセルシェーダーのバインド
- 白テクスチャのバインド
- メッシュのドロー: 1回

3600セルなら、定数バッファ更新は `2 × 3600 = 7200` 回です。論理データ量は、行列3個192バイトとパラメーター48バイトで、`240 × 3600 = 864,000` バイト/フレームです。データ量以上に、7200回の細かな更新と3600回の状態設定・ドローがCPU負荷になります。

### 変更後

- ビュー・プロジェクション行列の定数更新: 1回、128バイト
- 動的頂点バッファ更新: 1回
- シェーダーのバインド: 各1回
- ドロー: 1回

床頂点は1セルあたり `6頂点 × 28バイト = 168バイト` です。3600セル分の確保容量は `604,800` バイトで、行列を加えると `604,928` バイト/フレームです。旧方式の論理転送量864,000バイトに対し、約29.99%少なくなります。

現行 `MeshBuffer::Write()` は表示頂点数ではなく確保容量全体をコピーするため、この604,800バイトは削除セルや遮蔽セルがあっても基本的に変わりません。ただし、細かな更新を1回の連続コピーへ変えたことと、ドローコールを1回へ減らしたことが主な効果です。ロープ穴とゲート分の容量は上記へ少量加算されます。

## 線描画の削減

変更前の地形グリッドは1セルにつき4辺を登録していたため、3600セルなら最大14,400本を毎フレーム生成していました。共有辺も各セルから重複登録されます。

`Geometory` の線バッファは最大50,000本、100,000頂点を持ち、`LineVertex` は28バイトです。既存の `MeshBuffer::Write()` は使用本数に関係なく確保容量全体をコピーするため、`100,000 × 28 = 2,800,000` バイト、約2.67 MiBを `DrawLines()` ごとに転送していました。

今回 `SceneNarakuProto` から線の登録と描画をなくしたため、このシーンでは次が0になります。

- 線生成: 最大14,400本/フレーム + 各種マーカー線
- 線頂点転送: 2,800,000バイト/フレーム
- 線ドロー: 1回/フレーム

転送回避量は60 FPSで約168 MB/秒、120 FPSで約336 MB/秒です。これはメモリ帯域だけでなく、CPU側の頂点生成とコピー処理も含めて削減します。

## セル走査

変更前は床描画用とグリッド線用に地形セルを別々に走査し、削除、有効頂点、遮蔽の判定を行っていました。グリッド線ループを廃止したため、地形描画に関する主要セル走査は2回から1回となり、上限3600セルでは7200回から3600回、50%削減です。

## 地形やエリアが変わる場合

`ResetRun()`、`ActivateArea()`、`BuildCurrentAreaRuntime()` の後で `RebuildTerrainFloorBatch()` を呼びます。この関数は現在の全地形レイヤーのセル数、ロープ数、ゲート数から必要容量を再計算します。そのため、マップごとに地形構成や `groundTextureId` が変わっても、ゲーム処理やマップ形式を変更せず同じバッチ経路で描画できます。

現在の `groundTextureId` は実テクスチャではなく床色の選択に使われています。将来、本物の地面テクスチャを使う場合は、同一サイズ・同一フォーマット・同一ミップ数の画像を `Texture2DArray` にまとめ、頂点へテクスチャ配列インデックスを追加する方式が、マップごとに種類が変わっても1バッチを維持しやすい方式です。このテクスチャ機能は今回の変更には含めていません。

## 数値の扱い

上記はコードと固定生成条件から求めた理論値です。FPSの改善量はCPU、GPU、解像度、VSync、Debug/Release構成、画面内の箱・球・エフェクト数で変わるため、「FPSが何%上がる」とは断定できません。実測時は平均FPSだけでなく、`Scene Draw` と `Present` を分けて比較してください。
