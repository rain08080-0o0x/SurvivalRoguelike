# ファイル別変更解説

## この文書の目的

今回の最適化を今後の担当者が追跡できるよう、変更ファイルごとの責務、処理の流れ、保守時の注意点をまとめます。

## `DX22_Project/SceneNarakuProto.h`

### 追加した型

`TerrainFloorVertex` は地形床バッチ専用の頂点です。

- `position`: ワールド座標。床セルをCPU側で最終位置へ展開します。
- `color`: `groundTextureId`、上層透明度、階層遷移フェードを反映済みの色です。

ワールド行列を頂点ごとに持たず、全頂点が共通のビュー・プロジェクション行列だけを使うため、セルごとの定数バッファ更新が不要になります。

`EnemyBillboardVertex`は敵ビルボード専用の頂点です。

- `position`: CPU側でカメラ正対済みにしたワールド座標です。
- `uv`: 9×16スプライトシート内の使用セル座標です。

### 追加したメソッド

- `InitializeTerrainFloorBatch()`: 専用シェーダーを生成します。シーン生成時に1回だけ呼びます。
- `ReleaseTerrainFloorBatch()`: メッシュとシェーダーを解放します。シーン破棄時に呼びます。
- `RebuildTerrainFloorBatch()`: 現在のマップ構成から最大頂点数を計算し、動的メッシュとCPU配列を作り直します。
- `AppendTerrainFloorQuad()`: 四角形を2三角形、6頂点へ展開します。描画は行いません。
- `DrawTerrainFloorBatch()`: 当該フレームに追加済みの頂点をまとめて1回描画します。
- `InitializeEnemyBillboardBatch()`: 敵画像、専用シェーダーを読み込みます。
- `ReleaseEnemyBillboardBatch()`: 敵メッシュ、シェーダー、テクスチャを解放します。
- `RebuildEnemyBillboardBatch()`: 現在エリアの敵数から最大頂点数を確保します。
- `DrawEnemyBillboardBatch()`: 全生存敵の正対四角形を作り、1回で描画します。

### 追加したメンバー

- `m_terrainFloorMesh`: GPU側の動的頂点バッファを所有します。
- `m_terrainFloorVS` / `m_terrainFloorPS`: 床専用シェーダーを所有します。
- `m_terrainFloorVertices`: 最大構成分を確保して再利用するCPU配列です。
- `m_terrainFloorVertexCount`: 現フレームに実際に描く頂点数です。
- `m_enemyBillboardMesh`: 全敵共通の動的頂点バッファです。
- `m_enemyBillboardVS` / `m_enemyBillboardPS`: 敵画像の変換と透過描画を行います。
- `m_enemyTexture`: `Assets/Texture/Character/Enemy/enemy.png`を所有します。
- `m_enemyBillboardVertices`: 現在エリアの最大敵数分を再利用するCPU配列です。
- `m_enemyBillboardVertexCount`: 現フレームの生存敵に対応する頂点数です。

### 削除したメンバー・宣言

- `m_debugWhiteTexture`: 旧床描画で `Sprite` に設定していた白1×1テクスチャです。新シェーダーは頂点色を直接出力するため不要です。
- `DrawTransparentFloor3D()`: 床1枚ごとに `Sprite::Draw()` していた関数です。

## `DX22_Project/SceneNarakuProto.cpp`

### シーンの生成と破棄

コンストラクターで `InitializeTerrainFloorBatch()` を呼びます。デストラクターでは `ReleaseTerrainFloorBatch()` を呼び、メッシュ、ピクセルシェーダー、頂点シェーダーの順に解放します。旧白テクスチャの生成・解放は削除しました。

同じくコンストラクターで`InitializeEnemyBillboardBatch()`を呼び、敵画像と専用シェーダーを準備します。デストラクターでは`ReleaseEnemyBillboardBatch()`を呼び、敵表示資源を解放します。

### バッファ容量の再構築

`RebuildTerrainFloorBatch()` は次の最大四角形数を合計します。

```text
全地形レイヤーの (gridWidth - 1) × (gridHeight - 1)
+ 2 × ロープ数
+ 階層ゲート数
```

削除セルや遮蔽セルも容量には含めます。毎フレームの再確保を避け、マップ構成が変わったときだけ再構築するためです。

呼び出し位置は次の3か所です。

- `ResetRun()`: ラン開始時に現在のランタイムマップへ合わせる。
- `ActivateArea()`: 保存済みエリアへ切り替えた後に合わせる。
- `BuildCurrentAreaRuntime()`: 新しいエリアのランタイム構成を作った後に合わせる。

地形レイヤー、ロープ、ゲートの個数を変更する新しい経路を追加した場合は、その経路でも構成確定後に再構築が必要です。

### 1フレームの床描画フロー

```text
Draw3DField()
  → m_terrainFloorVertexCount を0に戻す
  → 各地形セルの既存表示判定を行う
  → 表示セルを AppendTerrainFloorQuad() へ渡す
  → ロープ穴と階層ゲートを同じ配列へ追加する
  → DrawTerrainFloorBatch() を1回呼ぶ
```

`AppendTerrainFloorQuad()` は配列上限を確認し、超える場合は追加しません。上限超過は容量計算漏れを意味するため、床種別を増やす場合は `RebuildTerrainFloorBatch()` も同時に更新してください。

### 表示判定と挙動の維持

次の判定と計算は旧床ループからそのまま残しています。

- `CellAttributeRemoved` のセルを描かない。
- 4頂点のいずれかが無効なら描かない。
- プレイヤーを遮るセルを描かない。
- 4頂点の平均位置に平面床を置く。
- `groundTextureId` で床色を選ぶ。
- プレイヤーより浅い上層を薄くする。
- 階層遷移中に透明度を下げる。

変更対象は描画命令のまとめ方であり、移動、当たり判定、イベント判定には床バッチのデータを使用していません。

### 線描画の停止

`Draw3DField()` 内の `Geometory::AddLine()` と `Geometory::DrawLines()` を削除しました。対象には地形グリッドと各種マーカー線が含まれます。箱、球、スプライトによる表示は維持しています。

`Geometory` 自体は変更していないため、他シーンの線描画には影響しません。

### 敵ビルボード

旧`DrawDebugSphere3D()`による敵本体表示を`DrawEnemyBillboardBatch()`へ置き換えました。

```text
ビュー行列を逆変換してカメラ正対行列を作る
  → 生存敵ごとに1.2×1.2の四角形を6頂点へ展開
  → 左上緑スライムの中央列・先頭行UVを設定
  → 全頂点を1回転送
  → 敵画像を1回バインド
  → 全敵を1回描画
```

透明ピクセルはピクセルシェーダーの`clip`で破棄します。これにより、透明な四角形部分が後続オブジェクトの深度を遮りません。敵描画中だけポイントサンプラーを使い、描画後に線形サンプラーへ戻します。

敵数に応じた容量再構築は床バッチと同じく`ResetRun()`、`ActivateArea()`、`BuildCurrentAreaRuntime()`の構成確定後に行います。

## `修正箇所.md`

要求に対して何を変更し、何を変更しなかったか、意図した表示差、ビルド結果をまとめた完了記録です。

## `最適化.md`

4×4生成マップを基準に、ドローコール、定数バッファ更新、頂点転送、セル走査の変更前後を式と数値で示しています。数値は理論値であり、実測FPSとは分離しています。

## `操作概要.md`

ビルド、動作確認、`F2` オーバーレイによる計測、地形や床種別を増やす場合の保守手順を記載しています。

## 変更していない関連ファイル

- `MeshBuffer.h/.cpp`: 共通の作成、更新、描画APIは変更していません。
- `Shader.h/.cpp`: 共通のシェーダーAPIは変更していません。
- `Sprite.h/.cpp`: 他のスプライト描画へ影響させないため変更していません。
- `Geometory.h/.cpp`: 他シーンの線・箱・球描画を維持するため変更していません。
- `NarakuStageGenerator.h/.cpp`: 4×4生成条件と地形生成処理は変更していません。
- `NarakuMapData.h/.cpp`: マップデータとJSON形式は変更していません。
- `Assets/Naraku/Pieces`: ピースデータは変更していません。

## 引き継ぎ時の注意点

- FPS改善率は環境依存です。理論上の呼び出し削減率を、そのままFPS改善率として扱わないでください。
- 現行床はテクスチャ画像ではなく頂点色です。`groundTextureId` は色選択に使われています。
- 現行敵表示は静止画です。待機、移動、攻撃、被弾、死亡アニメーションは未実装です。
- 敵アニメーション追加時は`EnemyBillboardVertex::uv`だけを状態と時刻から切り替え、1バッチ描画を維持してください。
- 実テクスチャ化する場合、セルごとにテクスチャを切り替えると再びバッチが分割されます。`Texture2DArray` と頂点の配列番号で1バッチを維持してください。
- 透明オブジェクトのため、将来セルごとの重なり順が視覚上必要になった場合はソート方法を別途検討してください。今回の描画順は既存セル順を維持しています。
- `MeshBuffer::Write()` は確保容量全体をコピーします。遮蔽セルが非常に多いマップで転送量も減らしたい場合は、使用頂点数を指定できる更新APIを別変更として設計し、全呼び出し元への影響を検証してください。
